---
name: rjl-crm-project
description: "RJL Capital Advisors custom CRM build replacing HubSpot - locations, stack, brand, scope decisions (started 2026-09-03)"
metadata: 
  node_type: memory
  type: project
  originSessionId: fdffdb35-ad78-46a7-a3c1-464ba2b6395a
  modified: 2026-09-03T20:43:23.021Z
---

Jonathan (RJL Capital Advisors, real estate capital advisory) is replacing HubSpot with a custom CRM built in Claude Code, started 2026-09-03.

**Locations**
- Source code: `C:\Users\Jon\Projects\rjl-crm` (local, not OneDrive, because node_modules breaks OneDrive sync).
- Docs, HubSpot exports, brand assets: `C:\Users\Jon\OneDrive - RJL Equities\Jonathan Livi\RJL Capital Advisors\New CRM Via Claude Fable 5\` (synced SharePoint folder the user shared). Subfolders: `01 HubSpot Export 2026-09-03`, `02 Brand Assets`, `03 Plan and Docs`.
- Node 24 installed per-user at `%LOCALAPPDATA%\Programs\nodejs` (winget install needs admin and was cancelled). Bash needs `export PATH="/c/Users/Jon/AppData/Local/Programs/nodejs:$PATH"`.

**Decisions**
- Stack: Next.js (App Router, TS, Tailwind) + Prisma. SQLite locally first, Postgres (Supabase) for hosted deploy. Vercel hosting planned.
- 5 users. Email provider is Outlook / Microsoft 365 (not Gmail). Fireflies for call transcripts.
- Brand from logo: ink `#1A2321` (near-black green), cream `#FFF8D9`, sky blue `#B7CFE8`.
- Deal pipeline stages (HubSpot order): Deal Mentioned, Deal Received, Deal Underwritten, Deal Taken To Market, Intro To Capital Made, Term Sheet Issued, Term Sheet Signed, Deal Closed, Deal Lost. Deals view is a board, not a table.
- Deal names follow "Sponsor | Property" pattern.
- Feature scope: companies, contacts, deals board, deal-driven mail merge to matched LPs, email blasts (via transactional provider, not Outlook), auto-log sent Outlook emails to contacts/companies, Fireflies-driven LP criteria proposals with manual approval.

**HubSpot export facts (2026-09-03)**: 6,188 companies, 15,157 contacts (13,686 with email, 357 unsubscribed, ~893 hard bounces), 1,017 deals (899 Deal Lost). Deals export only had default 6 columns; custom deal properties (property address, LTV, loan term, etc.) still need a fuller export or connector pull. Classifier fields are duplicated/cloned in HubSpot and should be collapsed to one multi-select: Sponsor / Investor / Retail Investor / Lender / Broker.

**Why:** These are decisions and locations not derivable from the code alone.
**How to apply:** Continue building in the local project folder; put user-facing docs and data in the SharePoint folder. See [[rjl-brand-colors]] if written later.

**Update 2026-09-03 (later)**: Phase 3 done: templates, campaigns w/ LP matching (src/lib/matching.ts, audience.ts), deal intake (src/lib/intake.ts, checklist.ts), role inheritance company->contacts (src/lib/roles.ts). Git repo initialized in project folder. Pending from Jonathan: ANTHROPIC_API_KEY (intake extraction), RESEND_API_KEY + MAIL_FROM (sending), deals re-export with all properties, 5 user list, choice of intake mailbox (M365 shared mailbox + Power Automate vs Resend Inbound), hosting (Vercel + Supabase). HubSpot sales email templates are NOT reachable via the connector; user must paste them. Business rule: company roles propagate to its contacts because blasts are segmented by contact role (Sponsor vs Investor).

**Update 2026-09-04**: Deal emails are ONE-AT-A-TIME and personal (Jonathan rewrites the first line per investor) -> built outreach queue (campaign mode OUTREACH, CampaignRecipient.openingLine, "Open in Outlook" mailto + "Mark sent"). Blasts (mode BLAST) are the only thing with unsubscribe footers. Intake mailbox choice: Resend Inbound (option B), handler at src/app/api/inbound/resend/route.ts. ANTHROPIC_API_KEY is set (intake uses claude-opus-5 structured output; schema uses all-string fields because API caps nullable/union fields at 16). 14 HubSpot sales templates imported via scripts/import-templates.ts; blast template seeded via scripts/seed-blast-template.ts. Users: Jonathan, Aviel, Shawn, Esther Livi, Nikko Acosta (all @rjlcapadvisors.com). Pending: Resend DNS (IT), Vercel/Supabase accounts, Fireflies phase, pref-equity/fund fields if still used.

**Update 2026-09-04 (tracker)**: Progress report ("tracker") lives at /deals/[id]/tracker (DealInvestor rows, 8 statuses in src/lib/tracker.ts matching the legacy Drive HTML report + Word template: Prepared For, Notable Feedback Themes, Items Needed from Sponsor = checklist gaps). Sponsor read-only share at /share/tracker/<signed dealId token>; legacy standalone HTML export at /deals/[id]/tracker/export. Follow-up button creates OUTREACH campaign with followUp=true using template "Follow-up – confirm receipt"; logSent moves tracker rows to Deal Sent / Followed Up. Jonathan's workflow: checklist -> deal ticket -> template -> send one-by-one -> tracker shared with sponsors -> follow-ups. Response statuses 4-8 are manual until Outlook inbound logging (phase 4).

**Mission (2026-09-04)**: forward deal to deals@ -> auto-log + extract from email/PDF/model -> reply with checklist gaps -> sponsor replies auto-fill ticket -> pick LPs (criteria filter + learned suggestions from emails/Fireflies) -> draft engagement letter listing groups -> sponsor approves subset -> approved groups onto progress report -> deal email generated, best contact per firm chosen from email history -> one-by-one launch -> real-time tracker, PDF any time, Thursday 4 PM reports to sponsors -> one-button follow-up -> auto stage updates, Monday 11 AM dead-deal cleanup (keep as data). Full text in SharePoint "CRM Build Plan.md" Mission section.

**UX rules from Jonathan (2026-09-04 evening)**: no cream/yellow (white, light blue, black only); dashboard = to-do list only; fields always in ONE straight column on the LEFT of record pages (HubSpot layout: left fields, center activity/preview, right association cards); deal fields differ by asset class (MF per unit+per foot; retail/industrial per foot; hotel per key; student per bed); cap rates before IRR/EM/YOC/CoC; thousands separators; forwarded deals create deals immediately (no intake review); Deal Lost first column; contacts filed under company by email domain (src/lib/domains.ts). House-style deal email generator in src/lib/deal-copy.ts, template "Deal email (house style)".

**Criteria spec (2026-09-04)**: asset classes (multi dropdown); check sizes $1-2MM, $3-5MM, $5-8MM, $8-10MM, $10-15MM, $15-20MM, $20-30MM, $30-50MM, $50-100MM, $100MM+ (old HubSpot buckets migrated via CHECK_SIZE_MIGRATION); deal locations = free text (geographyNotes; tags derived internally); return profile (Core, Core+, Light value-add, Heavy value-add, Opportunistic); Development/Acquisitions/Both single; hold period (1-3, 3-5, 5-7, 7-10, 10+ years); vintages (Older than 1960, 1960s…2020s, New Construction); OZ interest Y/N; closing time frame Fast/Average/Slow; open to minority Y/N; other info text. HUD D4 interest dropped. Investment types (JV/Pref/Debt) column kept for matching but hidden from UI. Criteria panel open only if company role includes Investor/Retail Investor/Lender; sponsors see "Sponsor focus" = asset classes only.

**Update 2026-09-04 (late)**: Nav now has "Active progress reports" (/reports) under Investor search: every deal out to investors, sorted by longest wait, with Follow up buttons; Jonathan checks it daily. "Campaigns" renamed "Email blasts" in UI (routes still /campaigns). Four real progress reports loaded from Word (Everett Mall Plaza, Kansas City Metro Retail Portfolio/BSPG, Purveyor Space Coast, Marble Capital Intros) via scripts/load-progress-reports.js; intake test deal deleted. Company page mirrors contact page (Activity first, criteria card below, Deals card shows deals the firm was sent w/ status). Companies + Contacts lists: NO search button, auto-filter on roles + asset classes (multi-check), email-status filter removed; contacts inherit asset classes from company criteria (blast segmentation = role + asset class only). Lists sort by lastActivityAt desc (nulls last); src/lib/activity.ts logActivity() bumps contact + company on every email/note/tracker change. Jonathan said he loves the contact page look; keep it as the reference style.

**Update 2026-09-04 (night)**: Progress report = exact copy of Jonathan's Word reports (simple: Arial, centered header lines, bullets, black-bordered table, doc status colors in src/lib/tracker.ts). He wants it SIMPLE; do not re-add dark headers/chips/action items. Company enrichment from website lives in src/lib/enrich.ts (claude-sonnet-5), auto on new domain companies + Refresh button + npm run enrich; Jonathan expects company info (website, description, location) to be filled automatically from the email domain. Company page: About collapsible, criteria under it on the left (full for Investor, asset classes only for Sponsor).

**Rule (2026-09-04)**: Company ROLES and INVESTOR CRITERIA are Jonathan-only; never auto-write either without his approval. Exception he asked for: SPONSORS get asset classes (+ focus markets) filled from their website when blank, triggered every time a sponsor is emailed (outbound EMAIL activity -> src/lib/activity.ts refreshSponsorLater -> enrichCompany force). Other company facts (website, description, phone, LinkedIn, HQ, year founded) auto-fill for everyone. Progress report themes/items are generated by Claude from tracker notes (src/lib/tracker-summary.ts) on note save + button; deal stage is togglable via dropdown on the deal ticket.

**Update 2026-09-07**: To-do page = ONLY two windows (quiet LPs 2+ days w/ Respond now button; criteria updates to approve). Jonathan found the derived to-do list unhelpful; do not bring it back. Criteria proposals engine: src/lib/criteria-proposals.ts (claude-opus-5, conservative: standing rules only, no deal-specific passes), fed by tracker notes now; wire EMAIL replies + Fireflies when connected. Respond now = mailto today; upgrade to Graph reply-all with attachments after M365 (Azure app registration).

**UX (2026-09-07)**: The dashboard page is called **Home** (not To do). Jonathan wants it easy on the eyes: larger text (15px base), few black buttons; use the light-blue .btn-soft for row actions, black btn-primary only for the one main action on a page.

**Infra (2026-09-07)**: DB is now Supabase Postgres (provider postgresql; DATABASE_URL = Supabase, password percent-encoded; SQLITE_DATABASE_URL = old local file kept as backup). Prisma contains filters need mode: insensitive. Resend sending works (domain verified). Vercel account exists (Hobby, email login); vercel CLI installed as devDependency; deploy pending (needs Jonathan to run vercel login in a browser). Never print .env values; check prefix/length only.

**Deployed (2026-09-07)**: https://rjl-crm.vercel.app (Vercel team rjl-capital-advisors, project rjl-crm; deploy with npx vercel --prod --yes; env vars set via vercel env add, values piped from .env). Password gate in src/proxy.ts + /login (APP_PASSWORD in .env and Vercel). Prod DATABASE_URL = Supabase pooler (port 6543, pgbouncer=true); local = direct 5432. Next steps after Jonathan's items: Resend inbound webhook URL https://rjl-crm.vercel.app/api/inbound/resend, Microsoft sign-in, Thursday 4pm reports (Vercel cron).

**M365 (2026-09-07)**: Graph app-only auth works (AZURE_TENANT_ID/CLIENT_ID/CLIENT_SECRET in .env + Vercel; client_credentials, scope https://graph.microsoft.com/.default). Consent granted. Tenant users: Jonathan, Aviel, Shawn, Esther, Nikko (+ Chaim Levtov, Eduardo Viesca, IT). DEALS_MAILBOX=deals@rjlcapadvisors.com shared mailbox exists. Next build: Graph email sync (sent items -> Activity + lastActivityAt), deals@ inbox -> intake, Respond now via createReplyAll draft, Microsoft sign-in replacing APP_PASSWORD.

**Auth/rules (2026-09-07)**: Microsoft sign-in via /api/auth/microsoft + callback (session cookie rjl_user signed with APP_SECRET; src/lib/session.ts, current-user.ts). Per-user mailbox actions use currentUser().email. Only Jonathan (CRITERIA_ADMINS) may edit/approve investor criteria; other users see read-only. Company-wide signals (criteria proposals) should draw from ALL team mailboxes. Follow-up drafts tracked on DealInvestor.followUpDraftId; syncFollowUpDrafts flips to status 3 when sent. Sliders trial for span fields (USE_SLIDERS).

**Naming (2026-09-07)**: the investmentTypes field is labeled **Position in the capital stack** everywhere (never 'Type of investment'). Values: Senior Debt, Mezz Debt, Preferred Equity, Participating Preferred Equity, JV Equity, Co-GP Equity, Programmatic. Non-admin criteria edits -> CriteriaProposal source MANUAL (sourceRef = editor name), never direct writes.

**Email log (2026-09-07)**: mail-sync.ts syncs all active users' mailboxes via Graph into Activity(type EMAIL, meta JSON, externalId = internetMessageId). kickMailSync() on page loads + /api/cron/mail-sync (CRON_SECRET, daily). Company page center = EmailLog feed (no note form). Roles live in the criteria card (CriteriaForm roles prop); About card has no roles. Nikko's mailbox synced 2240 msgs but logged 0 - investigate matching. Ranges: src/lib/ranges.ts (checkMinMM/checkMaxMM, holdMinYears/Max, vintageMin/Max; buckets derived).

**Handle button (2026-09-07)**: Home follow-up button is named **Handle** (was Respond now). Opens the Graph draft in classic desktop Outlook via outlook:<entryId> (translateExchangeIds, sourceIdType restImmutableEntryId), web link as fallback. Creating the draft must not bump DealInvestor.updatedAt (row would vanish from the 2-day list); row leaves only when the draft is sent. Faded buttons = contact has no email (placeholder '(firm)' contacts from the Word-report import).

**Desktop Outlook bridge (2026-09-07)**: outlook: protocol is NOT registered on Jonathan's PC and browsers only launch apps on a direct click. Solution: two-step Handle (prepare -> 'Open in Outlook' link) whose href is rjlcrm:open?mid=<Message-ID>; scripts/setup-outlook-link.ps1 registers rjlcrm: (HKCU) to scripts/outlook-open.ps1 which uses Outlook COM (Drafts.Items.Find on PR_INTERNET_MESSAGE_ID, then .Display()). Jonathan must run setup once per PC (I do not modify system settings myself). Jonathan's desktop signature (text-only 'RJL Cap Sig', 9pt, colors #53565A/#717073/#0E2841) is stored in User.signatureHtml; never use the phone one from sent mail.

**Deals mailbox (2026-09-07)**: src/lib/deals-inbox.ts processes deals@ inbox (Graph), attachments via src/lib/attachments.ts (pdf-parse, xlsx, html-to-text), DealIntake.messageId dedupes. Reply sent from deals@ via createReply+send (graph() now tolerates empty 202/204 bodies). Graph subscription -> /api/graph/notify (clientState = CRON_SECRET), renewed in /api/cron/mail-sync. Forwarder is internal so sponsor sender is parsed from the forwarded 'From:' line.

**Home = 3 windows (2026-09-07)**: LP follow-ups (tied to progress reports only), Deal momentum (src/lib/momentum.ts, Momentum model, kinds SPONSOR_ITEMS/INTRO/ACTION/MENTIONED, refreshed in kickMailSync + cron), Criteria updates (Jonathan). Generic DraftButton (src/app/draft-button.tsx) replaces respond-now. AutoSaveForm (src/components/autosave-form.tsx) used on company page forms; Jonathan wants edits to save automatically everywhere on company items. RoleCell inline on Companies list. Graph subscription id created 2026-09-07 for deals@ inbox -> /api/graph/notify.

**Home floor (2026-09-07)**: HOME_SINCE = 2026-08-31 in src/app/page.tsx; LP follow-ups, Deal momentum, and criteria proposals that began before that date are hidden so the dashboard is not flooded. Jonathan wants Home light and easy on the eyes; the backlog lives on report/deal pages.

**Ticket rules (2026-09-07)**: applyDealRules() in src/lib/intake.ts (majority equity => JV Equity; dev => no occupancy/yearBuilt/cap rates); extractor enums snap unitMix/holdPeriod (DEAL_HOLD_PERIODS)/loanTerm/amortization to taxonomy lists, never free text; dev deals store leverage as ltc not ltv; checklist core fields expectedClose + loanTerm; deals@ forwarder = deal owner. Deal form fades dev-irrelevant rows via isDev state.

**Engagement letter (2026-09-07)**: /investors?dealId=..&mode=engagement (checkboxes + Done via DraftButton) -> createEngagementLetter action -> src/lib/engagement.ts createEngagementDraft (Graph new draft to sponsor recipient = intake sender else most-emailed contact; groups -> DealInvestor status 1; deal.details.engagementGroups). Template text is Jonathan's verbatim; 2% fee, 30-day carveout.

**Investor search check size (2026-09-07)**: single exact slider (Spec.checkMM in $MM, 1..100); a firm matches when checkMin <= checkMM <= checkMax (checkMax 100 = open top). Preset from deal.requestedAmount rounded to $MM. Engagement mode uses the same search, so it inherits this. Column shows the firm's range label.

**AI suggestions (2026-09-07)**: src/lib/suggest-investors.ts -> deal.details.aiSuggestions cache (24h). Jonathan wants the CRM to 'learn the business' from all firm data (emails, calls, tracker notes) and offer opinions alongside his manual picks; no redundancy (shared picked set). Extend with Fireflies transcripts when FIREFLIES_API_KEY arrives.

**Stages (2026-09-07)**: DEAL_STAGES now includes 'Engagement Letter Sent' after Deal Underwritten. syncEngagementDrafts() (src/lib/engagement.ts) watches deal.details.engagementDraftId/engagementMailbox and advances the stage when the draft is sent; runs from kickMailSync and alongside syncFollowUpDrafts. Automatic stage moves are wanted throughout (Jonathan's mission item).

**Send deal (2026-09-07)**: src/lib/send-deal.ts (finalizeEngagement, createSendDrafts, syncSendDrafts, STAGE_ORDER incl. Engagement Letter Signed), /deals/[id]/send page + send-client.tsx, engagement-card.tsx on deal page. Email blasts = marketing only, never part of the deal process. Send deal = per-firm Outlook drafts from EmailTemplate (house style default), signature appended, template sign-off stripped, attachments copied from the deals@ intake message. Dashboard is the page name now (was Home, was To do).

**Dashboard/report (2026-09-07)**: 4 windows in 2x2 (h-[calc(50vh-70px)]); Deals ready for launch = stage Engagement Letter Signed. All record forms auto-save (AutoSaveForm) incl. DealForm autosave prop. Report in-app: headerEditor slot (two textareas + Prepared For), status filter ?status=N, showPeople false, labels bold values plain. Jonathan wants windows squarish, not tall.

**Intros (2026-09-07)**: src/lib/intros.ts (Intro model; scanIntros via Graph filter startswith(subject,'Intro') on each mailbox's Sent Items, thread activity via conversationId; quiet threshold 14 days). Dashboard = one row of 5 compact Window components (h-[calc(100vh-140px)]). Graph note: combining startswith filter with date filter/orderby errors; filter dates client-side.

**Send page v2 (2026-09-07)**: tokens + contentEditable per-firm email (drafts state: subject/html/touched) + LAUNCH (launchDealEmails: createDraft -> copy attachments -> /send per firm, status 2 now) + preview-to-self ([PREVIEW] prefix). Jonathan wants: individual emails, edit each before launch, launch all at once, preview to himself. Old createSendDrafts flow retained but not used by the UI.

**Deal email style (2026-09-07)**: subject + 'They are seeking' use usdShort ($5.6MM); seeking sentence bold; 'Deal Metrics' bold+underlined; bullet lead-ins bold (split at first ': '); 12pt gap after metrics before Business Plan. Bash pitfall: never write TS with literal $ via bash heredoc/node -e; use the Edit/Write tools.

**Send page v3 (2026-09-07)**: dealFiles(dealId) = attachments on the deals@ intake thread from the sponsor (dedupe by name, models/PDF first; images skipped), selectable, all on by default. usualRecipients(companyId) = people on >=40%% of our outbound emails to the firm in 12 months (email log meta.to/cc) -> default To. Contact.unsubscribed only matters for blasts; one-to-one deal emails include everyone with an email (Dwight Stephenson at Blue Vista was hidden by that flag).

**Git remote (2026-09-07)**: origin = https://github.com/RJLCapitalAdvisors/RJL-CRM.git (private). Pushing is blocked for me by the permission classifier; Jonathan runs `git push` himself (give him the command in a bash block). Repo carries docs/PROJECT-NOTES.md (copy of these notes; keep both updated) and docs/SETUP-LAPTOP.md. .env is not in git; he keeps it in his password manager.

**Handle fallbacks (2026-09-07 night)**: any Handle (LP, momentum, intro) must never dead-end on 'not in your mailbox': order = exact thread in my mailbox -> deal follow-up row -> replyToLatestWith(me, person) (latest Sent Items thread with them, prefer deal words) -> fresh email. Bridge outlook-open.ps1 tries GetItemFromID(eid) first (+SendAndReceive), then Message-ID, then Drafts folder; the rjlcrm: URL carries mid and eid. Graph-created drafts can lag syncing into classic Outlook.

**Mentions + intros (2026-09-08)**: src/lib/mentions.ts detectMentionedDeals() reads recent sponsor threads (full bodies via Graph) and creates stage 'Deal Mentioned' deals (MentionScan table dedupes); runs at the start of refreshMomentum. Intros: zero replies -> flagged after 3 days (UNANSWERED_INTRO_DAYS); otherwise quiet 14 days. Handle order now: my mailbox -> teammate/deals@ copy of the exact thread (replyViaTeammateCopy builds reply-all in my mailbox) -> deal follow-up row -> latest thread with the person -> fresh email.

**LP asks + forwarder notes (2026-09-08)**: src/lib/lp-asks.ts reads inbound LP emails on active deals (LpAskScan dedupe), writes tracker note/status, and momentum kind LP_ASK (party = LP name) sorts first; its Handle drafts a fresh email to the sponsor's usual recipients listing the asks. src/lib/forwarder-notes.ts: text a teammate types above a forwarded deal to deals@ is parsed (stage, investors already introduced -> Intro Made rows). Mentioned-deal tickets only nag in momentum when the sponsor promised to send (details.promised); all still appear in the Deal Mentioned column. Bridge: wscript launcher (outlook-open.vbs) avoids the black console; helper logs to %%LOCALAPPDATA%%\RJL CRM\last.log. Jonathan must re-run the Desktop 'Connect RJL CRM to Outlook' to switch the handler to wscript.

**Bridge v3 (2026-09-08)**: Graph-created drafts do NOT sync into Jonathan's classic Outlook (log proved GetItemFromID + Message-ID both fail). New default: rjlcrm:reply?mid=<ORIGINAL sent email Message-ID>&greet=&att=1 -> outlook-open.ps1 finds the original locally (Sent/Inbox/Drafts, then AdvancedSearch), calls ReplyAll(), Display() (native signature), prepends greeting, re-attaches files; falls back to opening the server draft. FollowUpResult.replyTo carries {messageId, greeting, attachments}. syncFollowUpDrafts also treats any outbound email to the LP after the click as the follow-up sent (and deletes the unused server draft). Companies list: Roles, then Asset classes (inline AssetCell; sponsors direct, investors via proposal unless Jonathan), Location last. Report bullets need listStyle disc (Tailwind preflight removes bullets).

**Sponsor linking (2026-09-08)**: sponsorContactsFor(dealId) in src/lib/engagement.ts = usual recipients at the sponsor company -> else external people on the deal's own email threads who are not LPs on the report (and links the company to the deal) -> else the intake sender. Used by LP_ASK Handle and engagement letters. Lesson: when importing/creating sponsor companies, check for an existing company by domain/name first (BrightStar duplicate came from my Word-report load).

**Ticket clearing (2026-09-08)**: Dashboard load runs syncRecentSent(me) (Sent Items last 6h) before judging LP follow-ups/intros, so a reply sent from Outlook clears the ticket on the next load; kickMailSync throttle 3 min. Intro.handledAt set on Handle; quietIntros hides handled intros once an outbound email to a recipient exists after handledAt (30-min grace). Jonathan expects tickets to disappear by themselves after he sends the email.

**Deal knowledge (2026-09-08)**: models DealEmail (intake + follow-ups, conversationId), DealFile (ticket Attachments; download /api/deals/[id]/files/[fileId]), DealFact (Questions answered). src/lib/deal-knowledge.ts: matchExistingDeal (thread id, then deal-name words in subject/body + sponsor domain, score>=2) -> deals@ follow-ups attach files, extract Q&A facts, merge blanks into ticket, reply 'Added to <deal>…'. dealFiles() reads DealFile first. LP asks annotate 'Already on the ticket: …' from factsFor(). Jonathan's principle: a deal ticket keeps learning; never create a second ticket for the same deal.

**FAQ + files + multi-deal (2026-09-08)**: src/lib/faq-pdf.ts builds '<Deal> - Investor FAQ.pdf' (pdf-lib) from DealFact; appears as virtual file key 'faq' in dealFiles() (ticked by default on Send) and downloadable at /api/deals/[id]/faq.pdf. File links carry signed tokens (signFileToken) so drag-out (DownloadURL) works without cookies; proxy lets /api/deals/*/files and faq.pdf through. AttachmentList component: type icons + draggable rows. Momentum kind ENGAGEMENT: letter sent 18h+ with no inbound from sponsor. deals@: detectMultipleDeals() splits an email into one ticket per deal (files assigned per deal; DealIntake/DealEmail messageId suffixed #2, #3).

**Rule (2026-09-08)**: NO duplicate deals on the board. findSameDeal(name) in deal-knowledge.ts (normalized name match among active deals) guards createDealFromIntake and detectMentionedDeals; deals@ follow-ups already reuse tickets. LP_ASK Handle replies all on the latest sponsor thread about the deal (asks on top). Legacy HubSpot per-intro tickets ('Wright | Marble', 'Core | Marble', …) still exist; consolidation is Jonathan's call.

## 2026-09-08 (evening): matching, email windows, General token, cloud links, letterhead
- deals@ follow-up matching (src/lib/deal-knowledge.ts matchExistingDeal): the thread (conversationId) still wins; otherwise most of the deal's name must appear AND Claude confirms it is the same deal (confirmSameDeal). Generic words (fund, equity, anchored, value...) never match. Two forwards had been misfiled on mention tickets (MAG Capital, Two Rivers) and were re-run; Southbay Pavilion (Steerpoint) is its own ticket.
- Shared subject matcher (src/lib/deal-match.ts): dealPhrases = property name + ticket-title segments ("Kansas City, KS"); subjectMatchesDeal (strict) and subjectLooselyMatchesDeal (city or one property word, only for people on the deal's report or at the sponsor). Used by mail sync (inbox + sent items, dealResolver) and by the deal page.
- Deal ticket middle window = EmailLog fed by src/lib/deal-emails.ts (dealEmailRows): activities tagged with the deal + subject matches (tagged back onto the activity) + deals@ DealEmail rows. Same component as the company page.
- LP asks (src/lib/lp-asks.ts): the email must name one of the firm's deals (subject) or Claude picks among them (dealIndex, -1 = other deal / intro / catch-up -> skipped). No more "most recent deal we sent them" fallback. LP_ASK Handle replies only on sponsor-only threads (no intros, no third parties), else a fresh email.
- Send deal page: "General" token (extra wide, first). One email with no name in the greeting (invisible <span data-first-name> placeholder, src/lib/first-name-marker.ts); every firm's email = General with its person's first name; per-firm edits still possible. "Ask the CRM to change this email" box -> reviseDealEmail (claude-opus-5) rewrites the General email. Preview to self from General has a blank greeting ("Hi - hope you are well.").
- deals@ opens Dropbox / Google Drive / OneDrive-SharePoint / Box links (src/lib/cloud-links.ts): files (and folders: Dropbox zip, Drive folder via GOOGLE_API_KEY or best-effort page scrape, OneDrive via Graph shares API) are parsed like attachments and recorded as DealFile rows with url (mailbox "link"); downloads and investor-email attachments re-fetch from the link. Links that need sign-in are listed in the deals@ reply. Optional env: GOOGLE_API_KEY (Drive API key) for Drive folders.
- Investor FAQ PDF now carries the RJL letterhead (logo + address block + navy rule) on every page.
- Handle on intros: handledAt is stamped only when a draft was actually prepared, and the intro stays on the dashboard (marked "handled HH:MM, drops off when your reply is sent") until the reply shows in Sent Items. No more 30-minute disappearance.
- Outlook bridge is installable by anyone from the live site: Settings -> Outlook on this computer (download `Connect RJL CRM to Outlook.bat`, which pulls public/outlook-bridge/setup.ps1 + helpers; "Test the Outlook link" = rjlcrm:test pops a confirmation). scripts/ stays the source; `npm run build` copies them to public/outlook-bridge (scripts/copy-bridge.js). Log for self-debugging: %LOCALAPPDATA%\RJL CRM\last.log.
- Onboarding checklist lives in src/lib/onboarding.ts (admin steps + new-person steps). Settings renders it with tick boxes (admin list only for Jonathan); `npm run build` writes docs/ONBOARDING.md from it. Rule: any feature that adds a per-person setup step (connector, permission, habit) adds a step there.
- PDF reading was broken until 2026-09-08 evening: pdf-parse's package index runs a debug routine under Next's loader; src/lib/attachments.ts now imports pdf-parse/lib/pdf-parse.js directly. Every deals@ PDF before that was skipped (Excel was read).
- Subject lines are not deal identifiers: the deals@ matcher and the extractor treat attachments and body as authoritative (Whiting St came in under "Harvest Portfolio"). Same-thread follow-ups with documents attached are confirmed by Claude before joining the old ticket.
- Files pulled from Drive / Dropbox / OneDrive links are stashed on a message in the deals@ "CRM Files" folder (src/lib/file-store.ts) and recorded like attachments; folder zips stream to disk and are read with yauzl (Indicap data room was 215 MB). Send page defaults to the first six files, not a whole data room.
- Manual sends count: an outbound email that the sync ties to a deal and that goes to an Investor/Lender contact puts the firm on the progress report as Deal Sent (one row per firm; colleagues join the row), moves the deal to Deal Taken To Market, and records the OM/model as files when the ticket has none (src/lib/deal-outbound.ts, called from mail-sync). House-style subjects match on "opportunity" + the deal's city + the ask amount (src/lib/deal-match.ts houseSubjectMatches). Word matches are whole-word only.
- Investor FAQ scope: facts that any investor could be told (property, market, plan, numbers, structure, sponsor). Nothing private to one conversation: other/prior offerings, what a particular investor said, one-party terms. A fact can be removed from the ticket (hover -> remove), which removes it from the FAQ.
- Send deal page autosaves its state (General email, per-firm edits, recipients, files, template) to deal.details.sendState a second after each change; people picker closes on any outside click; the ask-the-CRM box sits to the left of the General email.
- Graph attachment uploads (src/lib/graph.ts addAttachment): chunks in 320 KiB multiples with Content-Type: application/octet-stream, else 400. One message holds about 20 MB of attachments (Exchange limit), so stashFiles spreads a data room over several "CRM Files" messages.
- IndiVAC data room (2026-09-08): four tickets, IndiCap | Insight Bay / Avondale Bay / Virginia Bay / Denmark Bay, 40 documents kept in deals@ CRM Files. The "00 - DATA ROOM INDEX" copies shared by several tickets stay link-based (one Graph attachment can back only one DealFile row).
- Business plan / summary paragraph rule (Jonathan, 2026-09-08): lead with location and market context, then anchor/key tenants, value-add opportunity, notable physical attributes; 4-6 sentences, flowing prose, no dashes as punctuation; nothing that has its own field (exit, returns, dollar costs, metrics, seller profile, lender type, close timeline, year built, SF, units). Encoded in src/lib/intake.ts.
- Progress report: "Refresh responses" button (src/lib/refresh-responses.ts) re-syncs every mailbox, ties emails with the report's firms to the deal, clears earlier LP-ask verdicts on this deal's replies and re-reads them. Report carries a "Progress Report" title line above the deal name.
- Send deal page: Cc line under the email header (saved with the page, applied to every firm's email); teammates offered as one-click chips.
- Contact names: blank first/last names fill from the display name on emails (mail sync, and a one-off backfill on 2026-09-08 for 210 contacts). 8,000+ HubSpot-era contacts still have only an email; they fill as mail arrives.
- "Criteria updates" is now "Data updates". Besides criteria corrections it carries "Remove contact" items: an auto-reply or bounce that says someone left their firm (src/lib/departures.ts, run from the mail sync and from scanForDepartures) queues a removal for Jonathan; approving sets Contact.departedAt (and unsubscribed), which hides the person from pickers and stops emails. The colleague named as the new contact is never proposed for removal.
- Progress report PDF (src/lib/progress-report-pdf.ts, /api/deals/[id]/progress-report.pdf with a signed token): same layout as the Word reports; on the tracker page as a draggable file chip (drag into Outlook) plus Download PDF, left of Refresh responses.
- Report notes: re-reads never append the same note twice (mergeNote / dedupeNote in src/lib/tracker.ts). Follow-up syncs only raise a row to Followed Up, never lower a decision (Pass / Interested) back.
- Departure notices also add the replacement the auto-reply names ("please contact Mike McFadden ...") as a contact under the same company (addReplacementContacts); the Data updates item says who was added.
- Checklist document items are structural (src/lib/checklist.ts reconcileDocuments): the Excel underwriting model counts as received only when an .xls/.xlsx/.xlsm file is on the ticket; any document answered with "not provided / only in the PDF" wording is blank so deals@ asks for it; a matching file marks the item "Received - <file>". Runs at intake and after every follow-up merge.
- Engagement letter / investor search preset (Jonathan, 2026-09-08): Year built is never preselected (he adds it when relevant); the strategy filter always ticks the deal's strategy AND "Both", since some LPs are listed only as Both.
- Cloud links also cover Egnyte shared folders/files (spspartners.egnyte.com/fl/...): src/lib/cloud-links.ts egnyte() uses the link page's own public API (/rest/public/1.0/links/info/<id> and .../contents, downloads via /dd/<id>/?entryId=...). Password-protected links are reported back. Woodburn Exchange (SPS) was rebuilt from its five Excel files on 2026-09-08.
- Ask the CRM (/ask, top of the sidebar): Claude (claude-opus-5) with read-only lookups over the database (src/lib/ask-crm.ts: search_deals, get_deal, get_progress_report, search_companies, get_company, search_contacts, email_log, find_investors_for, dashboard, pipeline_summary). Answers link to the pages they drew on; it never writes. Up to 8 lookup rounds per question; chat history lives in the browser tab only.
- Aviel's data rules (2026-09-08), encoded in src/lib/intake.ts and src/lib/style.ts: pad sale / outparcel rule for stabilized yield on cost (NOI excluding pad income over total cap minus pad sale net proceeds); enum-like fields (seller profile, lender type, sourcing, closing time frame) only when the documents state them, never the closest guess; no placeholders (TBD, N/A, unknown become blank + a confidence note); sponsor bio 3-4 sentences (founding, focus, scale/track record, no return figures); no em dashes, en dashes or double hyphens anywhere in generated text (stripDashes applied to extractor output, facts, tracker themes/items, LP-ask notes, Ask the CRM answers).
- Progress report notes rules (Aviel): the note carries the actual reason an investor passed or hesitated, only what they said, cleaned up; no generic status language; pleasantries stripped; blank when there was no substantive response or only an acknowledgement (src/lib/lp-asks.ts).
- Fireflies (FIREFLIES_API_KEY set): src/lib/fireflies.ts finds recorded calls matching the sponsor, property or sponsor contacts and folds them into the business plan, sponsor bio and Questions answered when a ticket is created (enrichFromCalls, called from deals-inbox).
- Browser tab titles: root layout title template "%s · RJL CRM"; every page exports a title (deal name, company, contact, "<deal> Progress Report", "Send <deal>", Dashboard, Ask the CRM, ...).
- Yield on cost is always captured: the extractor takes stabilized YOC under any label (yield/return on cost, cap rate on all-in/total cost, stabilized yield), else the going-in cap rate on all-in cost, else computes it from stabilized NOI and total capitalization (noted in confidenceNotes); it is a checklist item for acquisitions and development so deals@ asks when it is missing; Deal Metrics falls back to a computed Going-In Yield on Cost (T12 NOI over total cap) when no figure is stored.
- Sponsor replies in any team mailbox (src/lib/sponsor-replies.ts, run on Dashboard refresh): inbound email from a sponsor-side contact on an active deal is read once (DealEmail kind SPONSOR_REPLY): answers become Questions answered and fill ticket blanks; after an engagement letter went out, a confirmation ("we are good with the agreement", with groups to leave out) finalizes the engagement (Engagement Letter Signed, groups trimmed) and logs a note.
- Engagement letters sent from desktop Outlook leave the Graph draft as a draft: syncEngagementDrafts now also looks in Sent Items for the letter subject after the draft date.
- LP-ask scan skips the deal's sponsor (their email is not an LP response) and feeds every substantive LP reply to proposeCriteriaChanges (Data updates).
- deals@: an email with several deals is split before the follow-up decision; each part joins its own existing ticket (findSameDeal) or opens one; a forward landing on a Deal Mentioned ticket moves it to Deal Received with the full summary reply. assembleDealText gives every attachment an equal share of the text budget (180k chars).
- Questions answered / Investor FAQ never mention other investors: nothing about which LPs RJL is approaching, which groups the sponsor knows, or the engagement list (prompt rule plus a name filter against the deal's report and engagement groups in extractDealFacts). Groups in an engagement discussion are equity investors, never lenders.
- Sponsor inline answers ("see below in blue") are read from inside the quoted email, so a close date or loan terms answered next to the question land on the ticket.
- Development vs acquisitions: Development only for ground-up / new construction; an existing operating building (year built in the past, occupancy, in-place rents, T12) is Acquisitions even with a heavy renovation budget. Prompt rule plus a post-check in fromClaude (src/lib/intake.ts).
- Ask the CRM conversations are kept per person (ChatThread / ChatMessage): left column lists them, ?t=<thread> in the URL points at the open one, New chat starts another, delete on hover.
- Investor FAQ rule (Jonathan, 2026-09-09): the FAQ only carries questions somebody actually asked and the sponsor answered (RJL's checklist / follow-up asks, an LP question relayed to the sponsor, a question on a call). Information mined from the OM, model or deck stays on the ticket as knowledge (DealFact.inFaq=false) and never reaches the FAQ. The extractor marks `asked`; only reply paths may set inFaq; the ticket has a to FAQ / off FAQ toggle per item. No FAQ file when nothing is flagged.
- Excel first (Jonathan, 2026-09-09): when a model is attached it is the source of truth for every number; the OM / deck supplies narrative and physical description; conflicts are noted in confidenceNotes. Models are read first and get the largest share of the text budget (assembleDealText); a model arriving on a follow-up overwrites PDF-derived numbers (mergeIntoDeal modelAttached). The Excel model is a checklist item and counts as received only when an Excel file is actually attached.
- Deliverability (2026-09-09): outgoing subjects are stripped of zero-width characters (HubSpot-era templates left U+200B in 56 of 391 September subjects); LAUNCH paces sends 1.5 to 2.2 s apart (send page maxDuration 300). DNS as found: SPF = outlook + hubspotemail (no Resend/amazonses include), DKIM selector1/2 (Microsoft) + resend._domainkey present, DMARC p=quarantine with EasyDMARC reporting. Recommended to Jonathan: add include:amazonses.com to SPF while Resend sends from the domain, drop the HubSpot include once HubSpot is retired.
- Email blasts (2026-09-09, src/lib/blasts.ts): segments (Investors / Sponsors / Everyone, optional asset classes from company criteria; valid, non-internal, non-role-address emails only; no unsubscribed, departed or bounced), blast templates (kind BLAST: "Deal Spotlight (blast)", "Capital Spotlight (blast)", seeded by scripts/seed-blast-templates.ts in the HubSpot newsletter style), schedule (Campaign.scheduledAt, status SCHEDULED) or draft, follow-ups (Campaign.followUpDays "3,7": the same email again as "Re:" to anyone with no inbound reply since the last send, stops on reply / unsubscribe / bounce). Sends go from MAIL_FROM (Jonathan) through Resend's batch endpoint, 100 at a time, with List-Unsubscribe headers. Due blasts and follow-ups run from page loads (kickBlasts on Dashboard and Blasts pages), from /api/cron/blasts (daily 13:00 UTC in vercel.json; Hobby plan allows daily only), or by pinging /api/cron/blasts?key=CRON_SECRET. Resend webhook /api/inbound/resend now also records email.opened / clicked / bounced / complained on recipients (events must be enabled on the Resend webhook; open tracking on the domain).
- Square footage is always net rentable (NRSF / rentable area / GLA for retail), never gross building area, land or site area (Jonathan, 2026-09-09). Encoded in the extractor (src/lib/intake.ts).

## 2026-09-09: RJL Israel workspace

- Second business inside the same CRM, chosen from the one sidebar (`src/components/workspace-sidebar.tsx`): both logos sit at the top of the left rail; the business you are in is open with its pages listed under its logo, the other logo is the button across. Sign-in lands on RJL CA by default.
- Look: the RJL Capital Advisors scheme with royal blue (#2f4fa3, Jonathan's swatch) wherever RJL CA uses its light blue, tints included, and royal blue in the logo mark. `.israel` in `src/app/globals.css` overrides the sky/cream/line tokens; the root layout puts the class on the body for `/israel/*` (header `x-pathname` from `src/proxy.ts`, kept in sync client-side by the sidebar). Jonathan tried black first and asked for it gone.
- Logo: `public/israel-logo.svg`, redrawn from the logo Jonathan pasted (light blue snowflake glyph, white RJL ISRAEL on a dark ground). His original file can replace it at that path (or swap the sidebar to a PNG). Both business logos sit side by side at the top of the rail at all times; the active one is ringed.
- Pages: Dashboard (blank except a Data updates window, rules to come from Jonathan), Apartments (a simple list in the same window as the RJL CA company list), Companies and Contacts (same windows as RJL CA), Deals (the pipeline funnel: a drag-and-drop board like the RJL CA deals board; a deal is a buyer pursuing an apartment, with agent, offer, agreed price, expected close; stages Lead, Viewing Scheduled, Offer Made, Negotiation, Under Contract, Closed, Lost; model `IlDeal`). No apartment search page: the list has the filters.
- Apartment ticket also carries seller type (Yad Rishona from the developer, second hand never occupied, second hand occupied; second hand opens a year of renovation) and shows dollars per square foot instead of dollars per metre (asking price in dollars over square feet on the same area basis as price per metre).
- Apartment ticket mirrors a deal ticket: fields on the left (`src/app/israel/apartments/apartment-form.tsx`, autosave), the floorplan as the big middle window (image or PDF stored on the row, `/api/israel/apartments/[id]/floorplan`), developer / sales agent / seller as associations on the right (linked with dropdowns). Fields: name, building address, city, neighborhood, rooms, year of construction or expected delivery (month/year), apartment floor, building stories, total building units, apartment direction, mirpeset direction, mamad (yes/no), internal m² (+ sq ft), mirpeset m² (+ sq ft), ceiling cm (+ feet), parking spots (None, 1, 2 back to back, 2 side by side, 3), machsan m² (+ sq ft) and location (attached / basement), asking price ₪ (+ $ at the live ECB rate via frankfurter.app, hourly), price per meter = asking price ÷ (internal m² + ⅓ mirpeset m²) in ₪ and $.
- Contacts are Buyer, Seller or Sales agent (the roles are the first thing on the contact form); sales agents are usually the people at agencies and developers. Dummy data: `scripts/seed-israel.ts` (4 companies, 5 contacts, 4 apartments), safe to rerun.
- Prisma models `IlApartment`, `IlCompany`, `IlContact`, `IlNote`; taxonomy and formatters in `src/lib/israel.ts`; server actions in `src/app/israel/actions.ts`. No email/Graph automation for Israel yet.

## 2026-09-09: Email blast editor

- New blast page is laid out like HubSpot: the email is the main window (`src/components/blast-editor.tsx`), shown exactly as it will render in an iframe, with Send to / From / Subject line / Preview text above it. Three modes: edit in place (contenteditable, merge fields visible), as the reader sees it (merged for the signed-in person via `previewCopyAction`), HTML. "Send a test to me" works before the blast exists (`sendTestCopyAction`). The edited copy is saved onto the campaign at creation (`createBlast` takes `copy`). The same editor sits on the blast page with Save copy (`saveBlastCopyAction`).
- LP follow-ups rows can be dismissed (one LP with the x, or Dismiss all on the deal): `DealInvestor.followUpDismissedAt`; the row stays on the progress report at its status. A follow-up actually sent from Outlook clears the dismissal so the clock runs again.
- LP follow-ups on the Dashboard count calendar days in New York (QUIET_AFTER_DAYS = 2): a deal sent Monday afternoon shows its quiet LPs Wednesday morning, not 48 hours later. Rows are judged on their last update, so anything that touches a row (a note, a status change) restarts its clock.
- Send to one person (Send deal page header): after launch, an LP on a call asks for the deal. Search a contact, click Draft in Outlook: `draftDealToOne` in `src/lib/send-deal.ts` drafts the General email from Send deal (with that person's first name) or the house template, attaches the files chosen on Send deal (else the usual first six), into the signed-in user's mailbox; the person joins the progress report as Deal Not Sent and flips to Deal Sent when the draft leaves Drafts (existing `syncSendDrafts`). Opens through the same rjlcrm: bridge as Handle.
- Dropdowns in autosaving forms: React resets a form after its action runs, and an uncontrolled `<select defaultValue>` snaps back to the option selected at first render even though the save went through (the strategy, OZ, closing time frame and minority dropdowns on the company page did this). Every native select in a form that runs a server action goes through `SelectField` (`src/components/select-field.tsx`) or the stateful `Select` in `form-rows.tsx` / `deal-form.tsx`, which keep the pick in state so React restores it.
- RJL Israel Projects (`IlProject`, nav right under Dashboard): the whole building or development (developer, address, city, neighborhood, total units, total parking spaces, total stories, built/delivery); apartments link to a project (`IlApartment.projectId`) and list on the project ticket. Royal blue also colors primary buttons and the active sidebar item in RJL Israel.

## 2026-09-09 (late): RJL Israel Ask the CRM and deals@rjlisrael intake

- Ask the CRM exists in RJL Israel too (`/israel/ask`, first item in its sidebar). Same chat UI (`src/app/ask/ask-client.tsx` takes `basePath` and `workspace`), separate conversations (`ChatThread.workspace` CA | IL), and its own lookups in `src/lib/ask-israel.ts` (apartments, projects, companies, contacts, deals funnel, match buyers to an apartment). `askCrm(history, name, workspace)` switches tools and system prompt.
- deals@rjlisrael intake (`src/lib/israel-intake.ts`): every email to the Israel deals mailbox (`ISRAEL_DEALS_MAILBOX`, default deals@rjlisrael.com) is read with its attachments, Claude pulls every distinct apartment into its own ticket (project and developer found or created, the sending agent filed as a Sales agent contact, an attached floorplan saved on the ticket), and the mailbox replies on the thread with a link per ticket and the fields still missing, in Jonathan's field order. Each message once (`IlInbound.messageId`). Runs from the Graph notification route and the mail-sync cron alongside deals@. Jonathan's side: create the mailbox in Microsoft 365 (same tenant as deals@ so the existing app-only Graph permission covers it; a different tenant would need its own app registration), set `ISRAEL_DEALS_MAILBOX` in .env and Vercel if the address differs.
- Send to one person registers the person on the progress report at once (Deal Not Sent) and flips them to Deal Sent when the Outlook draft goes out; the progress report page now runs that sync on open, so it is current as soon as Jonathan looks. A discarded draft leaves the row at Deal Not Sent (remove it on the report if unwanted).
- Working from another machine: `git pull`, `npm install`, copy `.env` over (never in git), `npx prisma generate`, `npm run dev`. Deploys go through `npx vercel --prod --yes` from any machine logged into Vercel.

## 2026-09-10: RJL Israel apartment filters and Deals to be approved

- Apartments list has a filter rail (`src/app/israel/apartments/filters.tsx`): search, city and neighborhood (multi, from the data), internal m², mirpeset m², rooms, floor and built-or-delivery year as two-handle sliders (the same control as check sizes), parking, seller type, direction, mamad, machsan, and a sort (price per meter both ways, price, size, year, name). Everything except asking price, as Jonathan asked. Filtering and sorting run in memory over the non-pending apartments; the URL carries the filters.
- Deals to be approved (RJL Israel dashboard): an apartment that arrives by email (deals@rjlisrael) is created with `pendingApproval: true`. It is not in the Apartments list, Ask the CRM, or the deal dropdowns until approved. The dashboard window lists each with what is still missing; Approve appears only when nothing is missing (`approveApartment` refuses otherwise). The ticket itself shows the same banner. The rule for "complete" is the list `IL_COMPLETE_FIELDS` in `src/lib/israel.ts` (currently every ticket field except description and rooms); Jonathan said he will name the exact items, edit that list when he does.

## 2026-09-10: culling quiet deals (Data updates)

- A deal between Deal Mentioned and Intro To Capital Made with nothing for 3 days (no email, note, report change or edit; `src/lib/stale-deals.ts`, `STALE_DAYS`) appears at the top of the Data updates window as "Deals gone quiet", most recently quiet first. Jonathan only. "Mark Deal Lost" moves it to Deal Lost (with a note and a lost reason), which takes it off Deal momentum and LP follow-ups. "Keep" (`Deal.staleCheckedAt`) hides it for another 3 quiet days. Any activity on the deal resets its clock. On day one this listed 78 deals, mostly old Deal Mentioned and Intro To Capital Made tickets.

## 2026-09-10: LAUNCH pacing (30 seconds) and the blasts menu

- LAUNCH on Send deal queues one email per firm (`DealLaunch`, `src/lib/launch-queue.ts`) and sends one every 30 seconds from the sender's mailbox (`LAUNCH_GAP_MS`), so each lands as an individually sent email rather than a burst. The first goes out on the click; the page polls every 5 seconds (`pumpLaunchAction`) and shows sent / queued / failed per firm with "next in Ns". Page loads elsewhere and the cron also pump (`pumpAllLaunches` from `kickMailSync`), so a closed tab does not strand a launch, though it goes faster with the page open. A row is claimed before it is sent, so two pumps never double-send. Rows flip to Deal Sent as each email goes; the deal advances to Deal Taken To Market on the first.
- Email blasts in the sidebar opens straight onto New email blast; the history is behind "Past blasts" (`/campaigns?list=1`).

## 2026-09-10: intro timing

- Intros to reconsider: quiet threshold 30 days (QUIET_INTRO_DAYS), first nudge after 3 days with no reply (UNANSWERED_INTRO_DAYS), and an intro stays in rotation for 12 months from its last touch (INTRO_WINDOW_DAYS = 365 on lastActivityAt, no longer 120 days from the intro date). Independent of the deal board: Deal Lost does not affect intros.

## 2026-09-10: re-underwrite from a later Excel model

- deals@: when a follow-up email on an existing deal carries an Excel model (typically the OM came first), `mergeIntoDeal` (src/lib/deal-knowledge.ts) re-runs the extractor with the model as source of truth and sets every underwriting field on the ticket to what the model says (price, capitalization, debt, equity ask, IRR, multiple, yield on cost, cap rates, cash on cash, units, NRSF, occupancy, interest rate, loan term, hold, expected close, unit mix, year built). Narrative only fills blanks. Each change is written as a note on the deal ("Re-underwritten from <file>: Purchase price $X to $Y; ...") and the deals@ reply lists them under "Re-underwritten from <file>". Rule from Jonathan: Excel files are usually more up to date than PDFs.
- Gap: a sponsor replying with the model directly to a team inbox (not deals@) is read for its text only; attachments there are not downloaded yet.

## 2026-09-10: duplicates

- One deal, one ticket, enforced at every creation point through `findSameDeal` in `src/lib/deal-dedupe.ts`: same normalized name, same street address (or a ticket named after the other's address, the engagement-letter case that made the RIG and Pearl duplicates), or same sponsor in the same city confirmed by Claude from the text at hand. Mentions, deals@ intake and multi-deal splits all pass sponsor, city, address and text. The old normName had lost its regex escapes; fixed.
- Possible duplicates (Data updates, Jonathan only): pairs of active tickets by same name, same address, similar names (more than half the meaningful words, at least two) or same sponsor and city. "Merge into first" folds the lighter ticket into the heavier one (`mergeDeals`: emails, files, facts, activities, report rows with the higher status kept, actions, campaigns, launches, momentum; blanks filled; source deleted; note on the target). "Not the same deal" is remembered (`DealNotDuplicate`).
- Merged today by hand: "Royal Imperial Group | 2700 South Green Street" into Westwind Apartments; "Pearl Capital | 2801 John Hawkins Pkwy" into Galleria Trace Plaza.

## 2026-09-10: departures from blast replies

- Departure detection (`src/lib/departures.ts`) now also fires on ordinary replies whose body says someone left ("John no longer works here, reach out to Jane"), not only auto-replies and bounces. mail-sync checks every inbound preview against HUMAN_DEPARTURE and fetches the full body (reply part only, quoted original cut) when it hits. The departed person is the one named in full; failing that, a first or last name near the departure phrase among contacts at the sender's firm we emailed in the last 60 days; failing that, the only such contact; "I am no longer with" marks the sender. Replacements: the verb pattern ("reach out to Jane Doe jane@...") plus any "Name <email>" or "Name (email)" at the departed person's domain; they are added under the firm. Everything still lands as a Remove-this-contact item in Data updates for Jonathan's approval, never automatic.
- A hard bounce on a blast (Resend email.bounced) queues the same removal proposal, on top of setting bounceReason.

## 2026-09-10: launch stall, sponsor off the LP list, sponsor picker

- Galleria launch stalled after the first email: the queue (src/lib/launch-queue.ts) is paced by the Send deal page pumping every 5s, and a reload forgot the launch was running. Now: the Send page resumes pumping whenever it opens with emails still queued (initialLaunch), and launchAction, pumpLaunchAction and the Send page itself start a background pump with `after()` for up to 270s, so a launch finishes even if the tab is closed. mail-sync's page-load kick still pumps as a backstop.
- The sponsor is never an LP: src/lib/report-guard.ts (`isSponsorSide`, `sponsorSideIds`, `removeSponsorRows`) checks the deal's sponsor company and email domain; every path that creates a progress-report row (LP asks, reply matching, campaigns, tracker add, engagement, launch, send to one, forwarder notes) skips sponsor-side people. Pearl on Galleria and MORVAY on Purveyor Space Coast were removed.
- Deal ticket: the Sponsor field is a company picker (src/components/sponsor-picker.tsx): type, pick a company on file, the ticket links to it (sponsorCompanyId) so associations, contacts and email matching follow. Free text without a pick keeps the name unlinked and says so.
- RecordLayout is responsive: three columns from xl (1280px), otherwise the right column (associations) stacks below, so it is never pushed off screen on a laptop.

## 2026-09-10: smarter LP-request Handle, faster dashboard

- Deal momentum, LP request Handle: the draft replies all on the latest sponsor-only thread about the deal and reads "Hi Kyle - please also see the below requests from Nelnet on Everett Mall Plaza:", the parsed asks as bullets, then the LP's own words quoted (signature and quoted thread stripped, src/lib/lp-message.ts) and every file the LP attached copied onto the draft (found in whichever team mailbox holds the LP's email). The server draft is what opens in Outlook, so the words and files are there.
- Dashboard speed: the page no longer awaits the Sent Items sync or the follow-up draft sync on render; both run after the response, at most once a minute, so Handle/Dismiss/Approve and their refresh come back fast. The result shows on the next load.
- LP request Handle picks the conversation by its words, not its subject (src/lib/sponsor-thread.ts): among my recent sponsor-only, non-intro threads with the sponsor, the newest whose messages mention the deal (name words, or city plus "deal") wins and the draft replies all on its latest message, inbound or outbound. If none names the deal, the newest sponsor-only conversation that is not about another of the sponsor's deals is used ("Citivest Retail Opportunities LP Progress" carries Everett). A third party joining a thread disqualifies it. Only with nothing at all does it start a fresh email.

## 2026-09-10: RJL Israel intake in Hebrew, by email and WhatsApp

- No mailbox for RJL Israel exists in the Microsoft 365 tenant yet (deals@rjlisrael.com returns ErrorInvalidUser; the tenant's only deals mailbox is deals@rjlcapadvisors.com). Jonathan's first test email was not received anywhere the CRM can see. His step: add the rjlisrael.com domain to the tenant and create the mailbox, or create an alias on rjlcapadvisors.com and set ISRAEL_DEALS_MAILBOX in .env and Vercel. The cron and the Graph notification pick it up as soon as it exists.
- The apartment extractor (src/lib/israel-intake.ts) reads Hebrew natively: the system prompt carries the vocabulary (חדרים, מ"ר, מרפסת, ממ"ד, מחסן, חניה, קומה, יד ראשונה, יד שנייה, טופס 4, כיווני אוויר, מיליון), asks for English field values with standard spellings of cities and neighborhoods, and warns that Hebrew PDF text may come out reversed. Photos (listing screenshots, flyers) go to Claude as images, so a Yad2 screenshot is read like a document.
- Shared core `intakeApartments` serves both channels; email replies in HTML, WhatsApp replies in plain text.
- WhatsApp (src/lib/whatsapp.ts, /api/inbound/whatsapp): Meta Cloud API webhook. GET verifies with WHATSAPP_VERIFY_TOKEN, POST checks the X-Hub-Signature-256 with WHATSAPP_APP_SECRET, stores every message (WaInbound), waits 45 seconds for the rest of the sender's burst (text, photos, PDF), then runs the intake once for all of it and answers on WhatsApp with the tickets and what is missing. Env: WHATSAPP_TOKEN, WHATSAPP_PHONE_NUMBER_ID, WHATSAPP_VERIFY_TOKEN, WHATSAPP_APP_SECRET. The Cloud API does not deliver group chats; the listing has to be sent or forwarded to the number directly.
- LP follow-ups and Intros clear as soon as the email is sent: both now ask Sent Items directly (sentMessagesTo since the Handle click) instead of waiting for the email log to catch up. The follow-up draft check runs on every dashboard render again (it only touches rows with an open draft, so it is cheap); the heavier Sent Items log sync still runs after the response. An intro whose two parties are now on a live deal together (one firm sponsors an active deal, someone at the other firm is on its report at Taking a look or better) is retired for good (status DONE). Handle never replies to a draft sitting on a sponsor thread (an earlier click); it falls through to a fresh email rather than a Graph ErrorInvalidReferenceItem.
- Deals gone quiet (Data updates) have a Handle before Mark Deal Lost (`handleStaleDeal`): a check-in ("Hi Adam - checking in on X. Any update on your end?") on the thread the deal lives on. A mentioned deal replies all on the email that mentioned it (mentions now store `mentionedMessageId`; older ones are found by subject), often the intro; otherwise the ongoing sponsor-only conversation about the deal; otherwise the latest thread with the sponsor contact; otherwise a fresh email.

## 2026-09-10: Ready for launch carries progress reports due

- The dashboard window is "Ready for launch" (was Deals ready for launch). On top: Progress reports due (src/lib/report-due.ts): live deals (Deal Taken To Market through Term Sheet Signed) whose report changed since it was last sent, when the next send is due: every other day, and every Thursday at 4:30pm New York. No change to the report, no item. Handle replies all on the latest exchange with the sponsor (sponsor-thread.ts), "Hi Kyle - please find the updated progress report on X attached.", with the fresh PDF attached; the draft is watched (Deal.reportDraftId) and reportSentAt moves when it goes out (or a "progress report" email to a sponsor contact shows in Sent Items), which restarts the clock. "Sent already" restarts the clock by hand. Below: the signed deals ready to send, as before.
- Pickers (MultiSelect in criteria and contact forms, RoleCell and AssetCell on the lists) save once when the list closes (outside click or Done), not on every tick. Saving per tick ran the server action and refreshed the page under the open list, which snapped it shut. MultiSelect holds ticks from the autosaving form (stopPropagation) and fires one input event on close.

## 2026-09-10: progress report rules (from the Westwind and Everett reviews)

- Themes and Items Needed from Sponsor write themselves: `ensureTrackerSummary` (src/lib/tracker-summary.ts) runs whenever the report is loaded (tracker page, PDF, sponsor copy) and any note or LP request is newer than the last summary; a hand edit on the tracker page counts as fresh until the next change. Items come only from OPEN LP requests on Deal momentum (a request that was dismissed or answered is off the list); themes come from the notes.
- Status rules on LP replies (src/lib/lp-asks.ts): any real reply from an LP is at least Taking a look (4), including "confirming receipt"; only an auto-reply or out-of-office leaves the status alone. Colleagues at the LP's firm copied on the reply ("looping in James") are created under the firm and added to the row's people, so the next email reaches them.
- A second email of ours to a firm still at Deal Sent, an hour or more after the send, is a follow-up: the row goes to Followed up (3) (src/lib/deal-outbound.ts, both the person's own row and the firm row when the email went to a colleague).
- Notes must never cross deals: an LP's reply is pinned to a deal only by a strict subject match or Claude's explicit pick; JDI's Everett note carried two Aug 25 fragments about Pearl/Galleria from before those rules and was cleaned by hand.
- Legacy HubSpot intro records ("Nelnet | Indicap", "Clairmont | Indicap": stage Intro To Capital Made with a hubspotId, the LP's name in the sponsor slot) are not tickets. `isLegacyIntroTicket` (taxonomy.ts) keeps them out of the email-to-deal resolver and out of LP-ask attribution, on top of the earlier exclusion from candidates. Clairmont's Indicap request had landed on "Nelnet | Indicap", whose "sponsor" was Nelnet, so Handle wrote to Nelnet on the Everett thread; the request, its three emails and Clairmont's row now sit on the IndiCap Shallow Bay portfolio deal.
- The sponsor-thread "ongoing conversation" fallback (sponsor-thread.ts) now refuses any thread that talks about another live deal, anyone's, or whose subject is a deal email ("... Opportunity in ... | $..."). Only a conversation that is about this deal, or about nothing in particular, qualifies.

## 2026-09-10 (evening): sync gap, activity feed, report notes, underwriting rules

- Mail sync missed emails sent while a pass was running: the next pass started exactly at the previous start time and Sent Items appear in Graph a little late, so anything sent in that gap was never seen (Aviel's Trademark and Logistics Property follow-ups). `syncMailbox` now overlaps the window by 90 minutes; the Message-ID check keeps it from logging twice. All mailboxes were resynced over 12 hours (14 of Aviel's emails came in).
- Activity feed (src/components/email-log.tsx + email-row.tsx): emails read like an inbox (who to whom, when, subject, first lines), "See more" fetches the whole email from whichever team mailbox holds it (own words first, quoted history behind a toggle), "Open in Outlook" jumps to the email through the rjlcrm: bridge with Outlook web as fallback (src/app/email-actions.ts). Contacts, companies and deals all use it.
- Report notes: one entry per response, oldest first (normalizeNote in tracker.ts); exact and near repeats collapse, so Refresh responses never doubles a note. All 84 existing notes were normalized.
- Business plan sanitizer (cleanBusinessPlan in style.ts): drops any sentence with a fielded fact (dollar amounts, returns, cap rates, yields, loan terms, exit, close timing, year built, SF, units, seller, sourcing), applied when the extractor or Fireflies writes the plan and again when the deal email renders {{deal.summary}}, so older tickets comply.
- Underwriting rules from the old system added to the extractor (src/lib/intake.ts): model-first with sponsor-email override flagged; LTC and LTV read separately; deal-level IRR/multiple with split-level flagged; amortization from the CF sheet; stabilized year chosen from the occupancy/CoC rows, not Year 3 by default; ambiguous labeled cells resolved from surrounding rows or left blank. Also in memory (aviel-data-rules).
- Legacy HubSpot intro records: `isLegacyIntroTicket` needs the sponsor slot to be a capital source (Investor/Lender, not Sponsor) or, failing roles, no report rows; a real HubSpot deal that reached Intro To Capital Made with a report (Pembroke Pines) is not legacy. Their Handle replies on the intro email itself (Intro table, else Sent Items "Intro" subject naming both firms).
- Refresh responses reads the full history: 180 days, tied by the deal's name and city as well as its firms, and the LP-ask scan reads any tied reply however old. The cron ties stray emails (no deal, subject names one) every run (linkStrayEmails). Pembroke Pines was rebuilt this way: 55 emails tied, 9 passes with reasons, Clairmont interested, 4 taking a look.
- deals@ replies carry the files: the summary that goes back on the thread has every document from the forward attached (and the files pulled from Dropbox / Drive / OneDrive links, from their "CRM Files" stash message), images and signature logos left out. Same for the multi-deal reply, the follow-up reply and the re-sent summary from the ticket (which attaches every file the ticket knows). Copying is per file and never blocks the reply.
- Deal momentum shows one item per matter (listMomentum in momentum.ts): items for the same party collapse when they are on the same deal, when one item's summary names the other's deal, when their summaries share a date ("Aug 25, Sep 8") or most of their words; the most actionable kind survives (LP request, then sponsor item or engagement letter, then intro, then action, then mentioned). A "mentioned, never sent" item hides once anything else is open on that deal. Nothing is deleted; Handle on the surviving item is the one to use.

## 2026-09-11: compare units, occupancy in every deal email

- RJL Israel Apartments: "Compare units" (next to New apartment) switches the list to compare mode (?compare=1): a tick box at the left of each row, up to five, and a bar at the bottom that opens /israel/apartments/compare?ids=... Side by side columns: asking price and price per meter on top (best marked green), then address, city, neighborhood, rooms, built or delivery, floor, stories, units, directions, mamad, internal and mirpeset m² with square feet, ceiling, parking, machsan, seller type, developer, project, and the floorplan at the bottom. The sidebar shows the comparison (and an open apartment ticket) under Apartments, like a deal's steps under Deals (src/components/apartment-context-nav.tsx).
- Current occupancy is a line in Deal Metrics on every acquisition deal email ("Current Occupancy: 91%") whenever the ticket has it, in every template that uses {{deal.metrics}} (all house templates do). deals@ already lists "Current occupancy" under Still missing on acquisitions without it; the Excel model and later follow-ups fill it.

## 2026-09-11: portfolios (several properties, one deal)

- A portfolio is a deal ticket of its own with the property tickets as components (`Deal.parentDealId`, src/lib/portfolio.ts). "Combine into portfolio" on any ticket lists the sponsor's other live tickets; picking them creates the portfolio with the totals (units, SF, price, cap, debt, ask; occupancy weighted by units; year built as a range), one business plan written from the components' plans (house rules, sanitized), the first sponsor bio, the highest stage, and moves any report rows and open momentum items up. Components leave the board, LP follow-ups, the culling list, duplicates and Ready for launch; each shows a banner linking to the portfolio, with "Take it back out".
- The deal email for a portfolio: subject "Multifamily Portfolio Acquisition Opportunity in Las Vegas, NV and Phoenix, AZ | $X of JV Equity"; intro names each property and market, then the totals, then the ask across the portfolio; then one Deal Metrics block per property under its own heading ("Deal Metrics: Tides on Twain (Las Vegas, NV)"); then one Business Plan and one Sponsor Background (deal-copy.ts: subjectLine, intro, metricsHtml read `children`; withChildren attaches them wherever the email is rendered). Files: the portfolio serves every component's files on Send deal.
- Emails about any component (subject names one property) file on the portfolio (dealResolver maps child to parent). Tides Sunbelt Portfolio (Twain, Indios, 51st) was formed this way.
- deals@ forms the portfolio itself when the forwarder says so: in an email carrying several deals, the forwarder's own note above the forwarded email containing "portfolio", "together", "as one deal" (or a name in quotes, "call it "Tides Sunbelt Portfolio"") makes the property tickets components of one portfolio deal; the reply describes the portfolio the way it would read to investors (intro, one Deal Metrics block per property, plan, bio) and lists what is still missing by property. Without those words, several deals in one email stay separate tickets as before.

## 2026-09-11: access by business, RJL Israel email log

- Access is decided by email (src/lib/access.ts): an @rjlcapadvisors.com (or @rjlequities.com) sign-in opens RJL Capital Advisors only, an @rjlisrael.com sign-in opens RJL Israel only, and a person with both (Jonathan) opens both. `User.workspaces` (JSON list) stores Jonathan's overrides; empty means the sign-in domain decides. The session cookie carries the list; the gate (src/proxy.ts) sends anyone who opens the wrong business to the one they may open; the sidebar shows only the allowed logo tile(s). `User.israelEmail` is the person's RJL Israel mailbox (a second address for someone signing in with a US address, or their sign-in address itself on the Israel side; signing in with it also finds the user).
- Settings gains "People and access" (Jonathan only, src/app/settings/access-card.tsx): per person the two business boxes, the RJL Israel mailbox, active, and an Add a person form. A person is created automatically on first sign-in when their domain is one of ours.
- Sign-in tenant: `AZURE_LOGIN_TENANT` (default AZURE_TENANT_ID). If rjlisrael.com is its own Microsoft 365 tenant, set it to `organizations` and make the app registration multi-tenant; Graph app-only reads of Israel mailboxes then use `ISRAEL_AZURE_TENANT_ID` / `ISRAEL_AZURE_CLIENT_ID` / `ISRAEL_AZURE_CLIENT_SECRET` (src/lib/graph.ts picks the realm from the mailbox in the path). Without those, Israel mailboxes are read from the main tenant, which works only if rjlisrael.com is a domain of that tenant.
- RJL Israel email log (src/lib/israel-mail.ts): every RJL Israel mailbox (each person's israelEmail plus deals@rjlisrael.com) is read into `IlActivity`, like the US sync. Since nobody is pre-imported on the Israel side, an outside correspondent becomes an IlContact on first sight (name from the display name or the address) and their company an IlCompany named after the email domain (`IlCompany.domain`; free-mail and Israeli ISP domains never become companies). Mailers, notifications and bounces are skipped, internal-only mail is skipped, and an email lands on the person's one open IlDeal when there is exactly one. Contact and company pages in RJL Israel show emails and notes in one Activity window (src/components/il-activity.tsx) with See more and Open in Outlook, which now search the Israel mailboxes too. Runs from the daily cron, RJL Israel dashboard loads (3 min throttle) and the deals@ Graph notification.
- Still on Jonathan: create deals@rjlisrael.com (Graph returns 404 ErrorInvalidUser today), say whether rjlisrael.com is in the rjlequities tenant or its own, provide the Israel tenant app credentials if separate, fill in israelEmail per person under Settings, and the WhatsApp values (WHATSAPP_TOKEN, WHATSAPP_PHONE_NUMBER_ID, WHATSAPP_VERIFY_TOKEN, WHATSAPP_APP_SECRET; webhook https://rjl-crm.vercel.app/api/inbound/whatsapp on the messages field).
- Change the same evening: both logos always sit in the sidebar. A side is faded until you sign in with the account for it (an @rjlcapadvisors.com sign-in unlocks RJL Capital Advisors, an @rjlisrael.com sign-in unlocks RJL Israel); clicking a faded logo starts that sign-in (/login?business=IL, Microsoft gets a domain_hint). Someone with both signs in twice, one account at a time; the second sign-in adds to the session (`Session.w` unlocked sides, `Session.a` the account per side, shown under the name in the sidebar). Opening a faded side's URL directly goes to that sign-in and back. The Settings boxes are the ceiling of what a person may unlock; the sign-in is what unlocks it. Sign out clears both.
- 2026-09-10 late: rjlisrael.com is its own Microsoft 365 tenant. Its app registration (RJL CRM Israel: Mail.Read, Mail.ReadWrite, Mail.Send, User.Read.All, admin consent) is in .env and Vercel as ISRAEL_AZURE_*; token, deals@ inbox and the tenant's users (deals@, info@, farshid@, jonathan@, shawn@) verified. Jonathan and Shawn carry their @rjlisrael.com mailboxes on their CRM users, both allowed for both businesses. First Israel pass done (Shawn 1,924 emails scanned, Jonathan 864).
- Israel email log rules learned from that pass: the first sweep created 890 contacts from newsletters and notifications, so the sync now keeps only conversations. Sent Items decide who becomes a contact (people we write to); an inbound email from someone we never wrote to is logged only when it is a reply (Re:/Fwd:/Hebrew equivalents); a first email from a stranger waits until we answer. Also skipped: Outlook "Other" classification, marketing words in subject or preview, mailer local parts (noreply, news, updates, hello, team...), automated-mail domains (Instagram, LinkedIn, Google, Zoom, banks, Yad2, Madlan...), emails to more than six outside people, and anything not addressed to one of our mailboxes. Company domains are reduced to the registrable root (mail.instagram.com is instagram.com; co.il keeps three labels).
- Still open on the sign-in side (Part 3 of the guide): make the main-tenant app multi-tenant in Azure, then set AZURE_LOGIN_TENANT=organizations in .env and Vercel. Do not set the variable before the app is multi-tenant or sign-in breaks for everyone. Then Jonathan signs in with jonathan@rjlisrael.com to unlock RJL Israel; Shawn the same. WhatsApp values still pending.

## 2026-09-11: sign-in across tenants, contacts from Sent Items, RJL Israel lists

- Sign-in accepts both tenants: the main app registration allows the rjlisrael.com tenant (Supported accounts: multiple tenants, allowed list), AZURE_LOGIN_TENANT=organizations in .env and Vercel. The faded RJL Israel logo unlocks with an @rjlisrael.com sign-in.
- RJL Capital Advisors mail sync now creates contacts too (mail-sync.ts): a person the team writes to who is not in the CRM is created through contactForEmail (company from the domain, website read in the background); an unknown person replying to us is created too; a stranger's first email, mailers, HubSpot bcc addresses and automated hosts are not. `syncMailbox(mailbox, { sinceDays })` backfills. 120-day backfill across the five mailboxes: 166 contacts, 105 companies, 555 emails added (Nikko's mailbox was already syncing: 2,353 emails logged since May).
- RJL Israel companies and contacts lists order by lastActivityAt (recently emailed first), show a Last activity column, and the company row carries the logo (Google favicon by domain, like RJL CA). The company page shows the logo in the header and the kind as a token (src/components/token-picker.tsx): one click opens the options, one click saves (setIlCompanyKind). RecordHeader takes an optional `avatar`.
- RJL Israel companies carry Roles (the same word as RJL CA) instead of Kind: Developer (Yazam), Broker, Buyer, Attorneys, Mortgage Broker, Other (`IL_COMPANY_ROLES`, colors in `ilRoleColor`, src/lib/israel.ts). Roles are colored tokens toggled in place on the companies list and the company header (src/components/il-role-cell.tsx, saves once when the picker closes, like RJL CA's RoleCell). A company's roles flow to its contacts (added, never removed): on save, on contact create or edit, and when the mail sync creates or links a person. Contact roles are the company roles plus Seller; "Sales agent" became "Broker" everywhere (agents on apartments and deals are contacts marked Broker). Contacts list rows toggle roles too, and both lists filter by role, ready for RJL Israel email blasts when those are built. Old Kind values were mapped (Developer, Agency to Broker, Law firm to Attorneys, Mortgage to Mortgage Broker) and the column dropped.
- RJL Israel Houses (sidebar, under Apartments): a separate list and ticket for cottages and villas (`IlHouse`, src/app/israel/houses). The ticket carries city, neighborhood, address, rooms, how many floors (miflasim) with a ceiling height per floor that appears as the count is set (each shown in feet; stored as a JSON list ground floor first), built or expected delivery, parking, seller type (renovation year for second hand), mamad, internal and mirpeset m² with square feet, migrash m² shown in dunam (÷1,000) and acres (÷4,046.86), asking price with dollars, price per meter in shekels and dollars per square foot (same formulas as apartments). Developer, broker and seller link on the right; notes in the middle (IlNote.houseId). The list searches and sorts (₪/m², price, migrash, size) with a city filter; no compare or floorplan yet.
- Mirpasot on apartments and houses (src/components/mirpasot-fields.tsx): "Number of mirpasot" on both tickets. One keeps the single size and direction; more than one opens a size and a direction per mirpeset. The server sums the sizes into mirpesetSqm and unions the directions into mirpesetDirection (`mirpasotFrom` in israel/actions.ts), so price per meter, filters, lists and compare keep working on the totals; the per-mirpeset list is `mirpasot` (JSON). Compare shows each mirpeset when there are several.
- Apartment levels: "Apartment floors (levels)" 1, 2 or 3 (no 4). One level keeps the single ceiling height. Two or three (duplex, triplex) show a ceiling height per level, each in feet, stored in `ceilingCms`; the first stands in for `ceilingCm` so completeness and compare still work, and "Apartment floor" reads "Lowest floor" with the span noted. Jonathan's message cut off after "then", so this is the assumed behavior pending his word.
- Houses gained the apartment machinery: compare mode (`?compare=1`, up to five, /israel/houses/compare, best value marked, floorplan at the bottom), a floorplan window and API (/api/israel/houses/[id]/floorplan), pendingApproval with a Waiting-for-approval banner and Approve, and the sidebar context nav under Houses (ApartmentContextNav takes a section). deals@ and WhatsApp intake now sets `kind` per unit (a private house on its own plot is a house; anything in a building is an apartment) and creates IlHouse tickets with floors, ceilings per floor, migrash and mirpasot; the reply links each ticket under /israel/houses or /israel/apartments and lists the house-specific missing items (IL_HOUSE_COMPLETE_FIELDS / houseMissing). The dashboard's Deals to be approved shows apartments and houses together with a kind chip.
- Intake fix: the ticket schema outgrew the API's structured-output limit (16 nullable fields; ours had 25), so every deals@ email failed with a 400 and nothing was created. `extract` now puts the JSON schema (z.toJSONSchema) in the prompt, asks for one JSON object, and validates it with zod (nullish with defaults, so a missing key reads as unknown). Jonathan's two Laguna, Netanya house emails were processed after the fix (both waiting under Deals to be approved). Note the local ensureIsraelSubscription skips when APP_URL is http; production holds the Israel-tenant subscription for deals@ (expires and renews from the cron).
- Mirpasot field order: Mirpeset m² and its square feet, then Number of mirpasot, then Mirpeset direction; with two or more, the per-mirpeset size and direction rows replace the single ones. The apartment ticket page now passes mirpesetCount, mirpasot, levels and ceilingCms to the form. Israel contacts list shows the company logo next to the company.
- Mirpasot layout per Jonathan: "How many mirpasot" dropdown (1, 2, 3) above Mirpeset m²; for each mirpeset in turn: m², square feet, direction, and "Sukka?" (Yes, Partial, No). One mirpeset keeps the single field names; the list `mirpasot` always carries one entry per mirpeset (with sukka), totals kept in mirpesetSqm/mirpesetDirection. Intake reads sukka (מרפסת סוכה) per mirpeset. Compare shows a Sukka row. Apartment floor is a dropdown 0 (ground) to 100 and Building stories 1 to 100.
- deals@ reply and the dashboard now ask for every field on a ticket. Apartments: developer, building address, city, neighborhood, rooms, built or delivery, floor, building stories, building units, apartment direction, mamad, seller type, internal m², mirpeset m² (each separately when more than one), mirpeset direction, sukka, ceiling height (one per level on a duplex or triplex), parking, machsan size and location, asking price; plus project name on yad rishona and renovation year on second hand. Houses: address, city, neighborhood, rooms, floors, a ceiling per floor, built or delivery, parking, seller type, mamad, internal m², mirpeset m², direction and sukka, migrash, asking price; plus developer on yad rishona and renovation year on second hand. Same rules drive `apartmentMissing` / `houseMissing` for Deals to be approved and the Approve button.
- Apartments list: the developer shows its logo (like companies), and the row is one line (street after the name in small grey) so rows are as tight as the other lists.
- Houses list has the apartments filter rail (src/app/israel/houses/filters.tsx): search, city, neighborhood, internal m² (80 to 600+), mirpeset m², migrash (50 m² steps to 2 dunam+), rooms, floors (1 to 6+), built or delivery year, parking, seller type, mirpeset direction, mamad, sukka (yes / partial or yes / no), and the same sorts plus "Migrash, largest first". Filters carry through compare mode. Rows are one line with the developer logo, like the apartments list.
- Both filter rails cover the new ticket fields: apartments add Levels (1, 2, 3), Mirpasot (1, 2, 3), Ceiling height slider (240 to 400+ cm, the tallest level on a duplex) and Sukka; houses add Mirpasot, Ceiling height (tallest floor) and Floors as a picker (1 to 6). The rail is a fixed-height window that scrolls inside, with the Show button pinned at the bottom, and sticks while the list scrolls on wide screens.
- Houses carry a House type: Villa, Semi-attached, Cottage (`IlHouse.houseType`, `IL_HOUSE_TYPES`). Dropdown under the name on the ticket, a Type column and a picker on the list, a row in compare, part of the complete-ticket list, and the intake reads it (וילה, דו משפחתי, קוטג').
- RJL Israel roles: "Developer (Yazam)" is now "Sponsor (Yazam)" and "Kablan" (contractor) joined the list: Sponsor (Yazam), Kablan, Broker, Buyer, Attorneys, Mortgage Broker, Other. Ticking Sponsor on a company opens "Development, acquisitions or both?" right under it, in the role picker (list row and company header) and on the company form; stored as `IlCompany.sponsorFocus`, shown on the Sponsor token ("Sponsor (Yazam) · Development"). Existing Developer roles were renamed on 2 companies and 2 people. The developer picker on projects looks for the Sponsor role; deals@ files a listing's developer as a Sponsor.
- Projects (src/app/israel/projects/[id]/page.tsx) now fill themselves from their units. Houses join projects too (`IlHouse.projectId`, a Project field on the house ticket, Add house on the project). Left: "From the units", boxes computed live from the apartments and houses filed under the project (src/lib/project-ranges.ts): units in the CRM vs total, rooms range, internal size range, mirpeset range, asking price range (with dollars), price per meter range, floors range and duplex count, ceiling range, delivery dates, parking mix, seller type mix, directions, mamad, sukka and machsan counts. Tickets waiting for approval are counted but left out of the ranges. Middle: every email about the project, meaning with the developer's people, with the brokers and sellers of its units, or naming the project in the subject, plus notes (IlActivityLog). Right: developer with its role tokens, the Brochure window (PDF or image stored on the project, /api/israel/projects/[id]/brochure; FloorplanWindow takes endpoint/title/compact), then apartments and houses. deals@ files a house with a project name under that project, as it does for apartments.
- Dashboard cleanup (src/app/page.tsx): every item's buttons sit in one right-hand column (`.act-col`, 128px, centered on the row), stacked and the same size (`.act`). Blue is the action (Handle, Send deal, Merge, Approve), grey is the quiet choice (`.btn-grey`: Dismiss, Keep, Sent already, Not the same), red is Mark as deal lost (`.btn-danger`). Deal momentum and Intros: Handle over Dismiss. Ready for launch: Handle, Sent already, Mark as deal lost; signed deals: Send deal, Mark as deal lost. Data updates: Handle, Keep, Mark as deal lost on quiet deals; Merge / Not the same on duplicates; Approve / Dismiss on proposals. DraftButton takes `block` to fill the column. LP follow-ups unchanged.
- Dashboard items leave when handled and sent (src/lib/handled.ts). Every Handle records the click and whose mailbox (Momentum.handledAt/handledBy, Deal.staleHandledAt/staleHandledBy); on the next dashboard load the pass looks for an outbound email to the party since the click (email log, then Sent Items) and closes the item: Deal momentum goes DONE with the sent email as its latest message, so refreshMomentum leaves it closed until the other side writes back (upsert skips DONE with the same lastMessageId); a quiet deal gets staleCheckedAt = the send time, so it leaves Data updates for another quiet spell, and is hidden while its Handle is pending (a week without a send re-lists it). LP follow-ups (syncFollowUpDrafts), Intros (handledAt) and progress reports (syncReportDrafts) already worked this way. Dismiss, Keep, Sent already and Mark as deal lost act at once.
- Dashboard: Mark as deal lost is faded red italic text (`.act-lost`) in the same slot under the buttons, on Ready for launch (reports and signed deals) and on quiet deals under Data updates.
- LP request email to the sponsor (Handle on an LP_ASK item) never names the LP individual: the quote is attributed to the firm ("From Nelnet, Sep 8"), the asks are written about the firm, and lpOwnWords (lp-message.ts) strips the sign-off ("Thanks, Jim", "Best regards") and any signature lines left at the bottom (name, title, firm, phone, email, links). Same rule as never naming other LPs to sponsors.
- Apartment ticket: "Apartment type" (Regular apartment, Garden apartment, Penthouse; `IlApartment.apartmentType`) sits above "Apartment levels" (renamed from floors, dropdown 1/2/3 with no blank line; same for "How many mirpasot"). A garden apartment's mirpeset section reads Garden (How many gardens, Garden m², Garden direction) on the same fields. Under Sukka: "Sukka area (m²)" unless the answer is No, then "Pool?" (Yes/No) and "Pool size (m²)" when yes, per mirpeset or garden (stored in the `mirpasot` items: sukkaSqm, pool, poolSqm). Houses ask Pool? and Pool size under Migrash in acres instead (`IlHouse.pool/poolSqm`), not per mirpeset. All of it is on the deals@ questionnaire and the approval rules (apartment type, pool yes/no, sukka area when yes or partial, pool size when yes; garden wording for garden apartments), read by the intake (דירת גן, פנטהאוז, בריכה), and shown in compare.

## 2026-09-14: dashboard speed

- Measured before: each dashboard render asked Microsoft inline (follow-up drafts 1.3 s, report drafts 1.9 s, intro Sent Items checks 2.1 s), and every button re-rendered the page through revalidatePath, so a Dismiss took seconds. Database-only pieces were 40 to 250 ms.
- Now the render path reads the database only. All Graph checks (syncFollowUpDrafts, syncReportDrafts, syncHandledIntros, syncHandledMomentum, syncHandledStale, syncRecentSent) run in `refreshDashboardSignals` (src/lib/dashboard-refresh.ts) after the page is served, throttled to once per 45 s and never twice at once; the cron runs the same pass. quietIntros keeps the email-log check only.
- Rows vanish on click: every dashboard `<li>` is an `Item` and every remove-button an `ItemForm` (src/app/dash-item.tsx). The row hides the instant it is clicked, the server action runs in a transition, and the row comes back with the error if the action fails. Window counts update on the next load. Handle (DraftButton) is unchanged: it still waits on Graph to build the draft (next step: pre-building drafts in the background).
- Still on Jonathan: DATABASE_URL points at the direct Supabase port 5432; from Vercel every cold function opens a new connection (~250 ms). The pooled connection (Supabase "Transaction" pooler, port 6543, add `?pgbouncer=true&connection_limit=1`) would remove that; the value is his to paste.
- quietIntros went from 90+ queries (three per intro) to three for the whole list: 1.5 s to 0.37 s for 30 intros. The dedupe key regex had lost its backslashes (matched nothing useful); fixed.
- Database connections: DATABASE_URL is now Supabase's transaction pooler (port 6543, pgbouncer=true, connection_limit=1) and DIRECT_URL the direct port 5432; the Prisma datasource has `directUrl = env("DIRECT_URL")` so `prisma db push` and migrations bypass the pooler. Vercel needs both variables. Local test through the pooler: first query 299 ms, second 93 ms.
- Everett / Corebridge (Sep 14): Handle on Corebridge's LP request opened a thread with AJ at JDI because the request was pinned to a phantom ticket "JDI Realty | Everett" (stage Deal Mentioned) that the mention detector had created from JDI's email about Everett; JDI is an LP on Citivest's Everett Mall Plaza, and its company carries the Sponsor role too. The phantom was merged into the Citivest deal and the request repointed at Citivest. Three guards: the mention detector skips a name that matches a deal the firm sits on (report rows) or any live deal by that name, whoever sponsors it (nameContains); LP-request pinning prefers the deal the firm is on and never lets a Deal Mentioned record outrank a live ticket the firm was sent; possible duplicates flags "one name contains the other" (5+ letters, whole word, not a generic word like Plaza). An audit found no other phantom Deal Mentioned records of this kind.
- Departure wording (departures.ts HUMAN_DEPARTURE): "I left you a VM" from AJ Greenberg (JDI) had queued a "remove this contact" proposal. Leaving now has to name the firm, role or team ("I left the firm", "moved on from Nelnet", "is no longer with the company"); "I left you a voicemail", "I have left comments", "we moved on to the next deal" no longer count. Jonathan had already dismissed that proposal; AJ was never marked departed.
- Data updates only carries deals at Deal Received or further: the quiet-deal culling list starts at Deal Received (was Deal Mentioned), and possible duplicates ignore Deal Mentioned records on either side. Deal Mentioned tickets still exist on the board and still feed Deal momentum's "mentioned, never sent".
- Quiet-deal culling threshold raised from 3 to 7 days (STALE_DAYS); Keep hides a deal for another 7.
- Blind intros (taxonomy.ts `isBlindIntro`): a HubSpot record shaped "Firm | Firm" with no files, no facts, no address, and the "property" just the second firm (Wright | Marble, Corta | Marble, Intro - MLG | BKM, Nelnet | Indicap...) is an intro, not a deal. They are out of Data updates (quiet deals and duplicates), never take emails (dealResolver) or LP requests (lp-asks candidates), and appear under Intros to reconsider as rows with id "deal:<id>" (parties from the name, last touch = record, Keep or latest email; same 30-day quiet / 12-month window as email intros). Handle on one replies on the intro email through handleStaleDeal's legacy path; Dismiss sets details.introDismissed. Data updates therefore holds only deals at Deal Received or further that came through deals@ or carry files, facts or an address.
- Blind intros in Intros to reconsider cover every live stage (a "Firm | Firm" record at Term Sheet Issued that went quiet is still an intro), and their last touch is the intro date, a Keep, or the latest email, never the record's updatedAt (the import rewrote every record on Sep 4).
- Progress-report Handle (openReportDraft): reopening an earlier unsent draft now swaps in today's PDF when the report changed after the draft was last touched (old "... Progress Report.pdf" attachments removed, fresh one added, recipients and thread untouched). Westwind's draft from Sep 10 carried a Sep 10 report while the report had moved on Sep 14; refreshed.
- Everett / JDI (Sep 14): the report PDF Jonathan saw was the one built into the Sep 10 draft, before JDI's Galleria fragments were cleaned from the Everett row; the draft refresh replaced it and today's PDF has no cross-deal words. Guard added in lp-asks: a note whose words name a different deal the firm is on (property name or city, 5+ letters) and not the pinned deal is not written to the pinned deal's row; the status still moves and the asks still file.
- Progress reports exist only while a deal is out to market: `REPORT_STAGES` (taxonomy.ts) = Deal Taken To Market, Intro To Capital Made, Term Sheet Issued. Active progress reports and Ready for launch's report reminders use it; earlier stages (Food Lion at Deal Mentioned with one row) and Term Sheet Signed and beyond are out.
- Deals board search: a search box in the Deals header (with the owner filter). It matches name, property, sponsor (name and company), city, state, address and the business plan, across active, closed and lost; a search lists every closed and lost match instead of the 15-deal preview, with a Clear link. The flat list under "see all" searches the same fields.
- Progress report page cleaned up: header has Back to deal, Sponsor view / PDF and "Send to sponsor" (the same Handle as Ready for launch: reply-all on the latest sponsor exchange with today's PDF, a reopened draft refreshed). Gone: Copy sponsor link, Follow up with N not responded (the dashboard's LP follow-ups cover it), Refresh responses and Rewrite from notes (both run on their own). Notes, Notable Feedback Themes and Items Needed from Sponsor are edited in place: borderless text that grows as you type and saves itself a moment after typing stops (src/components/growing-textarea.tsx); no edit window, no Save button.

## 2026-09-14: deal naming convention

- Every ticket from Deal Mentioned through Term Sheet Signed reads the same way. Board card (src/app/deals/board.tsx): sponsor on top, deal name, then three tokens (asset class, Development or Acquisitions, City, ST; a blank one shows as a dashed "asset class?" so the gap is visible), then the owner. Nothing else on the card (requested amount and dates came off). An intro record reads INTRO over "Group 1 | Group 2".
- The `name` field follows "Sponsor | Deal name" for deals and "INTRO | Group 1 | Group 2" for blind intros (`conventionalName` in src/lib/naming.ts). Nothing is renamed on its own: `proposeNamingConventions` (daily cron, run once by hand on Sep 14) files a CriteriaProposal with `dealId` and a `dealName` change under Data updates; Approve writes the name (applyProposal), Dismiss records the refusal and the same rename is not asked again. `isBlindIntro`/`introParties` accept the INTRO prefix.
- The convention also makes placement on the dashboard unambiguous: an INTRO ticket is an intro (Intros to reconsider, never Data updates or emails), a "Sponsor | Deal" ticket is a deal.
- Boynton Beach / SilverCap (Sep 14): Jim Delaney's seven questions (Sep 11) vanished from Items Needed because (1) his Sep 14 scheduling reply overwrote the firm's LP_ASK item (one item per deal and firm) with "Confirm call scheduling times", and (2) Items Needed drew only from OPEN dashboard items, so Dismiss on the dashboard also emptied the report. Fixes: a later email from the same firm merges into their outstanding asks (`mergeAsks`/`parseAsks` in momentum.ts; an ask answered from the ticket moves to the answered list); Items Needed from Sponsor lists every unanswered LP ask from the last 90 days whatever the dashboard item's state (OPEN, DONE or DISMISSED), one line per question, grouped when several firms ask the same thing. Jim's email was re-read and the Regal report regenerated. Notable Feedback Themes and Items Needed are bulleted in the editor too (GrowingTextarea `bullets`: "• " shown, stripped on save; Enter starts a new bullet); the sponsor view and PDF already used bullets.

## 2026-09-15: RJL Israel deals mentioned

- Properties people float by email without ever sending the listing become IlDeal records at the new "Mentioned" stage (src/lib/israel-mentions.ts, the Israel twin of mentions.ts). It reads the RJL Israel email log thread by thread (one per contact and subject, full bodies from Graph, Hebrew read natively), asks Claude which specific properties came up, skips anything RJL Israel already has as an apartment, house or mention, and files each one named "Property · Neighborhood, City" with the contact as its agent, the price if stated and one line of what was said. Each thread state is read once (MentionScan).
- "Mentioned" is the first IL_DEAL_STAGES entry and gets its own amber column on the Israel funnel, right after Lost, matching RJL Capital Advisors' Deal Mentioned. The dashboard's third window is "Deals mentioned" (it replaced the empty Data updates card) with Dismiss on each row (dismissIlMention moves it to Lost, reason "Mentioned, not pursued"). Runs from the daily cron and RJL Israel page loads; `closeLandedMentions` turns a mention into a Lead once the listing arrives as an apartment or house ticket.
- RJL Israel company roles gained "Lender" (violet token), between Buyer and Attorneys.
- One property, one ticket at deals@rjlisrael.com: a second email about an apartment or house we already have (same city or unknown city, names sharing most of their words, no clashing unit numbers; `sameUnit` in israel-intake.ts) updates that ticket instead of making another. Every value the new email states overwrites the old one, blanks and empty lists never erase anything, the floorplan lands if the ticket had none, a note reads "Updated from an email ...", and the reply lists what is still missing on the merged ticket. Three Genesis (Ir Ganim) and four Laguna (Netanya) duplicates from Sep 11 to 15 were folded into one each (oldest kept, newer values filled in, notes moved).
- Sponsor answers (src/lib/sponsor-answers.ts): the other half of the LP and sponsor back-and-forth. For every deal with LP asks (open, handled or dismissed), each new inbound email from the sponsor's people is read once (SponsorAnswerScan); Claude says which listed questions it answers and with what. Each answered firm gets a SPONSOR_ANSWER item at the top of Deal momentum ("sponsor answered, pass it to the LP") holding the question/answer pairs, the LP's ask email as its thread (lastMessageId) and the sponsor's email as `refMessageId`. Handle replies all on the LP's own email with the answers on top, the sponsor's words quoted (other investors' names replaced by "another group") and the sponsor's files attached. The answered asks move to the LP_ASK item's answered list, so Items Needed from Sponsor drops them. Scheduling notes, thanks and "we will get back to you" create nothing. Missed before this: Parker Webb and Cory Tuck answering Nelnet on the BrightStar recap, Benjamin Marks answering Dome Equities on Westwind, David Rottenberg sending KCB the Purveyor model.
- Follow-ups the same evening: parseAsks split on a bare "| Already on the ticket:" (an empty open list left the answered text reading as an ask); scheduling asks and answers ("confirm the call time", "which timezone") never become sponsor-answer items; malformed summaries repaired.
- Sponsor answers, who counts as the sponsor: the detector used to read only emails from people at deal.sponsorCompanyId, so Parker Webb and Cory Tuck answering Nelnet from ftwinvestmentsllc.com (the BrightStar principals' other entity) were never read. Now any inbound email filed on the deal from someone who is not an investor counts (whichever company they write from), plus the sponsor company's emails with no deal pinned; Claude still decides whether the words answer a listed question. The three BrightStar KC recap tickets (two legacy HubSpot imports, one live) were merged into "Bright Star PG | Kansas City Metro Retail Portfolio (BSPG Recap)"; the Nelnet and jdi-realty sponsor-answer items now exist on it.

## 2026-09-15 (afternoon): Required Items Lists, Users, check size ranges, Israel dashboard

- **Required Items Lists** (Jonathan: "if I realize I want to start asking for a new item, I could just edit it in the list instead of telling you"). RJL Capital Advisors: Templates > Required Items Lists (src/app/templates/required-items). One list for acquisitions, one for developments; every item carries the asset classes it applies to (grey chips, click to skip it there), how it is asked, its type (document, explanation, number, yes or no, short answer) and an Ask switch. RJL Israel: Required Items Lists under Deals in the left column (src/app/israel/required-items), tabs Projects / Apartments / Houses. Items save as you leave the field; arrows set the order the sponsor or agent reads.
- Storage and plumbing: RequiredItem rows (workspace CA or IL, category, key, label, question, kind, strategies, assetClasses, sortOrder, active). The first read seeds the rows from the code defaults (DEFAULT_CHECKLIST in checklist.ts, IL_DEFAULT_REQUIRED in israel.ts). `loadChecklist()` / `loadIlRequired()` in src/lib/required-items.ts swap the live lists (CHECKLIST, IL_REQUIRED) that every existing function reads, cached 30 seconds per server instance, and are called at the entry points: deals@ intake (extractWithClaude builds the Claude schema at call time now), the intake page and actions, the deals inbox reply, progress report (Items Needed from Sponsor), Ask the CRM, deal-knowledge merges; Israel intake, dashboard, ticket pages, approve. An item that fills a ticket field can be reworded, moved or switched off, never deleted; an item Jonathan adds is a question with no field (key x_...). RJL CA answers to those land in Deal.details under the key like every other checklist answer, so extraction, the intake form and Still needed pick them up with no further work. RJL Israel answers land in the ticket's `extra` JSON (new column on IlApartment, IlHouse, IlProject): the Israel extractor gets the custom questions in its prompt and returns `extra`; a second email merges its answers in; the ticket page shows an "Other items" card to fill them by hand. Automatic follow-ups stay in code (sukka area when there is a sukka, pool size, ceiling per level, project name on yad rishona, renovation year on second hand), and the page says so.
- Israel dashboard: three windows again. "Deals mentioned" had replaced the (empty) Data updates card; Data updates is back and now lists tickets a later email or WhatsApp message added data to (the intake's "Updated from ..." notes, last 14 days). The Dismiss button on Deals mentioned was stretched across the row (the `act` class is `w-full`, meant for the act-col stack on the RJL CA dashboard) and squeezed the text into a one-word-per-line column; plain button classes now.
- **Users**. RJL Capital Advisors: Settings is a dropdown (Settings, Users); RJL Israel: Settings in the left column. Both show the users of that business (src/components/users-card.tsx): what each opens, RJL Israel mailbox, active, and a status (Signed in on date / Invited on date, not in yet / Not invited). Add a person by email and, with "Send an invite" ticked, an email goes out through Resend with the sign-in link per business and which Microsoft account to use; "Send invite" / "Invite again" per row. User gained invitedAt and lastSignInAt (the sign-in callback stamps it). People and access left the main Settings page (a pointer remains).
- Engagement letter sent but the deal stayed at Deal Received (Carderock Investments | Gateway Shopping Center, Sep 15): the letter was sent from desktop Outlook, so the Graph draft stayed a draft and `syncEngagementDrafts` looked for it in Sent Items by subject. That filter went into the URL raw, and the ampersand in "Engagement Letter - RJL Capital Advisors & Carderock Investments" cut the query short; Graph rejected it, the catch swallowed it, and the letter was never seen. The filter is URL-encoded now. Rerunning the sync moved Carderock to Engagement Letter Sent and recorded the sent dates on two earlier letters that had the same problem (Zenith Group, Pearl Capital; both were already past that stage by hand).
- **RJL Israel access and email separation** (Jonathan, Sep 15). Sign-in domains: @rjlcapadvisors.com (and @rjlequities.com) open RJL Capital Advisors; @rjlisrael.com opens RJL Israel; the partner company @liviemisrael.com opens RJL Israel for the listed people only (Farshid, Elisheva, Fariba, Ohad, Leon, Shawn, Jonathan, Yitzchak; `PARTNER_PEOPLE` in src/lib/access.ts). No other domain can sign in or be added (`signInAllowed`; the Users page refuses others). Jonathan and Shawn are listed on the RJL Israel Users page by their @rjlisrael.com addresses; jonathan@ and shawn@liviemisrael.com are aliases of the same rows (`aliasesOf`: same first name at the other Israel domain), so a sign-in from the partner address does not make a second user. Six partner users were created (firstname@liviemisrael.com, RJL Israel access, not invited yet) so Jonathan can send invites from RJL Israel > Settings. Email logs stay apart: RJL CA reads @rjlcapadvisors.com mailboxes only (`isCaMailbox`), RJL Israel reads @rjlisrael.com mailboxes only (`isIlMailbox`); partner mailboxes live in another tenant and are never read. A partner person's first sign-in goes through the multitenant app with the `organizations` authority; their tenant may ask them (or their admin) to consent to the app once.
- Users page simplified (Jonathan, Sep 15 evening): no business checkboxes, the email's domain decides what opens (an Israel-side address on the RJL CA row adds that side); columns are Person, Email, Active, Status, Save / Send invite. The Israel page carries no business name because RJL Israel and LiviemIsrael use it together, and anyone at @rjlisrael.com or @liviemisrael.com can be added there (`domainAllowed`); the listed-names rule (`signInAllowed`) only applies to partner people who sign in before being added.
- Aviel's emails "not in the CRM" (Sep 15): his mailbox was being read all along (2,963 emails logged). Two things were off. (1) Timing: the full mailbox pass runs from the daily cron (11:00 UTC) and otherwise only when someone opens the dashboard or a company page, at most every 3 minutes; an email sent while nobody was in the CRM waited for the next visit. Now every RJL CA page load (contacts, companies, deals) kicks the throttled refresh from the root layout, and every RJL Israel page load kicks the Israel one. (2) The quick Sent Items pass that runs from the dashboard (`syncRecentSent`) logged the email but never bumped lastActivityAt, so Last activity on 57 contacts and 38 companies lagged the log (Hanna Rasche at Becknell showed Sep 8 with a Sep 15 email logged). Every logging path now calls `bumpLastActivity`; the stale stamps were backfilled from the log. Aviel has nothing to connect on his side.
- Users page, second pass (Jonathan): no Active box, no Save, no invite checkbox. A row is Person, Email, (RJL CA only: the Israel-side address as text), Status and one button, Send invite / Invite again. Adding a person is name plus email; the invite goes from the row. Status reads "Using the CRM, last in date" from lastSignInAt, which only exists since Sep 15; for people already in before that it was backfilled from the traces a signed-in person leaves (Ask the CRM threads, Handle clicks, stale-deal and intro handling), which is why Aviel no longer shows as "not in yet".
- Active progress reports (Jonathan, Sep 15 evening): the Follow up button is gone; each row has Open report and **Not active**. Not active stamps `Deal.reportInactiveAt` and the report leaves the page (and the dashboard's reports-due list) until an investor row on the deal changes after that moment, which brings it back on its own. A simple intro is not a progress report: a deal is listed only when its full investor list went out, read as five or more groups at Deal Sent or further (`REPORT_MIN_SENT` in src/lib/report-active.ts) and not an INTRO ticket. That took the four INTRO tickets with one or two rows (Stablewood | Gulfstream, Tall Grass | Stablewood, Clairmont | AREY, Bainbridge | Jordan Park) and "INTRO | Wright | Marble" off the page. The tracker page itself stays reachable from any deal.
- **Required Items Lists, second architecture** (Jonathan, Sep 15 night): windows instead of rows. Each window is one bulleted textbox (one item per line) with its heading on top: RJL CA toggles Acquisitions or Development and picks asset classes; RJL Israel toggles Apartments or Houses (a third default window covers the project ticket). The first windows are the defaults, one per kind, fixed heading, and apply to whatever has no window of its own; a window with asset classes picked replaces the default for those classes. The plus square after the last window opens a new one; the x removes a non-default one. Storage: RequiredList rows (workspace, kind, assetClasses JSON or null, isDefault, text). Seeded from the code defaults so nothing changed for any deal: default Acquisitions and Development windows, plus Residential acquisitions (Multifamily, BFR, Student, Senior, Mixed Use: adds lease trade-out and affordable), Land acquisitions (no rent roll, occupancy, capex, yield on cost) and Land development (no yield on cost); scripts/_windows-check.ts confirms every strategy and class reproduces the old list. `loadChecklist` turns lines into items: a line that matches a built-in item by its words (token overlap, `matchLine`) keeps that item's key, kind and core field, with the line as its label; any other line is a question of its own (key x_...). One key can sit in several windows with different classes, so `applicableItems` and the Claude schema builders dedupe by key (`uniqueChecklist`). The old RequiredItem table stays in the database unused until Jonathan says to drop it (dropping needs a data-loss confirmation).
## 2026-09-15 (night): RJL Israel tickets, send flow, pools, snooze

- **Pictures**: a Pictures window sits above the floorplan in the middle of every apartment, house and project ticket (src/components/il-photos.tsx). Drop or pick several renderings and photos at once; click to open full size; x removes. Stored as IlPhoto rows (bytes on the row, served by /api/israel/photos/[id]; upload at /api/israel/[kind]/[id]/photos). The Send page can attach them.
- **Location**: under Seller on the right (under Developer on a project), a Google Maps pin for the address (src/components/il-map-card.tsx). The address is checked first: `ensureGeo` (src/lib/geocode.ts) looks it up once per address through OpenStreetMap's Nominatim (Israel only, street and city first, then neighborhood, then city alone) and keeps lat, lng, the resolved place and the query on the row (`geoQuery` means an unchanged address is never looked up again). The line under the map says "Address found", "Street not found; pinned to the neighborhood" or "Only the city was found", so a bad address is caught before the unit goes out. "Check again" after fixing the address; "Open in Google Maps" searches the address text there. No Google API key is involved (the embed is the keyless maps.google.com output=embed frame).
- **Private pool**: one question on the unit ("Private pool?", `pool`/`poolSqm` now on IlApartment as well as IlHouse), no longer one per mirpeset. That was the "Pools goes to a dash every time I select a number" complaint: the pool select lived inside each mirpeset row, so picking a mirpeset count made a fresh blank pool question appear. The mirpasot component only carries size, direction and sukka now; the intake stores the private pool on the unit (a pool an email attributes to a mirpeset still counts). Projects gained "Project pool" (shared pool, yes or no). The Required Items windows say "Private pool (yes or no)"; a one-word line still matches its item (`matchLine` accepts a full one-token overlap at half the words).
- **Summary email** replaces the Notes card in the middle (src/lib/israel-summary.ts, src/components/il-summary-card.tsx): the send-out as it reads with the ticket's data today (subject "Name · Neighborhood, City"; greeting with {{first}}; one line per filled fact; the description; an offer to send the floorplan and pictures). A blank field is simply not mentioned, so a thin email means a thin ticket. Notes are folded under it; the intake still writes its "Created from" and "Updated from" notes there. Jonathan will work on the template wording next.
- **Send apartment / house / project** (src/app/israel/send/[kind]/[id]): step one, every contact with an email, Buyers ticked first, search and role chips, tick people, Next. Step two, the email (started from the summary, editable, {{first}} per person), the files to attach (floorplan or brochure, pictures), From (the signed-in person's @rjlisrael.com mailbox), Launch. Each person gets their own copy: a Graph draft in that mailbox with the files attached, then sent; logged as an IlActivity on the contact and company, a note on the ticket. No engagement letter on this side; recipients are contacts, not companies. Needs Mail.Send on the Israel tenant's app registration (Mail.ReadWrite covers the draft; the send step fails cleanly with the reason if the permission is missing).
- **Deals mentioned**: Snooze a week (`snoozedUntil` on IlDeal) beside Dismiss; snoozed mentions are back on the dashboard after seven days, Dismiss stays for good.
- **Deals on people and companies**: a Deals window on the right of every Israel contact (buyer or agent on the deal) and company (its people's deals, or deals on its apartments), under Companies / Contacts.
- **RJL Israel email blasts** (Email blasts in the left column, src/app/israel/campaigns, src/lib/il-blasts.ts): the RJL CA blasts idea on the Israel contacts. New blast: roles (Buyer ticked first) and optionally a city they want, a live count, the email in the same editor as RJL CA (edit in place, reader view, HTML, test to yourself), saved as a draft; the blast page has the people (x leaves one out), Send a test to me, Send now. Sent through Resend 100 at a time, `{{contact.firstName|there}}` and friends filled in per person, a one-click unsubscribe per email (the same signed token as RJL CA with an il: prefix; the unsubscribe route flips `IlContact.unsubscribed`), each send logged as an IlActivity on the contact. Models IlCampaign and IlCampaignRecipient; EmailTemplate gained `workspace` (CA or IL) so the RJL Israel form lists only Israel blast templates (a blank starter is always there) and the RJL CA form only its own. The sender is MAIL_FROM (the RJL CA domain) unless IL_MAIL_FROM is set to a verified RJL Israel sender in Resend; replies go to the sender's @rjlisrael.com mailbox. No follow-up rounds on this side yet.
## 2026-09-15 (late): everyone on an email becomes a contact; templates as windows; underwriting instructions

- **Root cause of the missing contacts** (Scott at Curtis Ashton, Kevin at Burnham, Keenan Klett and Lauren Campagna at Suncap, Brendan Neary at Hammond, many of Nikko's): the mail sync resolved one person per email ("who is this about") and created a contact only when neither a known contact nor a known company was on the email. So the second person at a firm, and anyone on CC once one recipient was known, could never be created. Now (`resolveParticipants` in src/lib/mail-sync.ts) every external person on an email is a contact when the email is a conversation: one we sent to up to ten outside people, or a reply thread with us on it (sender and everyone copied). A stranger's first email still creates nobody. The first of them is the Activity's contact; the others are `ActivityParty` rows, so the email shows on each person's page and their company's page (both pages query `OR: [{contactId}, {parties: {some: {contactId}}}]`), and Last activity moves for all. The quick Sent Items pass uses the same resolver.
- **Owners**: a contact or company the sync creates belongs to the person whose mailbox it came from (`ownerId` on create, and on an existing contact with no owner), so Aviel's and Nikko's people show them as owner. Backfill (scripts/_participants.ts) reread every logged email's stored recipients (from, to, cc), created the missing people, linked them, and gave ownerless contacts the mailbox that first wrote to them (companies take their earliest owned contact's owner). The company page's owner select was already there and saves through the autosave form; if Aviel still cannot set himself, that needs a look on his screen.
- **RJL Israel ticket layout**: summary email (left) and floorplan (right) side by side at the top of the middle column, 68vh tall so both show without scrolling; pictures underneath.
- **Underwriting instructions (Sep 15, from Jonathan and Aviel)** applied in src/lib/intake.ts: requested amount = 90% of total equity rounded to the nearest $500,000 (`applyDealRules`, always, total equity being total capitalization minus total debt); LTV = debt over price and LTC = debt over total capitalization computed separately (`ltc` is now its own extracted field, carried to the ticket); total capitalization from Sources and Uses; Year 1 cap rate always Year 1; stabilized cash-on-cash from the model; both interest rate scenarios when the model has two; JV Equity unless stated; expected close "Month Year" or "Q# Year"; closest-match with a flag for sourcing, seller profile and lender type, exact match only for asset class, loan term, amortization and position in the stack; sponsor bio order (founded when and by whom, focus, scale). Owner = the teammate who forwarded to deals@ (already so in deals-inbox). Formatting ($ with commas, two-decimal %, x multiples) is done at render time by the merge and copy helpers; fields stay numeric.
- **Email templates as windows** (Templates > Email templates, src/components/template-windows.tsx): each template is a window shown the way the email goes out (name, subject line, Calibri body), merge fields as light blue tokens over a transparent textarea so you type plain words in place; "Insert a field" drops a token at the cursor (grouped: the person, the deal, checklist answers); saves as you type; x deletes (a template that sent campaigns is archived under "(archived) ..." so history reads); the plus square opens a new one with a greeting. "Used for" is gone; kind stays in the database (blast templates carry a small "blast" tag and are picked in Email blasts). Old HTML templates show a "Convert to plain words" link. Send deal keeps its template picker and no longer lists archived ones. The old /templates/new form is gone (it redirects to the windows).
## 2026-09-16: templates the HubSpot way

- Jonathan's brief: a clean email template that looks exactly like an email he would send, with tokens he can drop anywhere to pull a fact from the ticket; RJL CA tokens all from the deal, RJL Israel tokens split by project, apartment and house.
- **TokenEditor** (src/components/token-editor.tsx): a contentEditable email body (and a single-line variant for the subject) where every `{{key|fallback}}` shows as a light blue chip reading "Deal: Sponsor" or "Contact: First name"; the chips are non-editable and travel with the text; bold, italic, underline and bullets on a small toolbar; "Insert a token…" drops a chip at the cursor; paste is plain words only. Storage stays HTML with `{{tokens}}`, so the send pipeline (renderTemplate, toHtml, signatureFor) is unchanged. Old HTML templates open as they are, with their tokens as chips.
- **RJL CA** Templates > Email templates: one window per template (name, subject, body), tokens from src/lib/template-tokens.ts (`caTokens`: contact, deal, checklist answers from the live Required Items List, sender). Saves as you pause; x deletes (archived when campaigns used it); plus square adds one that opens with "Hi {{contact.firstName|there}},". Send deal keeps its template picker.
- **RJL Israel** Email templates in the left column (src/app/israel/templates): tabs Projects / Apartments / Houses, each with its own token set (`ilTokens`: name, address, developer, price in ₪ and $, price per m², rooms, sizes, floor, mirpeset, sukka-free facts, private pool, project pool, a bulleted "every filled fact" list, plus the person and the sender). Values come from src/lib/il-merge.ts (`ilUnitValues`, `renderIlTemplate`); the person's tokens are filled per recipient at send time (`ilMerge`, now in il-merge.ts so the browser can preview it). EmailTemplate rows with workspace IL and kind projects | apartments | houses.
- **Send apartment / house / project, step two** now starts from the template you pick for that kind (a Template dropdown at the top; "Built-in summary" is the fallback), with the unit's facts already filled in and the person's name left as a chip; the body is the same rich editor; Launch fills the name per person and adds the sender's name and RJL Israel under the email.
- **Templates are a list, not windows** (Jonathan, Sep 16, after seeing the windows): Templates > Email templates is a HubSpot-style table (name, used for, sends, created, modified, Duplicate, Delete) and "New template" opens a blank one straight away. Opening a template shows the email box as on the Send deal page (src/components/template-windows.tsx `TemplateWindow`): name, subject with chips, body with chips, toolbar, saves as you type. RJL Israel has the same under Templates in its left column, which is a dropdown like RJL CA's (Email templates, Required Items Lists), with Projects / Apartments / Houses tabs on the list. The Required Items Lists keep their windows.
- **The mirpasot count that would not collapse** (Jonathan, Sep 15 and 16): reproduced on a dev-only page (/dev/mirpasot, served only when NODE_ENV is development; src/proxy.ts lets /dev through on a developer machine). The component itself was fine. The cause was React's form reset after a `<form action>` server action: AutoSaveForm submitted through the form action, React reset the form when it finished, and a controlled `<select>` snapped back to the option it had when the page first loaded while the component's state kept the new pick. Picking 3 then showed the select at 1 with three rows on screen, and picking 1 again changed nothing because the select already read 1. AutoSaveForm now calls the action from onSubmit inside a transition, so React never resets the form. Verified in the browser: 3 to 1, 1 to 3, 3 to 1, three saves, the rows follow the select. The same reset was the reason SelectField exists; every autosaving form (apartments, houses, projects, companies, contacts, deals) benefits.
- **Do the RJL Israel lists equal a complete ticket?** (Jonathan, Sep 16). Checked field by field. Apartments and houses: every list line is a ticket column (or the developer link), and the conditional follow-ups (sukka area, pool size, ceiling per level, project name on yad rishona, renovation year on second hand) stay automatic. Three gaps closed: `mamad` was a Boolean defaulting to false, so "not stated" read as No and was never asked; it is nullable now on IlApartment and IlHouse (forms and intake write null when blank), existing false values were reset to not stated (3 apartments and 1 house had one), and a blank mamad is asked for. "Floorplan" joined the apartment and house lists (satisfied by the floorplan on the ticket, or a floorplan file on the email when there is one unit). "Project pool (yes or no)" joined the project list. Description and pictures are not list items (narrative and files). So: yes, everything on a list provided means a complete ticket, with the lists as Jonathan keeps them.
- **deals@rjlisrael takes instructions** (src/lib/israel-intake.ts `wantedKinds`): when a teammate forwards a deal, the words above the forwarded email decide what comes back. "project", "apartment(s)", "house(s)" (and the Hebrew) each ask for that list; naming one unit kind forces the units to it. The extractor now also returns the whole project (name, developer, address, total units, stories, parking, delivery, project pool, description) when the documents describe a building or development, whether or not units are listed; a project ticket is created or updated (matched by name, or street and city), the largest PDF becomes its brochure, units from the same email hang off it, and the reply carries a "Still needed" block per ticket kind from the projects list. A requested kind the documents do not describe gets a line saying so. Without an instruction the documents decide on their own.
- **Templates split by use** (Jonathan, Sep 16): Templates > Email templates lists only the one-at-a-time deal emails, with no "Used for" column. Blast templates live in Email blasts: New email blast is two steps, first a list of blast templates (Use, Edit, Duplicate, Delete, and New blast template), then the blast maker opens with the chosen email (src/app/campaigns/new/page.tsx, `?templateId=`; BlastForm takes `initialTemplateId`). A blast template's own page goes back to that list and offers "Start a blast with it".
- **A wall between the two sides** (Jonathan, Sep 16, after an Israel template opened inside RJL CA): Israel templates open at /israel/templates/[id] (their own page, Israel tokens, back to the Israel list); the RJL CA page at /templates/[id] sends an Israel template there and the Israel page sends an RJL CA template back, so no link lands on the other side. Every template action (new, duplicate, delete) redirects to the template's own side. Audit: no page under /israel links to an RJL CA route and no RJL CA page links to /israel; the only shared surfaces are the sidebar's two logo tiles and the login page.
- **Team lists stay on their own side** (Jonathan, Sep 16, after the Send deal CC picker offered RJL Israel and partner people): every RJL CA user list (owner dropdowns on deals, companies and contacts; the CC picker on Send deal; signatures in Settings; the mailboxes the follow-up and intro scans read) filters on `CA_TEAM` (src/lib/access.ts: users with an @rjlcapadvisors.com or @rjlequities.com address). `IL_TEAM` is the mirror for the Israel side. The Users pages under Settings are the only places both companies' people can be seen, each on its own page.
- **Send deal starts from a chosen template** (Jonathan, Sep 16, "the email generator has gotten worse"): the General email always was the chosen template with the deal's facts filled in (renderDealEmail, no rewriting), but the picker was a small dropdown in a corner and templates saved from the new editor came out as one `<div>` per line that the email styling ignored, so they rendered cramped. Now a Template card sits under Attachments with the chosen template's name, "Change template" opening the list of deal templates as buttons (open by default until a template has been chosen for the deal), and "Edit templates". `editorBlocksToParagraphs` (src/lib/merge.ts, used by toHtml) turns editor output into paragraphs: each non-empty top-level div a `<p>`, blank divs dropped, lists kept; the sign-off strip understands `<div>` too; lists get the email margins. Plain-text and older HTML templates render as before. The Israel compose page has had its template dropdown since the templates went in.
- **Sourcing and seller dropdowns are HubSpot's lists, word for word** (Jonathan, Sep 16: "they are dropdowns for a reason; I can't have you add random stuff"). SOURCING_OPTIONS and SELLER_PROFILES in src/lib/taxonomy.ts are the option lists of HubSpot's "On or Off Market?" and "What kind of seller?" properties (labels as written there, odd ones included). Nothing gets added to them. The deals@ extractor treats both as enums (one of the options or blank, never a sentence; ENUM_DETAILS in src/lib/intake.ts) and the regex fallback writes "on-market" / "completely off-market" only. On Sep 16, 27 deals were normalized: HubSpot values became labels, and 23 deals had free-text sentences the extractor had written into these fields moved into a NOTE on the deal ("How the deal was sourced (from the sponsor's materials): ...") with the field blanked. A different wording for one email is edited on Send deal.
- **Send deal editor** (Jonathan, Sep 16): attachments carry a file-type icon (Excel green, PDF red, Word blue, PowerPoint orange, pictures violet). The email box has a toolbar (bold, italic, underline, bulleted and numbered list) and shows Calibri 11 with real bullets; Tailwind's reset had hidden list bullets on screen. Templates imported from HubSpot carried font-size and font-family spans that fought Calibri 11 in the email; stripFontSpans in src/lib/send-deal.ts removes them at render, and lists carry list-style-type inline so Deal Metrics is bulleted everywhere. The toolbar also has Font (Calibri, Arial, Georgia, Tahoma, Times New Roman, Verdana) and Size (9 to 18 pt) selectors that apply to the selected words or what is typed next, shown for the text at the caret; size goes in as a span with a pt value so Outlook honors it. Everything without an explicit choice goes out as Calibri 11pt: the wrapper div and every paragraph carry it inline (FONT in src/lib/send-deal.ts). Only HubSpot's exact leftover wrappers (14.6667px, sans-serif, black) are stripped, so a font chosen in the editor survives.
- **Emails read like Outlook, never like HubSpot** (Jonathan, Sep 16: "the actual font was better before the templates; it looks like a HubSpot email"). src/lib/email-html.ts is the one place: `cleanTemplateHtml` strips what a HubSpot export leaves (the color/14.6667px/sans-serif span wrappers, dir="auto" divs, hs-trailingbreak, <p style="margin:0"> blank-line paragraphs, <li><p>, &nbsp;) and `outlookHtml` puts Calibri 11pt inline on every p, li, div, td, ul and ol with Outlook's own spacing (10pt after a paragraph, none between bullets: Jonathan, Sep 16, saw an extra space between Deal Metrics bullets; list items carry margin:0 and any paragraph inside a list item is unwrapped, styled or not), because Word's engine falls back to its default font on any block that does not state one. Deal emails (renderDealEmail) and blasts (campaign-render) both go through it; a font or size chosen in the Send deal toolbar stays because it is its own span with an explicit value. The stored templates were cleaned once with the same function so the template editor shows what goes out, and both editors (template page, Send deal) display Calibri 11pt.
- **Requested equity amount: 90% of total equity, always a round number** (Jonathan, Sep 15 and 16). `roundAsk` in src/lib/intake.ts rounds to whole millions from $5MM up ("12MM"), half millions between $1MM and $5MM, hundred thousands below; `applyDealRules` sets requestedAmount = roundAsk(0.9 x total equity) on every deal deals@ extracts (equity = total equity, or total capitalization minus total debt), and rounds a stated ask when no equity figure exists. Debt requests are untouched. The extractor's prompt says the same so it does not fight the rule.
- **Underwriting rules live in the CRM** (Jonathan, Sep 16: "build a place where these rules are held; when mistakes are made we notify you and you edit the rules until we tweak it to perfection"). Settings > Underwriting rules (/settings/underwriting, under Users in the Settings dropdown) is one window, one rule per line, saved as you type into the `Setting` table (key underwritingRules). src/lib/underwriting-rules.ts holds the seed (DEFAULT_UNDERWRITING_RULES, every rule given through Sep 16), the FORMAT_RULES the schema depends on (in code, shown read-only), and BUILT_IN_RULES (what applyDealRules and the sanitizers enforce, shown read-only). The extractor's system prompt is built per run by `systemPrompt(rules)` in src/lib/intake.ts from `loadUnderwritingRules()`; the old SYSTEM constant is gone. **Working rule for Claude:** when Jonathan or Aviel report a misread deal, the fix goes into this page's text (and, if the seed should carry it for a fresh database, into DEFAULT_UNDERWRITING_RULES), not into a hard-coded prompt; formulas that must never drift stay in code and get a BUILT_IN_RULES line.
- **Check size shows as one precise range everywhere** (Jonathan, Sep 16: the companies list still listed "$8-10MM, $10-15MM, $15-20MM"). `checkLabel` in src/lib/ranges.ts renders "$8MM to $20MM" (or "$100MM+") from checkMinMM/checkMaxMM, falling back to the range the legacy buckets cover for records never edited on the slider. Used on the companies list (column "Check size"), the contact page and the dashboard's Data updates proposals. The bucket list (checkSizes) stays in the database only as a derived field for matching and the old imports; nothing displays it.
- **Agreed groups stay editable after the letter is signed** (Jonathan, Sep 16, Carderock | Gateway Shopping Center: he marked the letter signed with all 36 groups, then the sponsor's cross-offs came and the card had locked every checkbox). The Engagement card (src/app/deals/[id]/engagement-card.tsx) now enables its checkboxes and the add box in both states; after signing the button is "Save agreed groups", which calls `finalizeEngagement(..., { markSigned: false })`: same removals and additions, agreedGroups updated, an "Agreed groups updated after signing" note, no second signed stamp and no stage change. Groups the deal already went to (status above 1) still cannot be unticked there; they are handled on the progress report.
- **Jonathan's full underwriting rules document (Sep 16) is the house rules text** under Settings > Underwriting rules and the seed in src/lib/underwriting-rules.ts (41 rules, his wording). Two of his rules the extractor cannot satisfy by itself are resolved in FORMAT_RULES, which follow the house rules in the prompt: display formatting ($, commas, two-decimal %, the x on multiples) is applied by the CRM when it shows or emails values, so numbers stay plain in the extraction; Fireflies is searched by the CRM in its own step after extraction (src/lib/deals-inbox.ts), not by the model. His rounding convention replaced the earlier one: whole millions from $5MM, half millions below (roundAsk), and requestedAmount never equals total equity.
- **Agreed groups: nothing greyed** (Jonathan, Sep 16, Gateway): a group the deal already went to (Farallon, Arc Capital) can be unticked like any other; "having sent it before doesn't mean I won't send it again." A struck group's rows leave the deal whatever status they reached. The sponsor's redline email (Jeff Fabrikant, Sep 16 14:15) says "remove the groups with strike-thru below", but the strike-through formatting does not survive in the message body Graph returns, so which groups were struck has to come from Jonathan.
- **Deal Metrics all bold on Carderock (Sep 16)**: the template was fine; the saved General email on the deal carried what Chrome's execCommand("fontSize") left after the Size selector was used over the list: a <strong> stretched around the whole <ul>, styled <strong> tags and <span style="font-weight: bolder"> around every value. Fixes: the Font and Size selectors now set style on the paragraphs and bullets the selection touches (blocksInSelection in send-client.tsx) and never rewrite inline tags; cleanTemplateHtml strips those artifacts at render (a strong/b wrapped around a list, styled strong tags, font-weight bolder/normal declarations); the deal's saved General and its one firm draft were cleared so the page renders fresh from the template. Rule: never use execCommand fontSize/fontName on the email editor.
- **Total acres on every deal** (Jonathan, Sep 16): the About this deal card shows "Total acres" under Square feet on every deal, not only Land. Acreage is one number: `acresNumber` in src/lib/intake.ts turns the extractor's wording ("3.1 acres, approximately 134,000 SF of land", ".73", "730,371 sq ft") into "3.1", "0.73", "16.77" inside applyDealRules, and the 25 stored values that held sentences were normalized the same way on Sep 16 (a portfolio parent with a criteria range went blank). The Development Required Items windows gained "How many acres does the site sit on?" after the square footage line so development sponsors are asked too. Lesson from the cleanup: match deals by id, never by a name fragment (a fragment hit "Prospect | Greyhill" and "Dunleer | Hopkins" instead of the intended deals; both were restored from HubSpot's acreage properties).
- **Why Carderock had no year built (Sep 16)**: the extractor's own confidenceNotes said the Excel model "was truncated in the material provided and only showed revenue and expense cash flow rows". Two caps did it: src/lib/attachments.ts read at most 400 rows per sheet and assembleDealText gave every attachment an equal slice of a 180,000-character budget, so the monthly cash flows crowded out the Building Information block. Now: sheets are ordered summary/assumptions/inputs/sources/property/building/rent roll first and cash flows/schedules last, each sheet is read whole (up to 3,000 rows, runs of empty cells collapsed), the budget is 600,000 characters, and models are read whole (sharing up to 70% of the room) before OMs and decks take what is left. A house rule was added telling the extractor where year built, occupancy and acreage live in a model. Earlier deals extracted under the old caps (everything before Sep 16) may be missing the same facts; forwarding the model again fills the blanks.
- **LTV and LTC are calculated on the ticket** (Jonathan, Sep 16): LTC = total debt / total capitalization, LTV = total debt / purchase price (not shown on developments). The About this deal card shows them as calculated rows (deal-form.tsx `pctOf`), never as inputs; saving the ticket stores both from the dollars on it (actions.ts `pctCalc`, only when the form posted totalDebt), and every existing deal with a debt figure was recomputed on Sep 16 so templates ({{deal.ltv}}, {{deal.ltc}}) read the same numbers the card shows.
- **Lender type is HubSpot's legacy list** (Jonathan, Sep 16: "BridgeInvest is not a lender type but a specific lender"). LENDER_TYPES in src/lib/taxonomy.ts is HubSpot's "Lender Type" options word for word: (Agency), (Fannie Mae), (Freddie Mac), (Life Co.), (Bank Execution), (Credit Union), (CMBS), (Bridge), (Seller Financing). The extractor's lenderType is an enum of those or blank; a lender's name goes to details.lender (the Lender field). Stored values were mapped on Sep 16 (Agency -> (Agency), Bridge Lender and Debt Fund -> (Bridge), Bank -> (Bank Execution), the Fannie/Freddie variants -> their option); two "construction lender not selected" sentences moved to the Lender field. Same rule as sourcing and seller: nothing gets added to these lists.
- **LAUNCH never fails silently** (Carderock, Sep 16: "tried to launch and it's not working"; no DealLaunch row was ever created, the render worked, so the click stopped in the browser). The button no longer uses window.confirm (blocked in some windows): the first click arms it and the note says what will go ("Ready: 34 individual emails, one every 30 seconds, 3 attachments each. Click LAUNCH again to send"), the second click within ten seconds sends. Every early stop is a note: nothing ticked, General still rendering, firms with nobody picked (listed), a server error (caught and shown, "Nothing was sent"). When a launch "does nothing", read the note under the button first, then check DealLaunch rows for the deal.
- **The dead LAUNCH button (Carderock, Sep 16, second report)**: the deal's saved Send state held `include: []`, so every firm was left out, itemsToSend() was empty and the button was disabled. An empty saved include list now means everyone (savedInclude fallback in send-client.tsx), the deal's list was reset to its 26 unsent firms, and LAUNCH is never disabled for "nothing to send": it takes the click and the note says why. Preview-to-me working while LAUNCH looks dead points at the include list.
- **A firm that already got the deal can get it again** (Jonathan, Sep 16, Carderock: "the deal has changed since last time; even groups who saw it the first time should see it this time"). On Send deal, firms sent earlier start off with a "sent earlier" tag; the + turns them on ("sending again") and they go out with the current email like everyone else. The queue does not care about status (a row's status only rises, so Farallon at 8 stays 8). Arc Capital Partners and Farallon were turned on for the Gateway send.
- **Re-reading a deal's email** (Sep 16): `rereadDealFromIntake(dealId)` in src/lib/deals-inbox.ts finds the deals@ message behind a ticket by the Internet Message-ID kept on DealIntake, reads it and its attachments with today's extractor and model reading, and merges through mergeIntoDeal (a model's numbers replace, narrative fills blanks, a "Re-underwritten from" note lands on the deal). mergeIntoDeal now recomputes LTV and LTC whenever it changes debt, price or total capitalization. Run on Carderock | Gateway on Sep 16: total capitalization matched the model (Jonathan's own question about total sources turned out to be future funding), year built 1951 came in from the model's Building Information, both interest rate scenarios were written, Year 1 cap rate 5.87% filled, and stabilized cash-on-cash moved from 4.91% (OM) to 8.23% (model). No UI for it yet; it is a script call.
- **Why 13 of 28 Gateway emails failed (Sep 16)**: every email re-uploads its attachments to the sender's mailbox (9.9 MB each here: the OM PDF and the 6.6 MB model), and after the first fifteen Microsoft Graph answered HTTP 429 "ApplicationThrottled: Application is over its IncomingBytes limit" on the upload. The queue then marked each remaining email FAILED within a second of the last. Fixes in src/lib/launch-queue.ts: the gap between emails grows with the attachment bytes (`gapForBytes`: 30 seconds or about 40 MB per five minutes, whichever is longer; 9.9 MB gives roughly 75 seconds), a throttled email goes back to QUEUED with a `notBefore` hold (5 minutes times the attempt, up to 30, max 8 attempts) and the pump stops for that pass instead of hammering, and `retryFailed` puts a deal's failed emails back. The Send page shows the hold ("Microsoft paused attachment uploads... resumes at 4:12 PM") and a "Retry failed (n)" button. DealLaunch gained notBefore and attempts. The 13 were requeued and sent the same afternoon. If a launch carries big files, expect it to take longer, not to fail.
- **One file per name on a deal** (Jonathan, Sep 16: forwarding the same deal to deals@ again stacked the same attachments as duplicates under Attachments). recordDealFiles in src/lib/deal-knowledge.ts now looks for a same-named file on the deal first: if there is one, it is re-pointed at the newest copy (bytes come from the latest email) instead of a new row being added; only a new name creates a row. 24 duplicate rows across 6 deals were removed on Sep 16, keeping the newest copy of each. The Send page's dealFiles already deduplicated by name; the deal page lists DealFile rows directly, so the fix had to be at the source.
- **"This is an acquisition. Please treat it as such."** (Jonathan, Sep 16, Bethesda/Chevy Chase): the note above a forward can now say what the deal is. src/lib/forwarder-notes.ts reads strategy (acquisition / existing asset / recap / refi -> Acquisitions; development / ground-up / construction -> Development) and an asset class named in the note, and writes them on the ticket after the extractor and merge have run, so the teammate's word wins. Second cause of the wrong list: the Bethesda ticket had no strategy at all (a merged follow-up never set one), and applicableItems with a null strategy returned both lists, so the reply carried the development questions. A null strategy is now the acquisitions list; Development only when someone said so. The Bethesda ticket was set to Acquisitions and a corrected summary reply was sent on its thread.
- **OneDrive / SharePoint links in deals@ emails** (Jonathan, Sep 16, MLG's folder link on DRA | MLG): the CRM opens them through Graph's shares API with the app token (anonymous "Anyone with the link" links, and links inside RJL's own tenant, whose SharePoint the app can read) and falls back to a direct download. A link shared with specific people in another company's tenant (mlgcompanies-my.sharepoint.com answered with a sign-in page; Graph said "the sharing link no longer exists") cannot be opened by anything the CRM can sign in as, and there are no delegated user tokens to try. So src/lib/cloud-links.ts now recognizes the restricted case (Graph 401/403 or a login redirect) and the reply says plainly: shared with specific people only; ask the sender for an "Anyone with the link" link, or download the files and forward them to deals@ and the ticket picks them up. Jonathan's own OneDrive returned itemNotFound through Graph (no personal drive under that account), SharePoint sites are readable.
- **Return profile never came over from HubSpot** (Jonathan, Sep 16: the Core / Core+ filter is very important to him). The Sep 3 companies export the CRM was built from had no "Return Profile" column, so scripts/import-hubspot.ts never mapped it and only 5 of 1,314 criteria rows had one. Fixed three ways: `normalizeReturnProfiles` in src/lib/taxonomy.ts maps HubSpot's "Light Value-Add" / "Heavy Value-Add" / "Core Plus" spellings onto the CRM's list; the import maps the column whenever an export carries it; and all 150 HubSpot companies that have a return profile (pulled through the HubSpot connector on Sep 16, kept in scripts/_return-profiles-hubspot.json) were written onto their companies' criteria by hubspotId. Investor search filters on it (Return profile), and the criteria form's multi-select edits it. Companies with no return profile in HubSpot have none here either; that is a data gap to fill in HubSpot or on the criteria form, not an import bug.
- **RJL Israel projects: the same filter rail as apartments and houses** (Jonathan, Sep 16): src/app/israel/projects/filters.tsx and page.tsx filter in memory (the table is small) on search text, city, neighborhood, developer, units, stories, parking and delivery year as sliders, pool, brochure on file, whether any units are listed, and a sort; the list shows Pool, Brochure and Units listed columns. **One project per name**: Laguna existed twice (Laguna Netanya, Laguna (לגונה)); merged into the first on Sep 16 (delivery 09/2029 carried over). The intake now finds a project by exact name, then street and city, then a boiled-down key (parentheticals and Hebrew dropped, city and filler words removed) so those spellings resolve to one project (`findProject` in src/lib/israel-intake.ts, used for the project ticket and for a unit's projectName).
- **Progress report Handle "disappears without an email"** (Jonathan, Sep 16): the Handle worked every time (a reply-all draft on the latest sponsor thread with the fresh report PDF was created in his Outlook Drafts), but reportsDue skipped any deal whose report draft was opened in the last two days, and the button's router.refresh() re-rendered the dashboard without the row, so the "Open in Outlook" button vanished before it could be clicked and the desktop auto-open (rjlcrm: link, only when set up on that computer) had nothing to fall back to. Now a deal with an open report draft stays in Ready for launch with an "Open" button and the reason "draft waiting in your Outlook Drafts"; Open reopens the same draft (and swaps in a fresh PDF if the report moved). The row leaves when the draft is sent (syncReportDrafts) or when "Sent already" is clicked.
- **RJL Israel dashboard, fourth window: Pricing updates** (Jonathan, Sep 17): every apartment, house and project asks for a pricing check every 90 days (PRICE_CHECK_DAYS in src/lib/israel-pricing.ts). The clock runs from `priceCheckedAt` (new column on IlApartment, IlHouse, IlProject), or from creation when never checked, so everything older than three months showed up on day one. The window lists due tickets oldest first with the current asking price and who to ask (the agent, else the developer), with "Pricing confirmed" (stamps the check and writes a note) and a link to the ticket. Saving a changed asking price on an apartment or house counts as a check (updateApartment / updateHouse). Tickets waiting for approval are not asked.
- **Developer logos on RJL Israel projects** (Jonathan, Sep 17): the projects list and the project page's Developer card show the developer's logo next to its name (CompanyLogo from the company's domain, else its website; the initial when neither is known), as the apartments list already did.
- **RJL Israel companies: roles filter picks several at once** (Jonathan, Sep 17): the single "Any role" dropdown became the same MultiSelect the RJL CA companies list uses; the page reads every role parameter and matches a company carrying any of them. The RJL CA list already worked this way (ListFilters).
- **RJL Israel project ticket, Sep 17 (Jonathan)**: delivery is a month picker (input type=month; the ticket keeps "MM/YYYY", which the extractor and the templates use, converted both ways in project-form.tsx and projectData), Total stories is a 1 to 100 dropdown, a Doorman yes/no row sits above Project pool (new `doorman` column, extractor field, "Doorman (yes or no)" on the projects Required Items List and windows), and the From the units asking price reads "3 rooms starting at X - until 6 room penthouses starting at Y" (`priceByRooms` / `priceRangeLine` in src/lib/project-ranges.ts: the cheapest unit at the fewest rooms, the cheapest at the most rooms, "penthouses" when every top-size unit is one; one size gives "4 rooms starting at X").
- **Rooms are a dropdown on apartment and house tickets** (Jonathan, Sep 17): 1 to 12 in half steps (3.5 rooms is a real listing), never typed. The extractor still writes the number it reads; the form shows it as the matching option (an odd stored value shows as its own option, per the Select's rule).
- **Delivery as a month picker on all three Israel tickets, and the project fills its units** (Jonathan, Sep 17). `toMonthInput` / `monthFromForm` in src/lib/israel.ts: the picker shows "MM/YYYY" or a year-only value (as January), and an untouched picker never rewrites the ticket (a hidden completionDateOrig rides along; when the picker still shows what the ticket had, the ticket's value is kept, so "1998" stays "1998"). Labels are standardized: the apartment's building rows read "Total stories" and "Total units" like the project's (the apartments Required Items window lines were renamed). `syncProjectToUnits` in src/app/israel/actions.ts copies the project's total units and stories onto its apartments and its delivery onto its apartments and houses, blanks only, on every project save, on an apartment or house save under a project, and after the intake files a project. Houses have no building units or stories of their own (a house's floors are its own levels), so they take only the delivery date. **Doorman**: the "separate box" Jonathan saw was the Required Items line rendered as a custom question in the minutes between adding the line to the window and deploying the field; with the field deployed, the line matches the built-in item and the Doorman dropdown sits above Project pool in the form.
- **Invite button (Jonathan, Sep 17: "I don't think Invite again is working")**: it was working; Resend shows seven "Your RJL Israel CRM login" emails delivered to Shawn within ten seconds of each other at 11:48, one per click, and the plain form gave no sign. InviteButton (src/components/invite-button.tsx) now shows "Sending…", then "Sent to shawn@… at 11:48" and stays disabled for half a minute, or the reason it could not send; sendInviteAction returns a result instead of throwing. Second fix the same hour: an RJL Israel invite went to the sign-in (RJL CA) address; an Israel-only invite now goes to the person's Israel-side address (sendInvite in access-actions.ts, `to`), and the row's note names that address. Shawn's Israel invite was re-sent to shawn@rjlisrael.com.
- **RJL Israel sign-in was asking for RJL CA first** (Jonathan, Sep 17). Cause: with no session at all, the gate (src/proxy.ts) sent an /israel page to the generic /login with only `next`; the Microsoft link then carried no business, so Microsoft signed people in with the account the browser already had (their RJL CA one), the session unlocked RJL CA only, and the gate then asked for the RJL Israel sign-in, which read as "log in to RJL CA first". Now the no-session redirect carries `business=IL` for an Israel page (CA otherwise) and the login page infers the side from `next` when a link has none, so the RJL Israel page, wording and domain hint come up straight away. The two sides remain separate sign-ins; someone with both still signs in twice.
- **Project ticket, Sep 17 (Jonathan)**: Gym yes/no under Project pool (`gym` column, extractor field, "Gym (yes or no)" on the projects Required Items List and windows), and a Broker window under Developer on the right (`agentContactId` on IlProject, relation IlProjectAgent, `linkProject` action, contacts marked Broker to pick from; the intake sets the sender's agent on a project it files).
- **Progress report header edits and passed groups** (Jonathan, Sep 17). Two problems on a hand-edited report. (1) A hand edit to Themes or Items Needed was being rewritten: every note or status change re-ran the summary writer, which wrote the sections from scratch, so the edit vanished and the PDF showed the regenerated text. Now saveTrackerMeta stamps `trackerManualAt` when the text changed, and generateTrackerSummary hands the current hand-written sections to the writer with the instruction to keep that wording and only drop answered or passed-group items and add what new notes bring; the PDF route sends Cache-Control no-store and is force-dynamic. (2) A group at Not A Fit or Pass is owed nothing: its LP asks are filtered out of the Outstanding requests before the writer sees them (passedNames match on company or contact name), and the prompt says so too.
- **An LP's pass wipes its ask list** (Jonathan, Sep 17, Nelnet on Kansas City Metro Retail Portfolio: Handle sent the sponsor five accumulated requests, most already answered, instead of the one thing Nelnet's passing email asked for, an intro call). mergeAsks was built to keep earlier asks until the sponsor answered them (so a scheduling note did not wipe Monday's questions); it now yields to the extractor's stance: on "pass" the open list is only what the passing email asks for, and with nothing asked the LP_ASK item closes (src/lib/momentum.ts). The Handle email (todo-actions.ts) checks the LP's tracker status: at Not A Fit or Pass it opens "Nelnet is passing on X, but asked for the below" and closes "Let me know if you are open to it and I will make the connection" instead of asking the sponsor to send materials. Nelnet's row was reset to the intro call by hand.
- **Ash Creek (Seastone, Des Moines) closed "November 2026"** (Jonathan, Sep 17; actual close Oct 30). Traced: the OM's "Business plan | Value creation timeline" page reads "Buy the two retail centers at $13.6M in November 2026", and the extractor took that as the expected close; the Oct 30 date was nowhere in what deals@ received (no "Oct 30" or "10/30" in the intake text). A house rule was added (Settings > Underwriting rules and the seed): expected close only from a stated closing date, PSA/LOI date, critical dates or the sponsor's email; a business plan or value creation timeline is an illustration, never a closing date; blank and flagged otherwise. The ticket was corrected to October 2026 with closeDate Oct 30, 2026 and a note.
- **The wrong Fort Lauderdale ticket** (Jonathan, Sep 17: "sponsor is Carderock but listed as an LP; Clarion listed as sponsor; many LPs missing"). There were two tickets. The mentions detector (src/lib/mentions.ts, which reads threads with Sponsor-role companies) took Clarion's out-of-office reply to the Gateway Shopping Center launch as Clarion mentioning a deal and created "Clarion Partners LLC | Fort Lauderdale Retail Acquisition" at 4:56 PM on Sep 16 (Clarion carries both Investor and Sponsor roles); six LPs' replies on that thread then matched the new ticket by its subject words and their statuses and notes landed there, and Carderock's Bobby Rassuli was filed as an LP on it. Sep 17: folded into Carderock | Gateway Shopping Center (statuses merged as the higher of the two, notes combined, emails, activities and the Arc Capital ask moved, the Carderock row dropped) and deleted. Guards added to the detector: a thread carrying one of our launch subjects ("... Opportunity in ... | $12MM of ..."), any auto-reply, or a company that is an LP on a live deal writing on an "Opportunity" thread is marked read and skipped.
- **deals@ went silent on a three-deal forward** (Jonathan, Sep 17, Jordan Yarosh's three hotels). The forward arrived 2:41 PM; tickets were made at 2:42, 2:44 and 2:45 (each extraction with pitch book, market study and proforma PDFs takes over a minute), and the function's five minute limit ended the run before the combined reply and the mark-read. Every later sweep then saw the first ticket's intake row and answered "already processed", so no reply ever went and the message stayed unread; parallel webhook runs also left stray PENDING intake rows. Fixes in src/lib/deals-inbox.ts: a PROCESSING claim row is written before any work (a parallel run skips), a claim older than eight minutes is treated as a dead run and resumed with the parts already ticketed reused by their keys, a processed message that is still unread after five minutes gets its reply sent by `finishUnreplied`, and the webhook and mail-sync routes allow 800 seconds. The Yarosh reply was sent the same afternoon through that path and the five stray rows removed.
- **Faded Handle on LP follow-ups** (Jonathan, Sep 17, Marble: Textstone, RDS, Lokre). A Handle is disabled when the row's person cannot be written to. Textstone and RDS sit on "(firm)" placeholder contacts with no email and the firms have no emailable contact at all; Lokre's row sat on Rolly Lokre, who unsubscribed, while Victor and Brendan Lokre have emails. Now createFollowUpDraft (src/lib/followup.ts) hands a blocked row (no email, unsubscribed, departed) to a colleague at the firm with an email, most recently active first, moves the row to that person and notes it; the dashboard row disables Handle only when nobody at the firm can be written to and then says why ("no email on file · add a contact", linking to the company page). Textstone and RDS need a contact with an email added.
- **Removing a user** (Jonathan, Sep 18): a Remove button on every row of both Users pages (RJL CA and RJL Israel; two clicks, not on yourself). `removeUserAction` sets active=false and clears invitedAt: currentUser() returns null for an inactive user so the gate turns them away on their next request, the sign-in callback refuses them, every mailbox reader (mail-sync, israel-mail, follow-ups, intros) filters active so their mail is no longer read, and they leave the owner, CC and Users lists. History they created stays. Removal is from the whole CRM, both sides; re-adding the person under Users brings them back.
- **The interest rate box takes numbers only** (Jonathan, Sep 24, 2026). The deal form's Interest rate is a numeric box; the % is added on save and the value is stored as "6.75%" with no words (`cleanInterestRate` turns a bare number into a percentage and drops fixed/floating; `interestRateNumber` reads it back). Fixed or floating, spreads and second loans belong in the debt terms text; tickets whose rate was only a spread ("SOFR + 375") had it moved there and the box cleared.
- **The interest rate is one simple figure** (Jonathan, Sep 24, 2026). `cleanInterestRate` in src/lib/rates.ts keeps the first all-in percentage with its fixed or floating word ("6.75% fixed", "6.65% floating"), or the spread ("SOFR + 285") when no all-in rate is given, and drops everything else ("10.0% construction loan rate; preferred equity return left open" became "10.0%"). It runs on the extractor's answer, on every ticket save and on the Fireflies fill; the underwriting rule and the field description say the same. 24 tickets were rewritten once, two by hand (Boston Andes 7.18%, Project Barclays SOFR + 375).
- **Debt Fund is a lender type** (Jonathan, Sep 24, 2026). "(Debt Fund)" sits in LENDER_TYPES between Bridge and Seller Financing on every deal ticket, the Fireflies fill and the extractor, whose rule now reads a debt fund as "(Debt Fund)" and a bank bridge loan as "(Bridge)".
- **A condo's pref LTV is on its gross sellout** (Jonathan, Sep 24, 2026). A condo development has no purchase price to measure the last dollar against; its terminal value is the projected gross sellout. `prefMetrics` divides the last dollar by `projectedSellout` on Condo (`prefLtvBase: "sellout"`), by purchase price otherwise; the ticket labels the line "Pref LTV on gross sellout", the deal email metrics print "(x% LTV on gross sellout)", `{{deal.prefLtv}}` carries the same figure, and the Condo Pref Equity Development template's Last Dollar Exposure line now shows it.
- **Engagement picks are remembered; a second letter only adds** (Jonathan, Sep 24, 2026, the Certes list sent twice). The group picker on the engagement page keeps the ticks in the browser per deal (`engagement-picks:<dealId>`) until Done drafts the letter, so leaving the page loses nothing; groups already on the deal's report start ticked (the page passes `onReport`), the counter reads "79 already on the report · 4 new", and "Clear new ticks" drops the additions. The letter always adds rows and never removes them (`createEngagementDraft` creates only missing rows), so the report and the send list are the union of every letter: the second, fuller list. `details.engagementGroups` is rewritten with the latest run's names and is informational; the rows are the truth (`engagementGroups()` in send-deal.ts reads them).
- **"Expected close" on the request lists is now "Critical dates"** (Jonathan, Sep 24, 2026). The checklist item `timeline` reads "Critical dates (expected close; LOI, PSA, due diligence and hard money dates)" in the code default and in every stored CA Required Items window (all eight reworded); the extractor question asks for the closing date and where the deal stands with dates. The ticket column is still `expectedClose` (the deal form labels it "Critical dates: expected close"), so the reply, the checklist and the email merge field are unchanged underneath.
- **A forward that matches a ticket we already have fills that ticket** (Jonathan, Sep 24, 2026, Park Station). `createDealFromIntake` found the "Certes Partners | Park Station" stub (a Deal Mentioned record made from an email that only named the deal), marked the intake converted and returned, so the teaser and the model were read but none of the figures landed and the stub stayed at Deal Mentioned. `fillDealFromExtraction` now writes every blank column and every missing checklist detail from the extraction onto the matched ticket (values already there stand; `dealDataFrom` is the one mapping both paths use) and moves a Deal Mentioned stub to Deal Received; the intake note says what was filled.
- **Companies list: Deal locations after Asset classes, editable in place** (Jonathan, Sep 24, 2026). The column shows the criteria free text (`InvestorCriteria.geographyNotes`); click to edit (`LocationCell`, Enter saves), `setCompanyGeography` writes it and the region tags it maps to (`normalizeGeographies`), with the same rule as asset classes: sponsors directly, investors by Jonathan or as a proposal for his approval. Check size moved one column right. Call matching for the Fireflies enrichment was tightened the same day: emails match anywhere, a firm name as whole words in the title or participants, people and property names only as whole phrases ("nathan" inside jonathan@ had matched every call).
- **Fireflies calls fill the ticket, not only the bio and business plan, and calls after intake are read too** (Jonathan, Sep 24, 2026). `enrichFromCalls` (src/lib/fireflies.ts) used to rewrite the business plan and sponsor bio and file Q&A facts, and ran only at intake. It now also fills blank ticket fields the calls settle (expected close, unit mix, year built, on or off market, lender type, loan term, interest rate) and answers open checklist questions into `details` (sourcing, seller profile, GC bio and contract, shovel ready, construction loan timing, Opportunity Zone, acreage, insurance and taxes and the rest), blanks only, never numbers that belong to the model. `Deal.callsEnrichedAt` records the last read; the mail-sync cron runs `enrichActiveDealsFromCalls`, which reads the calls again for any live deal whose sponsor has a call newer than that (one transcript fetch per pass, a few deals per pass).
- **Developments ask for land acreage** (Jonathan, Sep 24, 2026, the Mann Brothers deal). The acres item applied to Acquisitions only; a development read the Development window, and the Condo and residential Development windows made this week had no acreage line, so a Multifamily development never asked. The default item now covers both strategies (development wording "Land acreage (how many acres the site sits on)") and the line was added to the Condo and residential Development windows; the default and Land windows already carried it.
- **Intros are caught in both subject styles** (Jonathan, Sep 24, 2026). The intro scan (src/lib/intros.ts, run by the mail-sync cron and after each mail sync) only took Sent Items whose subject started with "Intro", which is Jonathan's style ("Intro - ARC | Carderock"); Aviel writes "Stablewood | Cross Development - Intro", so none of his intros were recorded and none could come back under Intros to reconsider. The scan now pulls every sent subject containing "Intro" and `isIntroSubject` keeps the two styles (and "Intro: A | B") while dropping replies, forwards and calendar traffic; `parseParties` reads the two firms from either. A rescan recorded the missed intros.
- **Unit mix is asked; the webhook reply reads every figure the ticket has** (Jonathan, Sep 24, 2026, the Bay Harbor condos). Unit mix was a ticket field with no Required Item behind it, so no reply ever asked for it: "Unit mix (bedroom types)" is now a default checklist item (core `unitMix`) for the residential classes and Condo, added to the residential and Condo windows, and a Development window for the residential classes was created so residential developments ask it too. The webhook intake reply (src/app/api/inbound/route.ts, the Resend path) lists `missingItems(extracted)`, which read the extracted deal through `toChecklistDeal`; that mapping lacked yieldOnCost, the sellout figures and unit mix, so the reply asked for a projected sellout the extractor had already read. The mapping now hands the checklist every core figure.
- **Investor search filters by where the firm sits** (Jonathan, Sep 24, 2026). The Deal specs rail has a Company location block: a state pick (the states on file across investor company cards) and a city box, matched against the company's own city and state (not its deal geography, which stays the Deal locations column). With a deal chosen, a "Near <city>, <state>" link applies the deal's state in one click, so investors in a deal's area can be found. The results table shows a Based in column.
- **Acquisitions pipelines are lists; the Deals board is its own page** (Jonathan, Sep 24, 2026). The sidebar has Deals (the board: properties carrying the Deal call result, stages across columns, editable on the board; /acquisitions/deals) and Pipeline with Buyers Pipeline, Operators Pipeline and Deals Pipeline always open under it (/acquisitions/pipeline/{buyers,operators,deals}). The three pipelines are the same sheets as Contacts and Properties (columns and rows shared from contacts/columns.ts and properties/columns.ts; the Deals Pipeline is the three-pane Properties sheet) holding only what was sent to the pipeline: `AqContact.pipelineAt` / `AqProperty.pipelineAt` (set by the one "Send to pipeline" token on the contact card's Role group and in the property ticket header, `PipelineToggle`; Remove clears it; the record stays where it was), Buyers filtered to the Buyer role and Operators to the Operator role. Each row has a Priority 1 to 5 (`pipelinePriority`; 5 highest) beside the token and as a grid column, and the lists sort highest priority first, then newest on the pipeline. The Contacts and Properties sheets carry Priority and In Pipeline (Yes/No) columns too. The buyer and operator stage boards from Sep 23 are gone (their `buyerStage`/`operatorStage` columns stay unused); Operator Pipeline Status (Pipeline/Corporate/Not interested) is a separate operator field.
- **The deals@ reply and Send deal start from the template that fits the ticket** (Jonathan, Sep 23, 2026). `templateForDeal` in src/lib/deal-template.ts picks the stored deal template by name from the ticket: Senior Debt → "Deal Template For All Debt Deals", Fund Investment → "Fund Raise Template", Condo + pref + development → "Condo Pref Equity Development Deals", otherwise "Multifamily" (Multifamily, Build-For-Rent, Student, Senior) or "NON-Multifamily" + "Pref"/"JV" + "Development"/"Existing" + " Equity … Deals". The deals@ reply's "How it would read to investors" is that template filled from the ticket (greeting at its default, blanks where the ticket has no value yet, the template named under it); the house copy from deal-copy.ts stays the fallback. The Send deal page opens on the same template. The extractor has a condo rule (Settings > Underwriting rules and the code defaults): no yields, cap rates or cash on cash on a condo; projectedSellout, selloutPerUnit and selloutPerFoot from the sales schedule; and the code blanks those NOI figures and derives the per-unit and per-foot sellout when a condo comes in.
- **Condo developments** (Jonathan, Sep 23, 2026). Condo is an asset class (taxonomy, with Condominium / For-Sale Residential synonyms; asset profile per unit and per foot, no occupancy). A condo sells out, so NOI-based figures do not apply: the ticket hides Yield on cost at stabilization and Stabilized cash-on-cash and asks instead for Projected sellout (whole dollars), Average sellout per unit and Sellout price per foot (`Deal.projectedSellout/selloutPerUnit/selloutPerFoot`; the per-unit and per-foot boxes fill themselves from sellout ÷ units and ÷ square feet until typed over, and the save and the intake fill blanks the same way; `NumberInput` takes a driven `value`). The Required Items checklist excludes the yield item on Condo and carries the three sellout items for Condo only (defaults in src/lib/checklist.ts; Condo windows for Development and Acquisitions were created in the Required Items Lists). The intake extracts the three figures. The deal email's metrics show "Projected Sellout: $X ($Y per foot | $Z per unit)" instead of yield on cost and cash on cash. Pref metrics (`prefMetrics`): no going-in yield on the last dollar on any development, no stabilized yield on a condo, and a condo's stabilized basis on the last pref dollar reads per foot. Template placeholders `{{deal.projectedSellout}}`, `{{deal.selloutPerUnit}}`, `{{deal.selloutPerFoot}}` exist; the template "Condo Pref Equity Development Deals" is the multifamily pref development template with those in place of the yields and "Stabilized Basis on Last Pref Dollar per Foot: {{deal.basisLD}}".
- **Tracker rows: + adds, − removes after a second click** (Jonathan, Sep 23, 2026). The right-hand column of each progress report row is `RowActions` (src/app/deals/[id]/tracker/row-actions.tsx): + opens the investor picker right there (`TrackerContactPicker` takes `autoFocus` and `onAdded`), − turns into "Remove <firm>? Yes / No" and only Yes calls `removeTrackerRow` (Undo stays in the strip above the table for 30 days). The page file had been re-saved in the wrong encoding (the × showed as "Ã—", the dash as "â€”"); the characters are fixed and the BOM dropped.
- **The sponsor's PDF is the page, every time; the sponsor is never in Items Needed** (Jonathan, Sep 23, 2026, Galleria Trace). `buildProgressReportPdf` now uses the same `loadReport(dealId)` as the tracker page (sections brought up to date by the summary key, which only rewrites them when a row or a request changed), and reopening the Send to sponsor draft always swaps in today's PDF instead of only when an investor row had moved: the sections can change with no row moving, which is how a draft went out with EVCap at Taking A Look while the page said Pass. The summary writer (`generateTrackerSummary`) drops the sponsor's own company from the rows and from the asks, and the LP ask scan marks an email as "sponsor" when the deal it is pinned to is that company's own; sponsor asks already logged as LP asks were removed from every deal. The grey "Still blank on the ticket" line is internal and stays off the PDF.
- **Investor replies are read whole; a plus sign in a message id no longer hides them** (Sep 23, 2026, EVCap's pass on Galleria Trace). Every Graph lookup by internetMessageId (14 places: the LP ask scan, mentions, sponsor replies, follow-ups, send-deal, the deals inbox, to-dos) passed the id raw in the query string, so a Gmail id with a "+" turned into a space and the lookup found nothing; the LP ask scan then read only Graph's 255-character preview ("…the profile of a deal that would") and called EVCap's pass "0 asks", leaving the row at Taking A Look. The filter is now URL-encoded everywhere. The mail sync also keeps an inbound email's own text (what the sender wrote above the quoted thread, up to 6,000 characters) on the activity instead of the preview. The 98 plus-sign scans of the last 60 days were read again: EVCap moved to Pass with the reason, two other rows gained their asks.
- **Handing Acquisitions to Shawn; the wall between the three CRMs** (Jonathan, Sep 23, 2026). Shawn runs his own Claude Code on this repository for RJL Acquisitions; Jonathan keeps Capital Advisors, Israel and the shared code. The Acquisitions side is defined once in scripts/scope-guard.mjs (`src/app/acquisitions/**`, `src/app/api/acquisitions/**`, `src/lib/acquisitions*.ts`, `src/lib/aq-*.ts`, the `Aq*` models, appends to this file) and enforced twice: a Claude Code hook (.claude/settings.json → scripts/claude-scope-guard.mjs, active only when the environment has `CRM_SCOPE=AQ`, which Shawn sets and Jonathan does not) refuses edits outside it, `vercel --prod`, pushes to main and destructive database commands; and a GitHub check (.github/workflows/scope-guard.yml) fails any pull request from anyone but Jonathan (login RJLCapitalAdvisors) that reaches outside it, including a schema change to a non-Aq model. .github/CODEOWNERS makes Jonathan the reviewer of everything except the Acquisitions paths. Shawn works on `acq/<what>` branches with pull requests; ONBOARDING.md is his guide (shared as a Claude Code link). Still Jonathan's to do in the browser: GitHub Settings > Branches: protect main, require the Scope guard check and Code Owner review; Vercel account: add the GitHub login connection, then `vercel git connect` so merges deploy and pull requests get previews; invite Shawn to the repository and the Vercel team; give him the secrets and his own Anthropic key. Note: the repository is public on GitHub; Jonathan should make it private.
- **Acquisitions junk: phone numbers and properties** (Shawn via Jonathan, Sep 23, 2026). Settings on the Acquisitions side has Junk Phone Numbers and Junk Properties. Right-click a property's address (the ticket header, the Properties grid and panes, a Deals pipeline card) for "Send this property to junk…"; a confirm step explains what happens, then `junkAqProperty` sets `AqProperty.junkedAt/junkedBy/junkReason` and the whole card disappears from the Properties list, the map, the pipeline and the assistant (every property query filters `junkedAt: null`) but keeps its people, notes, transcripts and calls; the Junk Properties page restores it or removes it for good (second confirm). Right-click a phone number, on the contact card's phone inputs (the line under the caret in Other Phones counts alone) or on any number in a property's Owners and Operators windows, which now list every number of the person one by one, for "Send this number to junk…": `junkAqPhone` records it in `AqJunkPhone` (digits unique) and takes it off every contact carrying it, one line of a bulleted list at a time; the Junk Phone Numbers page lists them, takes a number typed by hand, and Forget releases one. A junk number is never written onto a contact again: `scrubJunkPhones` runs on the contact form saves and on grid cell edits, and imports (`runImport`, AQ) junk every number the sheet marks junk (`junkPhones` top level or per person; the assistant prompt's JUNK rule) and file rows marked junk under Junk Properties. Components: src/components/junk-target.tsx (`JunkTarget`, `phoneUnderPointer`), src/app/acquisitions/junk-actions.ts; the DataGrid takes `linkWrap` to wrap its link cell.
- **A project's address flows to its units, and the pin reads the street as typed** (Jonathan, Sep 23, 2026). `syncProjectToUnits` now copies the project's street to every apartment and house under it that has no street or a street without a house number, fills blank city and neighborhood, and clears their pin so it is looked up again. The geocoder (`cleanStreet` in src/lib/geocode.ts) drops the Hebrew in parentheses and "St.", cuts a range to its first number ("Eliezer Yaffe 6-8 (אליעזר יפה)" → "Eliezer Yaffe 6"), and falls back to the street name before the neighborhood; Mophet's pin moved from a wrong area match to the street itself.
- **Script-built sites are read as a browser shows them; the street field offers addresses that map** (Jonathan, Sep 23, 2026). mophetraanana.com serves only a loading line; its address ("Eliezer Yaffe 6-8") appears once the scripts run. The site reader (src/lib/web-pages.ts) now keeps a page's head text (title, description, Open Graph, structured data) and, when the static body is tiny or says it relies on JavaScript, fetches the rendered page through a public rendering reader (Jina Reader, `WEB_RENDER_READER` overrides the prefix, "off" turns it off; only the page's public address is sent). Re-reading the "Mofet Project" email filled Mophet's street, City Center and Gym. The street field on the apartment, house and project forms is an `AddressPicker` (src/components/address-picker.tsx): typing three letters asks /api/israel/address, which runs the same OpenStreetMap geocoder the map pins use (limited to Israel, with the form's city) and lists matching addresses; picking one writes the street with its house number and fills a blank city and neighborhood; street-only hits show greyed as "street only", and a typed street without a number gets an amber edge because it still counts as missing. Google Places can replace the geocoder when a key arrives. The project ticket's Developers card carries the same multi-pick as the unit tickets (`linkProject` takes `developersSet`), beside the one in the form.
- **An address counts only when Google Maps could find it** (Jonathan, Sep 23, 2026). `hasHouseNumber` in src/lib/israel.ts: a street without a house number ("Eliezer Yafe St.") is still missing on projects, apartments and houses, in Still needed, The Que and the deals@ reply (`blankOn` and the intake's `blankExtracted`). The labels read "Address (street and house number, enough to find on Google Maps)"; the stored windows were reworded. Mophet asks for its address again until the number is in.
- **Batch replies group what is missing by unit; Machsan yes or no; the total mirpeset sits under the count** (Jonathan, Sep 23, 2026). When one email yields several units, the deals@ reply lists the units once and then "Still needed across the units" with each item followed by the units lacking it, named by type code (degem) or by name, rooms and size when there is none: "Floorplan (missing for A-D, A-J)", "Asking price (NIS) (all 23 units)"; a single unit keeps the per-ticket list (`groupedMissing`, `unitTag`; `IntakeRow.degem`). The reply's footer points at The Que, not the old dashboard window. Apartments have Machsan? (Yes/No, `IlApartment.machsan`) above the machsan size: No folds the size and location away and stops them being asked; the Required Items default and stored window carry "Machsan (storage room): yes or no" above the size line; the intake fills it (a drawn, listed or priced machsan is Yes). The typed total mirpeset field sits right under How many mirpasot, since price per meter depends on that one number.
- **The total mirpeset size is the truth; the mirpasot are fitted to it** (Jonathan, Sep 23, 2026). `reconcileMirpasot(total, list)` in src/lib/israel.ts makes several mirpasot add up to a stated total exactly: a mirpeset with no size takes the remainder, otherwise the largest absorbs the difference. The intake applies it to what Claude reads against the plan's Balcony Area (and the prompt says the total matters more than the split); the apartment and house forms have a typed "Total mirpeset (m²)" beside the parts, with the parts' sum and any gap shown, and the save fits the parts to the typed total (blank total means the sum). Existing units were fitted once by script.
- **Project amenities are answered one by one; the unanswered ones are asked** (Jonathan, Sep 23, 2026). Ticking five amenities does not mean the other two are absent, so the project form shows every amenity with its own Yes / No / blank; Yes goes to `IlProject.amenities`, No to `IlProject.amenitiesNo`, and the doorman/pool/gym flags follow (`amenityFlags(yes, no)`, blank when nobody has said). `projectMissing` turns the Amenities line of the Required Items list into one ask per unanswered amenity ("Gym (yes or no)", "Doorman (yes or no)"), so the deals@ reply and The Que ask exactly those. The intake fills `amenitiesNo` from what the documents say the project lacks and is told never to guess either way.
- **Israel intake reads a whole project site, cuts each unit's plan page out as its floorplan, and reads a plan the way Jonathan does** (Jonathan, Sep 23, 2026, the Mophet website and decks). The site reader (src/lib/web-pages.ts) now follows every same-site page up to eight, the linked page and the front page first and pages whose link says units, plans, prices, project, complex, developer or location before the rest; legal, accessibility and other-language pages are skipped and the page header is kept (the Mophet facts, 176 apartments and 7 floors, sat on "The Complex", which the old keyword filter never followed). Each floor plan deck is rendered once; Claude gives every unit a `planPage` and the intake stores that rendered page (JPEG) as the unit's floorplan (`attachPlanPage`), so a 20-page deck yields one plan picture per ticket; the one-unit whole-file fallback stays. The prompt (FLOOR_PLANS) carries Jonathan's lesson on type UA7: the compass rose bottom left orients the page (north up, so bottom is south and left is west), the apartment faces every side with windows or a mirpeset, the side table gives Floor, Apartment Area and Balcony Area, the rooms badge gives rooms (bedrooms are rooms minus one), bathrooms are counted, and each outdoor area is its own mirpeset with the direction of the side it sits on and its size from the printed centimetre dimensions (366 / 160 is 5.8 m²), the balcony area left over going to the unmeasured one. The same lesson sits in Settings > Data rules (RJL Israel) where the team can edit it. Type codes match with or without the building letter ("UA7" and "A-UA7"; "A-D" and "B-D" differ). A project's facts flow to its units from src/lib/israel-sync.ts (`syncProjectToUnits`: total units, stories, delivery, developers), called after a project is saved by hand and after the intake touches one. Company names match without the Hebrew in parentheses (`companyKey`), so "Ramot Ba'ir" is not made twice. Projects, apartments and houses carry several developers: `developerIds` (JSON list, the lead first and mirrored in `developerId`), ticked by name on the project form and the ticket's Developers card from companies with the role Sponsor (Yazam); the intake fills `developerNames`; lists show " + Adi Capital" after the lead. The Required Items labels are dummy-proof ("Parking spots and configuration (how many; back to back or side by side; underground or open)", "Total stories in the building", "Asking price (NIS)"); the stored windows were reworded line for line. Apartments have Bathrooms under Rooms (1 to 10 in halves; `IlApartment.bathrooms`). The dashboard's Deals to be approved window is gone: The Que is the one place, a row an agent approved with data missing reads "Waiting on Jonathan" until his Approve. Mophet now: one project, one Ramot Ba'ir plus Adi Capital, 23 unit tickets each with its plan page, direction, floor and mirpasot.
- **Acquisitions has three pipelines: Buyers, Operators, Deals** (Jonathan, Sep 23, 2026). The sidebar entry is Pipeline with the three boards always open under it, like Owners/Operators/Buyers under Contacts; /acquisitions/pipeline opens Buyers. All three use the same board (src/app/acquisitions/pipeline/[pipeline]/page.tsx): one column per stage, a picker on each card moves it, Edit stages adds, renames, reorders and removes empty stages. Buyers holds every contact whose role carries Buyer, Operators every contact whose role carries Operator (card shows brand or company, phone, email, roles, the Operator Pipeline Status chip and the last call), Deals every property whose Call Result carries Deal. Stages are data per pipeline (`AQ_PIPELINES` in src/lib/acquisitions.ts; Settings aqBuyerStages, aqOperatorStages, aqDealStages; `getAqStages`/`saveAqStages`), placeholders until the first edit. A contact's column lives in `AqContact.buyerStage` / `operatorStage`: the contact form shows Buyer Pipeline Stage under the role and Operator Pipeline Stage in the operator details, the Contacts grid has both as editable columns, and moving a card (or picking a stage in the grid) turns the role on if it was off. Turning a role off clears its stage. Operator Pipeline Status (Pipeline/Corporate/Not interested) is separate from the stage and does not filter the board.
- **Israel units: name is the project, the type code is Degem, mirpasot can be None** (Jonathan, Sep 23, 2026). `IlApartment.degem` (and IlHouse) holds the developer's unit type code (UA1, D, PH4); the ticket form has a Type / Degem field after the apartment type, the Apartments list a Degem column, The Que shows it as a chip beside the name. The intake names a unit after its project only and puts the code in degem and the sizes in their fields; two units of one building are different tickets when their code, room count or size differ (`sameUnit`). The mirpasot count offers None: every mirpeset question folds away, the save writes mirpesetCount 0 with empty mirpeset fields, and `blankOn` does not count mirpeset items as missing on such a unit. The Que rows are one line tall again: a "N missing" chip with the items truncated beside it and the full list in the cell's tooltip. The zoom controls of the Israel Apartments, Houses, Projects and Que lists and the CA Active progress reports list sit in a strip above the table (a `zoom-tools` slot, as on the Acquisitions grids) instead of floating over the header.
- **Israel intake reads floor plan decks, websites; big files stream; amenities are one list** (Sep 23, 2026, Jonathan's Mofet decks). A PDF that is mostly drawings (little text per page, or plan/floor/type in its name; `isVisualPdf`) is rendered page by page with MuPDF (WebAssembly, src/lib/pdf-images.ts, `serverExternalPackages: ["mupdf"]`) and sent to Claude as pictures, one deck per call, results merged (`mergeExtracted`, a unit is the same only when name, rooms and size agree); Claude's own PDF reader had refused the decks ("Could not process PDF"). The system prompt carries FLOOR_PLANS (unit tables, compass rose for direction and mirpeset direction, one apartment per unit type per building named with type code, rooms and m², price list by type) and the Israel Data rules. Websites in the email (src/lib/web-pages.ts: the linked page and same-site pages about units, plans, prices; cloud, social and our own hosts skipped) are read as text attachments. Stored files (brochure, floorplans, photos, the CA PDFs) are served with `streamBytes` (src/lib/stream-file.ts): a Vercel function may not return a body over about 4.5 MB in one piece, which is why the 23 MB brochure showed "Failed to load PDF document"; a streamed body has no cap. Project amenities are one set on `IlProject.amenities` (IL_AMENITIES: Doorman, Pool, Gym, Jacuzzi, Sauna, Yoga/Pilates Studio, Co-working spaces) picked with checkboxes; doorman/pool/gym stay as Yes/No fields derived from it (`amenityFlags`) so older Required Items keys still work; the default project Required Items list has one Amenities item.
- **Projects from the intake wait in The Que too** (Jonathan, Sep 23, 2026): every IlProject the Israel intake creates (a project email, or a project named on an apartment or house) starts with pendingApproval true and is approved from The Que like the units; the Projects list shows approved projects only. The RJL CA Graph app cannot read OneDrive (no Files.Read.All): a link shared with specific people, and even Jonathan's own drive, come back access denied, so a folder link in a deals@ email yields nothing until Files.Read.All (application) is granted and consented in Azure, or the link is set to Anyone with the link, or the files are attached.
- **Israel deals@ opens cloud links and never loses a reply** (Sep 23, 2026; Jonathan's Mofet email carried only a OneDrive folder link, the intake read the bare text into an empty project and the reply never went out). `processIsraelMessage` now runs the same `findCloudLinks` / `fetchCloudFiles` as the RJL CA intake (OneDrive, SharePoint, Dropbox, Google Drive, Box; a CA-tenant OneDrive link is read with the CA Graph app) and turns each document into an intake file; a link that could not be opened is said so in the reply. A reply Microsoft refuses is kept on the IlInbound row (`pendingReply`, `graphId`) and sent by the next inbox pass (`sendPendingReplies`).
- **Israel deals@ replies copy Jonathan** (Sep 23, 2026): every reply the RJL Israel deals mailbox sends (ticket links, "could not find an apartment") carries jonathan@rjlisrael.com on CC, unless he is the sender. `ISRAEL_REPLY_CC` in the environment overrides the list (comma separated); set it empty to turn the copy off.
- **Progress report: the PDF is the page, and removals undo** (Jonathan, Sep 22, 2026). Themes and Items Needed used to be rewritten on every load (page, PDF, sponsor view) whenever any row had been touched, and the writer never words them the same twice, so the downloaded PDF differed from the page. Now `ensureTrackerSummary` rewrites only when the material changed: `Deal.trackerSummaryKey` holds a digest of every row's status and note plus the open LP asks, and an unchanged key means no rewrite; the PDF (`buildProgressReportPdf`) and the sponsor share page load the report with `refresh: false`, so only the tracker page and Refresh report rewrite. Removing a row (`removeTrackerRow`) keeps a `TrackerRemoval` copy for 30 days and logs it on the deal; the tracker page shows "Removed from this report" with Undo (row restored as it was) and ×. Adding a firm back (or any new firm) clears the LpAskScan rows for its emails on that deal, so the next Refresh report rebuilds its status and notes from the emails.
- **To do list on the RJL CA dashboard** (Jonathan for Aviel, Sep 22, 2026). A team member forwards an email to deals@ with a note on top such as "remind me to handle this", "to do", "don't forget" (src/lib/todos.ts `looksLikeReminder`, tested on the forwarder's own words above the forwarded header, or the subject). deals@ treats it as a to-do, not a deal: Claude writes a one-line item naming the person or firm involved and lists the people in the forwarded email, a `Reminder` row is kept for the forwarder (userEmail), deals@ replies "Added to your To do list", and the message is marked read. The dashboard shows a To do window (where Data updates sits for Jonathan; for Jonathan too when he has items) with the item, the people and the date, and one grey Dismiss button that marks it done for good (dismissTodoAction in src/app/todo-actions.ts). The older `Todo` model belongs to the To-do page and is untouched.
- **Acquisitions Properties sheet is three panes** (Jonathan, Sep 22, 2026; src/app/acquisitions/properties/panes.tsx): Property (every property field), Owner and Operator (every contact-card field of the first linked owner or operator, "+N" when there are more), one row per property across the three so vertical scroll is shared (scrollTop synced across the panes' scroll boxes, marked data-grid-scroll) while each pane scrolls sideways on its own. Cells in the Owner and Operator panes save to the contact (updateAqCell("contact", contactId, …)); a property with no linked owner or operator shows a dash and refuses edits with a pointer to the ticket. Zoom and Reset columns in the search strip act on all three. DataGrid takes `zoom`/`onZoom`, `showTools` and `title` for this.
- **Acquisitions: calls live on contacts; the property is the real estate** (Jonathan, Sep 22, 2026, superseding the owner/operator blocks on the ticket). The contact card carries the role (Owner, Operator, Buyer), every phone and email (primary, secondary, others, mailing address), operator details shown only with the Operator role (Operator Brand Name, Website, Operator Entity Name, Directory Operator Name, Store Phone from Google, Directory Operator Phone, Operator Total Locations; more fields to come from Jonathan's email), and the call: Last Call Date, Call Result (Callback with a target date that feeds the dashboard's Call Me Back and the Follow Up Date; Wrong number drops a phone), then Call Notes and Transcript cards newest first (AqNote/AqTranscript rows with contactId). The property ticket keeps address, city, state, county, business, asset type, parcel, physical facts, last sale and the Deal Pipeline tick; on the right, Location, Owners and Operators windows (linked contacts by role at a glance, New owner / New operator opens the contact form with the role ticked and links on save) and Companies. The old property owner/operator/call columns stay in the schema but are unused (data was moved onto the linked contacts once). The Contacts grid shows every contact field as a column (operator details first on the Operators view); Owners, Operators and Buyers stay open under Contacts in the nav. The dashboard's Call Me Back keys on contacts. The assistant's import treats owners and operators as contact rows with roles, linked to properties by address.
- **Acquisitions: Operator block, auto-made people, Map View, zoom** (Sep 22, 2026). The property ticket has an Operator block under Owner (operatorEntity, operatorName, phones, emails, mailing address). After every save (form, grid cell, import) `syncPropertyPeople` in src/lib/aq-people.ts turns the owner into an AqContact with role Owner at an AqCompany named after the owner entity, and the operator into a contact with role Operator at a company named after the operator entity (or the Current Business Name), links both to the property and carries phones and emails onto the contact; the ticket remembers ownerContactId/operatorContactId so edits update the same records. The Seller role is now Owner everywhere (data migrated). Map View (/acquisitions/properties/map, its own nav line under Properties; src/lib/aq-map.ts + acq-map.tsx) pins every property, filters by contact, company or operator (stacking), and Find an address drops a red star for any address with the closest properties still shown listed with distances; Leaflet on OpenStreetMap until NEXT_PUBLIC_GOOGLE_MAPS_KEY is in Vercel. Every data window (the Acquisitions grids and the RJL CA and Israel list tables) has a zoom control in its top-right corner (src/components/zoom-box.tsx, CSS zoom, remembered per window in the browser).
- **Progress report rules from Sep 22, 2026 (MLG, Corebridge, JDI and Ufund on Everett Mall).** The rules live in the CRM under Settings > Data rules (RJL CA), card "Progress report rules" (Setting `reportRules:CA`, defaults in src/lib/report-rules.ts); the list is read into the prompts that write notes (lp-asks) and themes/items (tracker-summary), and the mechanical ones are code: a pass read from an LP email always moves the row to Pass, whatever it said before (the old rule stopped at Intro Made, so MLG's pass after their intro call never registered); other stances only move a row forward and never past Intro Made. What an LP asks for is written on the row as "Requested: …" with the date (an acknowledgement alone is still no note; Corebridge's nine questions had been dropped with the "thanks"). Once a group passes or is not a fit the row keeps only the reason (`passReasonOnly`: the entries dated the day of the last one); earlier entries go to the deal log; applied once to every passed row. Notes print one bullet per entry (report view, PDF, tracker page edits one per line). Statuses keep their ids but print and sort by `rank`: Pass 1 and Not A Fit 2 at the bottom, Intro Made 8, and the new status 9 Term Sheet Issued at the top; groups at 7, 8 or 9 are owed nothing on Items Needed (`owedNothing`). Intro calls that happened (src/lib/intro-calls.ts, run with the mail sync and the cron): a Fireflies recording, or a past calendar meeting, with someone at the group and someone at the sponsor moves a Taking A Look or Interested row to Intro Made, drops entries dated before the call and writes "Intro call held with <sponsor> (date)". The Graph app has no Calendars.Read yet, so only Fireflies counts until Jonathan grants it in Azure.
- **RJL Israel: The Que and Map View** (Sep 22, 2026). The Que (/israel/queue, nav under Dashboard) lists every deal that came in and is not yet in the system, apartments, houses and projects together in the list-table shape (IlProject now has pendingApproval too). Approve on a row (queueApprove in src/app/israel/queue-actions.ts): complete data goes straight in; with data missing, Jonathan's own click admits it at once, anyone else's stamps approvalRequestedAt/By and the deal appears in the dashboard's Deals to be approved, where Jonathan's Approve (dashboardApprove) admits it whatever is missing or Back to The Que (dashboardDecline) clears the request. The dashboard window shows only those requested deals now, with a count link to The Que. Map View (/israel/houses/map, a Houses subnav beside the list) is Leaflet on CARTO/OpenStreetMap tiles (no Google key on hand; a NEXT_PUBLIC Google Maps key would let the base map switch), fed by loadMapUnits in src/lib/il-map.ts, which also geocodes up to 8 unplaced units per page load. One pin per address (a count when several units share it; colors by kind), hover lists each unit's internal m², mirpeset m² and ₪/m² with a link. Toolbar on the right: pan, circle an area (press and drag; the panel lists every unit inside), pick pins to compare; tick up to five of one kind and Compare opens /israel/apartments/compare or /israel/houses/compare.
- **The page header is one slim line on every side** (Jonathan, Sep 18, 2026): PageHeader in src/components/ui.tsx is about 44px, title and subtitle side by side, small action buttons; `compact` is accepted and ignored. Search bars sit in a `px-6 py-2` strip and list windows use `h-[calc(100vh-176px)]` (Israel list pages 168px) so the table gets the room. Keep new pages to this: no tall headers, no subtitle on its own line.
- **Acquisitions list pages are Excel-like grids** (Jonathan, Sep 18, 2026): Properties, Companies and Contacts use src/components/data-grid.tsx through src/app/acquisitions/grid.tsx. Drag a header to reorder columns, pull a header's right edge to resize (double-click resets), scroll sideways; the Properties grid carries every ticket field. Click a cell to edit in place (Enter or clicking away saves, Escape cancels, Tab moves right); dropdown fields edit as selects, roles as checkboxes, one-per-line fields as a textarea. Order and widths are remembered per grid in the browser (localStorage). Saves go through `updateAqCell(kind, id, key, value)` in src/app/acquisitions/actions.ts, which allows only listed fields and keeps the ticket's rules (LLC suffix, state codes, Callback fills Follow Up Date, company roles flow to contacts, a contact picking a company inherits its roles). Call Notes are AqNote rows shown in a Call Notes card under the About form (above Transcript), newest on top and bulleted, with the latest one as a read-only grid column and on the dashboard; the centre column of a property ticket is emails only.
- **Acquisitions property ticket fields** (Jonathan's list, Sep 18, 2026), left side in this order: Property Address (text; the Location map window top right, like RJL Israel, geocodes it through src/lib/aq-geocode.ts), City, State (dropdown), Current Business Name, Asset Type (Free-standing, Strip center, Other), Parcel ID, Property Owner (LLC appended by ensureLlc unless an entity suffix is already there), Owner Name, Primary and Secondary Phone, Other Phones (bulleted, one per line), Primary Email, Email (public record, bulleted), Owner Mailing Address, Acreage, Gross SF (squareFeet), Year Built, Last Sale Date and Price, Last Call Date, Call Result (Deal, Callback, Not interested, No answer, Wrong number; stored in `stages` as one token so the list page, filters and pipeline keep working), Call Notes, Follow Up Date, then a Transcript card (AqTranscript rows, newest on top, bulleted). Callback asks for a target date (callBackAt) which fills Follow Up Date (followUpAt) unless typed over; the dashboard's Call Me Back window keys on followUpAt (falling back to callBackAt) and shows the owner's numbers from the ticket plus linked contacts. Wrong number asks which number to drop and removes it from the property and from linked contacts with the same digits. The owner and their phones live on the property row; separate company and contact records are for brokers, buyers and operators, and the assistant's import guide says so.
- **Ask the CRM takes files and can be taught** (Sep 18, 2026), on all three sides (/ask, /israel/ask, /acquisitions/ask; the Acquisitions nav has it above Dashboard). One shared client (src/app/ask/ask-client.tsx: centered text box on an empty chat, moving to the bottom once it starts; folding conversation rail; Attach or drag-and-drop for .xlsx/.xls/.csv) and one shared library (src/lib/assistant.ts). A dropped spreadsheet is read sheet by sheet to CSV (xlsx package, clipped at 90k chars per file) and kept with the message (ChatMessage.data) so later turns still see it. Claude reads it with that side's Data rules and calls propose_import; the reply shows the rows with an Import button (importAction: companies matched by name, people by email or name at the company, Acquisitions properties by address and city; existing rows only get empty fields filled; nothing is written until Import). Acquisitions imports properties, companies and contacts and links them; RJL CA and Israel import companies and contacts. When the person corrects a reading or says "remember", Claude calls save_rule and the line lands in Setting `dataRules:<side>`, shown and edited under Settings > Data rules (src/components/data-rules-page.tsx; the Settings subnav on every side now has Users and Data rules). Shawn's existing file-reading rules go in that page for RJL Acquisitions. The Acquisitions logo and any top-level image are public in the proxy, since a path like /acquisitions-logo.svg read as an RJL CA page and was bounced for anyone without that side.
- **Acquisitions Deal Pipeline stages are data** (Sep 18, 2026): Setting `aqDealStages` (JSON list, column order), read by `getAqDealStages` in src/lib/acquisitions-stages.ts; AQ_DEAL_STAGES are only the defaults before the first edit. Edit stages on the pipeline page adds, renames (deals move with the name), reorders (Dead to the front, say) and removes an empty stage. Property forms and the card stage picker read the live list. The Acquisitions dashboard carries only the Call Me Back window for now (Jonathan removed the other windows).
- **Email reading is a switch per user per side** (Sep 18, 2026), on the Users card of all three Settings pages. It says whether that person's mailbox feeds THIS side's email log and lets Jonathan flip it. Stored in `User.mailReads` (JSON list of sides), read through `mailReadsFor` in src/lib/access.ts, which every mailbox selector uses (mail-sync.ts for RJL CA, acquisitions-mail.ts for RJL Acquisitions, israel-mail.ts for RJL Israel). One RJL CA mailbox feeds one side: switching it on for RJL Acquisitions switches it off for RJL Capital Advisors and vice versa. The Israel side needs an @rjlisrael.com address on file. With nothing set, the default follows access: an RJL CA address feeds RJL Acquisitions when that is the only RJL CA side the person opens, otherwise RJL Capital Advisors. Jonathan is a user of all three but his mail reads into RJL CA only. Adding a person from the Acquisitions Users page grants them AQ on top of what they had.
- **RJL Acquisitions is the third CRM** (Sep 18, 2026), for Shawn Aziz: workspace `AQ`, paths under `/acquisitions`, light yellow palette with dark blue accents (body class `acquisitions` in globals.css), third logo tile under the two others in the sidebar. It is walled off like Israel: its own tables (AqCompany, AqContact, AqProperty with AqPropertyCompany/AqPropertyContact joins, AqNote, AqActivity), its own pages (Dashboard, Contacts, Companies, Properties, Deal Pipeline, Settings), its own actions in src/app/acquisitions/actions.ts and constants in src/lib/acquisitions.ts. Access: an @rjlcapadvisors.com address can be granted CA, AQ or both (possibleByDomain); someone with AQ and not CA is "acquisitions only" (acquisitionsOnly) and their mailbox is read by src/lib/acquisitions-mail.ts into the Acquisitions log, never by RJL CA's mail-sync; their @rjlisrael.com address is still read by Israel. The daily cron (api/cron/mail-sync) and AQ page loads (kickAcquisitionsMailSync) run the sync. Roles are multi-token (Seller, Operator, Buyer) on companies and contacts, and a company's roles flow to its contacts. Property Stage is multi-token (Deal, Call me back, Not interested, No answer): "Call me back" requires a date and the property shows in the dashboard's Call Me Back window from that day, with the linked contacts' phone numbers, until Dismiss; "Deal" puts it in the Deal Pipeline at a dealStage (placeholder stages New, Underwriting, Offer Made, Under Contract, Closed, Dead until Jonathan and Shawn settle them). Address, neighborhood, city and state are separate fields with filters.
- **Check size in Data updates** is a range on the slider, never the old buckets. Proposal field `checkRange` replaces `checkSizes`: Claude proposes "min-max" in $MM (100 = $100MM and up), `parseCheckRange` reads "$3MM to $12MM", "5+", "up to $20MM", "$2,500,000 to $8,000,000"; the card shows "$3MM to $12MM"; Approve writes checkMinMM/checkMaxMM and keeps checkSizes buckets in step (checkBucketsFor) for the pages that still read them. A teammate's edit on the company page proposes the range too. Proposals filed before this (field checkSizes) still show and still apply.
