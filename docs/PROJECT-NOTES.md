---
name: rjl-crm-project
description: "RJL Capital Advisors custom CRM build replacing HubSpot - locations, stack, brand, scope decisions (started 2026-09-03)"
metadata: 
  node_type: memory
  type: project
  originSessionId: fdffdb35-ad78-46a7-a3c1-464ba2b6395a
  modified: 2026-09-03T20:43:23.021Z
---

Jonathan (RJL Capital Advisors, real estate capital advisory) is replacing HubSpot with a custom CRM built in Claude Code, started 2026-09-03.

**Locations**
- Source code: `C:\Users\Jon\Projects\rjl-crm` (local, not OneDrive, because node_modules breaks OneDrive sync).
- Docs, HubSpot exports, brand assets: `C:\Users\Jon\OneDrive - RJL Equities\Jonathan Livi\RJL Capital Advisors\New CRM Via Claude Fable 5\` (synced SharePoint folder the user shared). Subfolders: `01 HubSpot Export 2026-09-03`, `02 Brand Assets`, `03 Plan and Docs`.
- Node 24 installed per-user at `%LOCALAPPDATA%\Programs\nodejs` (winget install needs admin and was cancelled). Bash needs `export PATH="/c/Users/Jon/AppData/Local/Programs/nodejs:$PATH"`.

**Decisions**
- Stack: Next.js (App Router, TS, Tailwind) + Prisma. SQLite locally first, Postgres (Supabase) for hosted deploy. Vercel hosting planned.
- 5 users. Email provider is Outlook / Microsoft 365 (not Gmail). Fireflies for call transcripts.
- Brand from logo: ink `#1A2321` (near-black green), cream `#FFF8D9`, sky blue `#B7CFE8`.
- Deal pipeline stages (HubSpot order): Deal Mentioned, Deal Received, Deal Underwritten, Deal Taken To Market, Intro To Capital Made, Term Sheet Issued, Term Sheet Signed, Deal Closed, Deal Lost. Deals view is a board, not a table.
- Deal names follow "Sponsor | Property" pattern.
- Feature scope: companies, contacts, deals board, deal-driven mail merge to matched LPs, email blasts (via transactional provider, not Outlook), auto-log sent Outlook emails to contacts/companies, Fireflies-driven LP criteria proposals with manual approval.

**HubSpot export facts (2026-09-03)**: 6,188 companies, 15,157 contacts (13,686 with email, 357 unsubscribed, ~893 hard bounces), 1,017 deals (899 Deal Lost). Deals export only had default 6 columns; custom deal properties (property address, LTV, loan term, etc.) still need a fuller export or connector pull. Classifier fields are duplicated/cloned in HubSpot and should be collapsed to one multi-select: Sponsor / Investor / Retail Investor / Lender / Broker.

**Why:** These are decisions and locations not derivable from the code alone.
**How to apply:** Continue building in the local project folder; put user-facing docs and data in the SharePoint folder. See [[rjl-brand-colors]] if written later.

**Update 2026-09-03 (later)**: Phase 3 done: templates, campaigns w/ LP matching (src/lib/matching.ts, audience.ts), deal intake (src/lib/intake.ts, checklist.ts), role inheritance company->contacts (src/lib/roles.ts). Git repo initialized in project folder. Pending from Jonathan: ANTHROPIC_API_KEY (intake extraction), RESEND_API_KEY + MAIL_FROM (sending), deals re-export with all properties, 5 user list, choice of intake mailbox (M365 shared mailbox + Power Automate vs Resend Inbound), hosting (Vercel + Supabase). HubSpot sales email templates are NOT reachable via the connector; user must paste them. Business rule: company roles propagate to its contacts because blasts are segmented by contact role (Sponsor vs Investor).

**Update 2026-09-04**: Deal emails are ONE-AT-A-TIME and personal (Jonathan rewrites the first line per investor) -> built outreach queue (campaign mode OUTREACH, CampaignRecipient.openingLine, "Open in Outlook" mailto + "Mark sent"). Blasts (mode BLAST) are the only thing with unsubscribe footers. Intake mailbox choice: Resend Inbound (option B), handler at src/app/api/inbound/resend/route.ts. ANTHROPIC_API_KEY is set (intake uses claude-opus-5 structured output; schema uses all-string fields because API caps nullable/union fields at 16). 14 HubSpot sales templates imported via scripts/import-templates.ts; blast template seeded via scripts/seed-blast-template.ts. Users: Jonathan, Aviel, Shawn, Esther Livi, Nikko Acosta (all @rjlcapadvisors.com). Pending: Resend DNS (IT), Vercel/Supabase accounts, Fireflies phase, pref-equity/fund fields if still used.

**Update 2026-09-04 (tracker)**: Progress report ("tracker") lives at /deals/[id]/tracker (DealInvestor rows, 8 statuses in src/lib/tracker.ts matching the legacy Drive HTML report + Word template: Prepared For, Notable Feedback Themes, Items Needed from Sponsor = checklist gaps). Sponsor read-only share at /share/tracker/<signed dealId token>; legacy standalone HTML export at /deals/[id]/tracker/export. Follow-up button creates OUTREACH campaign with followUp=true using template "Follow-up – confirm receipt"; logSent moves tracker rows to Deal Sent / Followed Up. Jonathan's workflow: checklist -> deal ticket -> template -> send one-by-one -> tracker shared with sponsors -> follow-ups. Response statuses 4-8 are manual until Outlook inbound logging (phase 4).

**Mission (2026-09-04)**: forward deal to deals@ -> auto-log + extract from email/PDF/model -> reply with checklist gaps -> sponsor replies auto-fill ticket -> pick LPs (criteria filter + learned suggestions from emails/Fireflies) -> draft engagement letter listing groups -> sponsor approves subset -> approved groups onto progress report -> deal email generated, best contact per firm chosen from email history -> one-by-one launch -> real-time tracker, PDF any time, Thursday 4 PM reports to sponsors -> one-button follow-up -> auto stage updates, Monday 11 AM dead-deal cleanup (keep as data). Full text in SharePoint "CRM Build Plan.md" Mission section.

**UX rules from Jonathan (2026-09-04 evening)**: no cream/yellow (white, light blue, black only); dashboard = to-do list only; fields always in ONE straight column on the LEFT of record pages (HubSpot layout: left fields, center activity/preview, right association cards); deal fields differ by asset class (MF per unit+per foot; retail/industrial per foot; hotel per key; student per bed); cap rates before IRR/EM/YOC/CoC; thousands separators; forwarded deals create deals immediately (no intake review); Deal Lost first column; contacts filed under company by email domain (src/lib/domains.ts). House-style deal email generator in src/lib/deal-copy.ts, template "Deal email (house style)".

**Criteria spec (2026-09-04)**: asset classes (multi dropdown); check sizes $1-2MM, $3-5MM, $5-8MM, $8-10MM, $10-15MM, $15-20MM, $20-30MM, $30-50MM, $50-100MM, $100MM+ (old HubSpot buckets migrated via CHECK_SIZE_MIGRATION); deal locations = free text (geographyNotes; tags derived internally); return profile (Core, Core+, Light value-add, Heavy value-add, Opportunistic); Development/Acquisitions/Both single; hold period (1-3, 3-5, 5-7, 7-10, 10+ years); vintages (Older than 1960, 1960s…2020s, New Construction); OZ interest Y/N; closing time frame Fast/Average/Slow; open to minority Y/N; other info text. HUD D4 interest dropped. Investment types (JV/Pref/Debt) column kept for matching but hidden from UI. Criteria panel open only if company role includes Investor/Retail Investor/Lender; sponsors see "Sponsor focus" = asset classes only.

**Update 2026-09-04 (late)**: Nav now has "Active progress reports" (/reports) under Investor search: every deal out to investors, sorted by longest wait, with Follow up buttons; Jonathan checks it daily. "Campaigns" renamed "Email blasts" in UI (routes still /campaigns). Four real progress reports loaded from Word (Everett Mall Plaza, Kansas City Metro Retail Portfolio/BSPG, Purveyor Space Coast, Marble Capital Intros) via scripts/load-progress-reports.js; intake test deal deleted. Company page mirrors contact page (Activity first, criteria card below, Deals card shows deals the firm was sent w/ status). Companies + Contacts lists: NO search button, auto-filter on roles + asset classes (multi-check), email-status filter removed; contacts inherit asset classes from company criteria (blast segmentation = role + asset class only). Lists sort by lastActivityAt desc (nulls last); src/lib/activity.ts logActivity() bumps contact + company on every email/note/tracker change. Jonathan said he loves the contact page look; keep it as the reference style.

**Update 2026-09-04 (night)**: Progress report = exact copy of Jonathan's Word reports (simple: Arial, centered header lines, bullets, black-bordered table, doc status colors in src/lib/tracker.ts). He wants it SIMPLE; do not re-add dark headers/chips/action items. Company enrichment from website lives in src/lib/enrich.ts (claude-sonnet-5), auto on new domain companies + Refresh button + npm run enrich; Jonathan expects company info (website, description, location) to be filled automatically from the email domain. Company page: About collapsible, criteria under it on the left (full for Investor, asset classes only for Sponsor).

**Rule (2026-09-04)**: Company ROLES and INVESTOR CRITERIA are Jonathan-only; never auto-write either without his approval. Exception he asked for: SPONSORS get asset classes (+ focus markets) filled from their website when blank, triggered every time a sponsor is emailed (outbound EMAIL activity -> src/lib/activity.ts refreshSponsorLater -> enrichCompany force). Other company facts (website, description, phone, LinkedIn, HQ, year founded) auto-fill for everyone. Progress report themes/items are generated by Claude from tracker notes (src/lib/tracker-summary.ts) on note save + button; deal stage is togglable via dropdown on the deal ticket.

**Update 2026-09-07**: To-do page = ONLY two windows (quiet LPs 2+ days w/ Respond now button; criteria updates to approve). Jonathan found the derived to-do list unhelpful; do not bring it back. Criteria proposals engine: src/lib/criteria-proposals.ts (claude-opus-5, conservative: standing rules only, no deal-specific passes), fed by tracker notes now; wire EMAIL replies + Fireflies when connected. Respond now = mailto today; upgrade to Graph reply-all with attachments after M365 (Azure app registration).

**UX (2026-09-07)**: The dashboard page is called **Home** (not To do). Jonathan wants it easy on the eyes: larger text (15px base), few black buttons; use the light-blue .btn-soft for row actions, black btn-primary only for the one main action on a page.

**Infra (2026-09-07)**: DB is now Supabase Postgres (provider postgresql; DATABASE_URL = Supabase, password percent-encoded; SQLITE_DATABASE_URL = old local file kept as backup). Prisma contains filters need mode: insensitive. Resend sending works (domain verified). Vercel account exists (Hobby, email login); vercel CLI installed as devDependency; deploy pending (needs Jonathan to run vercel login in a browser). Never print .env values; check prefix/length only.

**Deployed (2026-09-07)**: https://rjl-crm.vercel.app (Vercel team rjl-capital-advisors, project rjl-crm; deploy with npx vercel --prod --yes; env vars set via vercel env add, values piped from .env). Password gate in src/proxy.ts + /login (APP_PASSWORD in .env and Vercel). Prod DATABASE_URL = Supabase pooler (port 6543, pgbouncer=true); local = direct 5432. Next steps after Jonathan's items: Resend inbound webhook URL https://rjl-crm.vercel.app/api/inbound/resend, Microsoft sign-in, Thursday 4pm reports (Vercel cron).

**M365 (2026-09-07)**: Graph app-only auth works (AZURE_TENANT_ID/CLIENT_ID/CLIENT_SECRET in .env + Vercel; client_credentials, scope https://graph.microsoft.com/.default). Consent granted. Tenant users: Jonathan, Aviel, Shawn, Esther, Nikko (+ Chaim Levtov, Eduardo Viesca, IT). DEALS_MAILBOX=deals@rjlcapadvisors.com shared mailbox exists. Next build: Graph email sync (sent items -> Activity + lastActivityAt), deals@ inbox -> intake, Respond now via createReplyAll draft, Microsoft sign-in replacing APP_PASSWORD.

**Auth/rules (2026-09-07)**: Microsoft sign-in via /api/auth/microsoft + callback (session cookie rjl_user signed with APP_SECRET; src/lib/session.ts, current-user.ts). Per-user mailbox actions use currentUser().email. Only Jonathan (CRITERIA_ADMINS) may edit/approve investor criteria; other users see read-only. Company-wide signals (criteria proposals) should draw from ALL team mailboxes. Follow-up drafts tracked on DealInvestor.followUpDraftId; syncFollowUpDrafts flips to status 3 when sent. Sliders trial for span fields (USE_SLIDERS).

**Naming (2026-09-07)**: the investmentTypes field is labeled **Position in the capital stack** everywhere (never 'Type of investment'). Values: Senior Debt, Mezz Debt, Preferred Equity, Participating Preferred Equity, JV Equity, Co-GP Equity, Programmatic. Non-admin criteria edits -> CriteriaProposal source MANUAL (sourceRef = editor name), never direct writes.

**Email log (2026-09-07)**: mail-sync.ts syncs all active users' mailboxes via Graph into Activity(type EMAIL, meta JSON, externalId = internetMessageId). kickMailSync() on page loads + /api/cron/mail-sync (CRON_SECRET, daily). Company page center = EmailLog feed (no note form). Roles live in the criteria card (CriteriaForm roles prop); About card has no roles. Nikko's mailbox synced 2240 msgs but logged 0 - investigate matching. Ranges: src/lib/ranges.ts (checkMinMM/checkMaxMM, holdMinYears/Max, vintageMin/Max; buckets derived).

**Handle button (2026-09-07)**: Home follow-up button is named **Handle** (was Respond now). Opens the Graph draft in classic desktop Outlook via outlook:<entryId> (translateExchangeIds, sourceIdType restImmutableEntryId), web link as fallback. Creating the draft must not bump DealInvestor.updatedAt (row would vanish from the 2-day list); row leaves only when the draft is sent. Faded buttons = contact has no email (placeholder '(firm)' contacts from the Word-report import).

**Desktop Outlook bridge (2026-09-07)**: outlook: protocol is NOT registered on Jonathan's PC and browsers only launch apps on a direct click. Solution: two-step Handle (prepare -> 'Open in Outlook' link) whose href is rjlcrm:open?mid=<Message-ID>; scripts/setup-outlook-link.ps1 registers rjlcrm: (HKCU) to scripts/outlook-open.ps1 which uses Outlook COM (Drafts.Items.Find on PR_INTERNET_MESSAGE_ID, then .Display()). Jonathan must run setup once per PC (I do not modify system settings myself). Jonathan's desktop signature (text-only 'RJL Cap Sig', 9pt, colors #53565A/#717073/#0E2841) is stored in User.signatureHtml; never use the phone one from sent mail.

**Deals mailbox (2026-09-07)**: src/lib/deals-inbox.ts processes deals@ inbox (Graph), attachments via src/lib/attachments.ts (pdf-parse, xlsx, html-to-text), DealIntake.messageId dedupes. Reply sent from deals@ via createReply+send (graph() now tolerates empty 202/204 bodies). Graph subscription -> /api/graph/notify (clientState = CRON_SECRET), renewed in /api/cron/mail-sync. Forwarder is internal so sponsor sender is parsed from the forwarded 'From:' line.

**Home = 3 windows (2026-09-07)**: LP follow-ups (tied to progress reports only), Deal momentum (src/lib/momentum.ts, Momentum model, kinds SPONSOR_ITEMS/INTRO/ACTION/MENTIONED, refreshed in kickMailSync + cron), Criteria updates (Jonathan). Generic DraftButton (src/app/draft-button.tsx) replaces respond-now. AutoSaveForm (src/components/autosave-form.tsx) used on company page forms; Jonathan wants edits to save automatically everywhere on company items. RoleCell inline on Companies list. Graph subscription id created 2026-09-07 for deals@ inbox -> /api/graph/notify.

**Home floor (2026-09-07)**: HOME_SINCE = 2026-08-31 in src/app/page.tsx; LP follow-ups, Deal momentum, and criteria proposals that began before that date are hidden so the dashboard is not flooded. Jonathan wants Home light and easy on the eyes; the backlog lives on report/deal pages.

**Ticket rules (2026-09-07)**: applyDealRules() in src/lib/intake.ts (majority equity => JV Equity; dev => no occupancy/yearBuilt/cap rates); extractor enums snap unitMix/holdPeriod (DEAL_HOLD_PERIODS)/loanTerm/amortization to taxonomy lists, never free text; dev deals store leverage as ltc not ltv; checklist core fields expectedClose + loanTerm; deals@ forwarder = deal owner. Deal form fades dev-irrelevant rows via isDev state.

**Engagement letter (2026-09-07)**: /investors?dealId=..&mode=engagement (checkboxes + Done via DraftButton) -> createEngagementLetter action -> src/lib/engagement.ts createEngagementDraft (Graph new draft to sponsor recipient = intake sender else most-emailed contact; groups -> DealInvestor status 1; deal.details.engagementGroups). Template text is Jonathan's verbatim; 2% fee, 30-day carveout.

**Investor search check size (2026-09-07)**: single exact slider (Spec.checkMM in $MM, 1..100); a firm matches when checkMin <= checkMM <= checkMax (checkMax 100 = open top). Preset from deal.requestedAmount rounded to $MM. Engagement mode uses the same search, so it inherits this. Column shows the firm's range label.

**AI suggestions (2026-09-07)**: src/lib/suggest-investors.ts -> deal.details.aiSuggestions cache (24h). Jonathan wants the CRM to 'learn the business' from all firm data (emails, calls, tracker notes) and offer opinions alongside his manual picks; no redundancy (shared picked set). Extend with Fireflies transcripts when FIREFLIES_API_KEY arrives.

**Stages (2026-09-07)**: DEAL_STAGES now includes 'Engagement Letter Sent' after Deal Underwritten. syncEngagementDrafts() (src/lib/engagement.ts) watches deal.details.engagementDraftId/engagementMailbox and advances the stage when the draft is sent; runs from kickMailSync and alongside syncFollowUpDrafts. Automatic stage moves are wanted throughout (Jonathan's mission item).

**Send deal (2026-09-07)**: src/lib/send-deal.ts (finalizeEngagement, createSendDrafts, syncSendDrafts, STAGE_ORDER incl. Engagement Letter Signed), /deals/[id]/send page + send-client.tsx, engagement-card.tsx on deal page. Email blasts = marketing only, never part of the deal process. Send deal = per-firm Outlook drafts from EmailTemplate (house style default), signature appended, template sign-off stripped, attachments copied from the deals@ intake message. Dashboard is the page name now (was Home, was To do).

**Dashboard/report (2026-09-07)**: 4 windows in 2x2 (h-[calc(50vh-70px)]); Deals ready for launch = stage Engagement Letter Signed. All record forms auto-save (AutoSaveForm) incl. DealForm autosave prop. Report in-app: headerEditor slot (two textareas + Prepared For), status filter ?status=N, showPeople false, labels bold values plain. Jonathan wants windows squarish, not tall.

**Intros (2026-09-07)**: src/lib/intros.ts (Intro model; scanIntros via Graph filter startswith(subject,'Intro') on each mailbox's Sent Items, thread activity via conversationId; quiet threshold 14 days). Dashboard = one row of 5 compact Window components (h-[calc(100vh-140px)]). Graph note: combining startswith filter with date filter/orderby errors; filter dates client-side.

**Send page v2 (2026-09-07)**: tokens + contentEditable per-firm email (drafts state: subject/html/touched) + LAUNCH (launchDealEmails: createDraft -> copy attachments -> /send per firm, status 2 now) + preview-to-self ([PREVIEW] prefix). Jonathan wants: individual emails, edit each before launch, launch all at once, preview to himself. Old createSendDrafts flow retained but not used by the UI.

**Deal email style (2026-09-07)**: subject + 'They are seeking' use usdShort ($5.6MM); seeking sentence bold; 'Deal Metrics' bold+underlined; bullet lead-ins bold (split at first ': '); 12pt gap after metrics before Business Plan. Bash pitfall: never write TS with literal $ via bash heredoc/node -e; use the Edit/Write tools.

**Send page v3 (2026-09-07)**: dealFiles(dealId) = attachments on the deals@ intake thread from the sponsor (dedupe by name, models/PDF first; images skipped), selectable, all on by default. usualRecipients(companyId) = people on >=40%% of our outbound emails to the firm in 12 months (email log meta.to/cc) -> default To. Contact.unsubscribed only matters for blasts; one-to-one deal emails include everyone with an email (Dwight Stephenson at Blue Vista was hidden by that flag).

**Git remote (2026-09-07)**: origin = https://github.com/RJLCapitalAdvisors/RJL-CRM.git (private). Pushing is blocked for me by the permission classifier; Jonathan runs `git push` himself (give him the command in a bash block). Repo carries docs/PROJECT-NOTES.md (copy of these notes; keep both updated) and docs/SETUP-LAPTOP.md. .env is not in git; he keeps it in his password manager.

**Handle fallbacks (2026-09-07 night)**: any Handle (LP, momentum, intro) must never dead-end on 'not in your mailbox': order = exact thread in my mailbox -> deal follow-up row -> replyToLatestWith(me, person) (latest Sent Items thread with them, prefer deal words) -> fresh email. Bridge outlook-open.ps1 tries GetItemFromID(eid) first (+SendAndReceive), then Message-ID, then Drafts folder; the rjlcrm: URL carries mid and eid. Graph-created drafts can lag syncing into classic Outlook.

**Mentions + intros (2026-09-08)**: src/lib/mentions.ts detectMentionedDeals() reads recent sponsor threads (full bodies via Graph) and creates stage 'Deal Mentioned' deals (MentionScan table dedupes); runs at the start of refreshMomentum. Intros: zero replies -> flagged after 3 days (UNANSWERED_INTRO_DAYS); otherwise quiet 14 days. Handle order now: my mailbox -> teammate/deals@ copy of the exact thread (replyViaTeammateCopy builds reply-all in my mailbox) -> deal follow-up row -> latest thread with the person -> fresh email.

**LP asks + forwarder notes (2026-09-08)**: src/lib/lp-asks.ts reads inbound LP emails on active deals (LpAskScan dedupe), writes tracker note/status, and momentum kind LP_ASK (party = LP name) sorts first; its Handle drafts a fresh email to the sponsor's usual recipients listing the asks. src/lib/forwarder-notes.ts: text a teammate types above a forwarded deal to deals@ is parsed (stage, investors already introduced -> Intro Made rows). Mentioned-deal tickets only nag in momentum when the sponsor promised to send (details.promised); all still appear in the Deal Mentioned column. Bridge: wscript launcher (outlook-open.vbs) avoids the black console; helper logs to %%LOCALAPPDATA%%\RJL CRM\last.log. Jonathan must re-run the Desktop 'Connect RJL CRM to Outlook' to switch the handler to wscript.

**Bridge v3 (2026-09-08)**: Graph-created drafts do NOT sync into Jonathan's classic Outlook (log proved GetItemFromID + Message-ID both fail). New default: rjlcrm:reply?mid=<ORIGINAL sent email Message-ID>&greet=&att=1 -> outlook-open.ps1 finds the original locally (Sent/Inbox/Drafts, then AdvancedSearch), calls ReplyAll(), Display() (native signature), prepends greeting, re-attaches files; falls back to opening the server draft. FollowUpResult.replyTo carries {messageId, greeting, attachments}. syncFollowUpDrafts also treats any outbound email to the LP after the click as the follow-up sent (and deletes the unused server draft). Companies list: Roles, then Asset classes (inline AssetCell; sponsors direct, investors via proposal unless Jonathan), Location last. Report bullets need listStyle disc (Tailwind preflight removes bullets).

**Sponsor linking (2026-09-08)**: sponsorContactsFor(dealId) in src/lib/engagement.ts = usual recipients at the sponsor company -> else external people on the deal's own email threads who are not LPs on the report (and links the company to the deal) -> else the intake sender. Used by LP_ASK Handle and engagement letters. Lesson: when importing/creating sponsor companies, check for an existing company by domain/name first (BrightStar duplicate came from my Word-report load).

**Ticket clearing (2026-09-08)**: Dashboard load runs syncRecentSent(me) (Sent Items last 6h) before judging LP follow-ups/intros, so a reply sent from Outlook clears the ticket on the next load; kickMailSync throttle 3 min. Intro.handledAt set on Handle; quietIntros hides handled intros once an outbound email to a recipient exists after handledAt (30-min grace). Jonathan expects tickets to disappear by themselves after he sends the email.

**Deal knowledge (2026-09-08)**: models DealEmail (intake + follow-ups, conversationId), DealFile (ticket Attachments; download /api/deals/[id]/files/[fileId]), DealFact (Questions answered). src/lib/deal-knowledge.ts: matchExistingDeal (thread id, then deal-name words in subject/body + sponsor domain, score>=2) -> deals@ follow-ups attach files, extract Q&A facts, merge blanks into ticket, reply 'Added to <deal>…'. dealFiles() reads DealFile first. LP asks annotate 'Already on the ticket: …' from factsFor(). Jonathan's principle: a deal ticket keeps learning; never create a second ticket for the same deal.

**FAQ + files + multi-deal (2026-09-08)**: src/lib/faq-pdf.ts builds '<Deal> - Investor FAQ.pdf' (pdf-lib) from DealFact; appears as virtual file key 'faq' in dealFiles() (ticked by default on Send) and downloadable at /api/deals/[id]/faq.pdf. File links carry signed tokens (signFileToken) so drag-out (DownloadURL) works without cookies; proxy lets /api/deals/*/files and faq.pdf through. AttachmentList component: type icons + draggable rows. Momentum kind ENGAGEMENT: letter sent 18h+ with no inbound from sponsor. deals@: detectMultipleDeals() splits an email into one ticket per deal (files assigned per deal; DealIntake/DealEmail messageId suffixed #2, #3).
